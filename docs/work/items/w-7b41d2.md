---
work_item: true
id: w-7b41d2
state: working
priority: high
owner: agent-e5a71b
updated: 2026-09-26T15:10:00Z
branch: madgab-canon-reach
worktree: /workspace/madgab-canon-reach
---

# The canonical "hits justice dupe hid came" clue is unreachable for its own target

## Goal

`tests/corpus_integration.rs::approximate_finds_classic_madgab_resegmentation`
fails on `post-milestone-acceptance` at `293d723` (and at `45419a7`
before it). This item exists to own the fix. The test is correct: the
resegmentation of *It's just a stupid game* as *hits justice dupe hid
came* is the canonical Mad Gab and it is the whole reason approximate
mode exists. Do not delete or relax the assertion to make the suite
green.

Found by the [w-3d6c81](w-3d6c81.md) determinism pass, which is
`src/`-read-only and so can only report this.

## Context and measurement

Measured on `post-milestone-acceptance` at `293d723` with the release
binary:

- `--approximate --top 50 "It's just a stupid game"`: the canonical
  clue is absent. Same at `--top 100`, `--top 200`, `--top 500` and
  `--top 2000`.
- The failure is **not** in ranking, selection or the diversity rule.
  `MADGAB_TRACE_PHRASES` reports
  `raw phrase="hits justice dupe hid came" missing candidates=17405`
  even at `--top 2000`, which is the maximum retention budget
  (`top_n * 128` clamped to `[1024, 8192]`, so `final_keep = 8192`
  there). The phrase is absent from the *deduplicated candidate pool
  itself*, so it is lost before the scorer ever ranks it.
- The alternative wordings that do survive are the same resegmentation
  with different clue words: `it justice too bad aim` (0.925),
  `hits justice too add aim` (rank ~1000 at `--top 2000`), and so on.
  Across the whole visible list at `--top 2000` the words `hits` and
  `came` occur, but **`dupe` and `hid` never occur at all**.

So the structural resegmentation survives and two of the four clue
words are reachable; `/dup/` and `/hɪd/` are not. That points at
lexical reachability in the approximate span match — the span
shortlist in `FuzzyLexicon::matches_at` (`src/approx.rs`), its edit
budget, or `min_word_ipa_chars` — rather than at scoring. Note
`"it's just a stupid game"` contains the exact substring *stupid*, so
`dupe` and `hid` are expected to be ordinary high-confidence matches
for the `/stʊp/` and `/ɪd/` spans; a correct `dupe` would be
`/dup/` against `/stʌp/` (a single `t`→`d` substitution plus the
`st` reduction).

Whether this is a regression introduced by the recent
scoring/selection/optimization work or a long-standing gap is not
established here; `git log` bisecting it is the natural first step and
is cheap.

## Completion criteria

- The mechanism is identified by reading the search, with the fix
  located in the code, not worked around in the test.
- `cargo test --release --test corpus_integration` passes, including
  `approximate_finds_classic_madgab_resegmentation`, and
  `--lib` and `--test exact_determinism` still pass.
- `tests/approx_determinism.rs` (from
  [w-3d6c81](w-3d6c81.md)) still passes: whatever restores the
  canonical clue must not cost approximate mode its run-to-run
  determinism or the `top_n` membership monotonicity, and those two
  properties are the guard for exactly this kind of change.
- `cargo fmt`, `cargo clippy` and doctests do not exist on this host;
  see [../../environment-notes.md](../../environment-notes.md). Do not
  report them as satisfied.
- No phrase-specific special-casing in `src/` to make this one target
  pass.

## Handoff / notes

Claimed at 2026-09-26T13:45Z by coordinator `coord-5e1d` and assigned to
Antonina agent `e5a71b` in `/workspace/madgab-canon-reach` on branch
`madgab-canon-reach`, based on `post-milestone-acceptance` at `76c693c`
(clean tree, `src/` writable, nothing in flight here).

### The contradiction this front exists to settle

Two fronts disagree about the *same* clue on the *same* head, and the
disagreement is about which kind of problem it is:

- [w-04f83f](w-04f83f.md) (scorer, agent `04f83f0`) measured that all
  five canonical words survive the per-span shortlist, that the lexical
  enumeration only misses the full combination at prohibitive cost, and
  that under the real final scorer the alignment scores **0.819901**
  against a top-10 cutoff of **0.929514** — i.e. a search/objective
  problem, fixable by reweighting.
- [w-3d6c81](w-3d6c81.md) (determinism, agent `3d6c810`, `src/`-read-only)
  measured the release binary at `293d723` and found the phrase absent
  from the **deduplicated candidate pool** even at `--top 2000`, and that
  the words `dupe` and `hid` never occur in any visible list at all
  while `hits` and `came` do — i.e. a lexical-reachability problem inside
  `FuzzyLexicon::matches_at`, not fixable by reweighting.

These cannot both be right, and the difference matters: reweighting
cannot produce a word the matcher never returns. The likely
reconciliation is that the scorer front's reachability evidence came
from a test harness with a relaxed or non-default budget rather than
from the real default path, and the determinism front measured the real
one — but that is a hypothesis, and this item's first job is to settle it
by measurement, not by argument.

### How this front is scoped to avoid contention

`04f83f0` and `d17a620` both hold uncommitted `src/lib.rs` edits in
their own worktrees, so:

- the **first** deliverable is diagnosis: locate, by reading and by
  instrumented measurement, the exact place where `dupe` (against
  `/stʌp/`) and `hid` (against `/ɪd/`) fail to appear in
  `matches_at`'s returned shortlist under the *default* approximate
  configuration, and record that finding in this work item;
- temporary instrumentation is a diagnostic, not the deliverable, and
  must not reach `post-milestone-acceptance`; the measurements go in
  this item instead, as [w-04f83f](w-04f83f.md) does for its
  `zz_dump_canon` survey;
- `git log` bisecting the two claims against each other is cheap and
  should be done early: it may show the reach front
  ([w-6d1f45](w-6d1f45.md), integrated as `9ae1f5a`) is what stopped
  `dupe`/`hid` from being returned, which would be a far cheaper answer
  than any new axis;
- only then implement, and only the general fix. No phrase-specific
  special case, no new test-only escape hatch, no per-word allowlist.


## Pass 2026-09-26T14:10Z (coordinator coord-6f0a): the coordinator reproduced the pool-level absence

Independent re-measurement on the current head, `434312e`, with the release
binary built from the current tree, on the **default** approximate path with
no relaxed budget:

```text
$ MADGAB_TRACE_PHRASES="hits justice dupe hid came" \
    ./target/release/madgab --approximate --top 200 "It's just a stupid game"
MADGAB_TRACE raw phrase="hits justice dupe hid came" missing candidates=17405
MADGAB_TRACE raw_cutoff rank=199 score=0.906262887 phrase="it justice too uhh dame"
target: It's just a stupid game
IPA:    /ˈɪtsdʒˈʌstəstˈupədɡˈeɪm/
(corpus loaded in 499ms; search 3007ms)
```

So [w-3d6c81](w-3d6c81.md)'s pool-level claim reproduces on the current head
and [w-04f83f](w-04f83f.md)'s reachability premise does not: the alignment is
never enumerated on the real path, so **no reweighting of the scoring axes can
surface it**, and the 0.819901-vs-0.929514 decomposition that front swept for
half an hour was measured through a harness with a wider budget than the
default one. This is now settled by measurement rather than by argument, and
this front's diagnosis is the only route to the milestone.

Additional evidence for the lexical-reachability reading, from the same run's
visible list: for the `/stʌp/` and `/ɪd/` spans the matcher does return
near neighbours — `it justice tube day mm` (rank 145), `it justice too ugh
came` (173), `it justice coupe add aim` (166), `shit justice too add aim`
(161) — but never `dupe` and never `hid` at any rank. `dupe` is not a wild
stretch: the target literally contains `stupid`, so `/dup/` against `/stʌp/`
is a `t`->`d` substitution plus the `st` reduction. Its absence next to
`tube`/`coupe`/`ugh` points at a *ranking or admissibility cut inside the
span match*, not at a lack of close candidates.

Agent `e5a71b` is running in `/workspace/madgab-canon-reach` and has been
steered with three things this pass established:

- **do not trust `git bisect` on this tree without forcing a rebuild.** The
  commits around the boundary are docs-only, so cargo's fingerprint is
  unchanged and `cargo build --release` finishes in 0.02s while the binary
  under test is still the previous commit's. The agent had already drawn a
  "first bad commit is a docs commit" conclusion from this and needs
  `git checkout <rev> && touch src/lib.rs src/approx.rs && cargo build
  --release` at every step;
- the deliverable is the **exact rejection point** — line, span, and the
  value of the quantity that rejects `dupe` (edit budget, `min_word_ipa_chars`,
  shortlist cap, or score floor) — ahead of any fix;
- `04f83f0` has independently found that the diversity policy and the
  structural-diversity test re-derive a clue's resegmentation by summing
  clue-word IPA lengths instead of reading `madgab::Clue::cuts`. That is
  orthogonal; this front must not merge it, and must not be contradicted by
  it without measuring.

Left running. No source change on this pass, and nothing to integrate.

## Pass 2026-09-26T15:10Z (coordinator coord-8b31): the rejection point is now named, and 3d0130a has landed under it

Two fronts had been measuring this item's target independently and had
reached apparently different conclusions. Both are now in the repository,
and the difference between them is a stage, not a disagreement.

[w-04f83f](w-04f83f.md)'s agent committed its measurement record as
`79014de`, which is now merged into `post-milestone-acceptance` as
`3d0130a` (with its `adfc7c4` fix). It reports, from the built-in
`MADGAB_TRACE_SPANS` / `MADGAB_TRACE_WORDS` trace on the **default**
approximate configuration at `434312e`, with target offsets
`it's`=3, `just`=8, `a`=9, `stupid`=15, `game`=19:

- the structural DP **keeps** the canonical segmentation, at rank 151 of
  256;
- all five words **do** survive the per-span shortlist, at per-span ranks
  22, 0, 64, 85 and 7 of 160;
- but `LEXICAL_BRANCH_KEEP = 10` truncates every slot to its ten
  highest-ranked alternatives *before* the enumeration runs, so `dupe`
  (rank 22) and `hid` (rank 85) are never expanded.

That is consistent with this item's pool-level measurement
(`MADGAB_TRACE raw phrase=... missing candidates=17405`): the words are
reachable, and the combination is not enumerated. It also falsifies
[w-04f83f](w-04f83f.md)'s original 0.819901-vs-0.929514 framing, which was
produced through a harness with a wider budget than the default path.

`e5a71b`'s own instrumentation, from its latest log turn, reported
something that reads as a contradiction — the canonical partial never
appears in `beam[p]` even at `p=3`, where the segmentation is already
decided — and its own log contains the likely explanation: `prune_partials`
takes `beam[p]` before the probe reads it, so `in=0` and a probe placed
after the take sees nothing. Reconciling that against the rank-151
finding, by line and by ordering, is now the first thing asked of the
agent, so this item does not end up with two of its own documents
disagreeing.

### State of the branch

`madgab-canon-reach` is at `85b4fab` with an uncommitted instrumented
`src/lib.rs`. It must be rebased before final validation: the accumulation
branch has moved to `3d0130a`, which adds `Clue::cuts` and replaces the
re-derived `clue_cuts` with `clue_structure`. Validation expected on the
rebased head: `--lib` 30/30, `--test exact_determinism` 1/1,
`--test approx_determinism` 2/2, `--test corpus_integration` 9/9 if the
canonical clue lands (it is 8/9 now, and that one failure is this item's
target).

### Steer delivered

`e5a71b` was steered, non-blocking, with: the `LEXICAL_BRANCH_KEEP`
measurement and a pointer to the item section holding the full table; the
instruction to reconcile its own beam probe against the rank-151 finding
explicitly rather than leaving a contradiction; the requirement to
locate the cap in `src/` and say **what it exists to bound** — memory,
time, or pool size — because a cap with a reason must be replaced by a
bound that serves the same reason rather than deleted; a preference for
budget-derived retention over a raised constant, in the spirit of
`span_score_bound`; and the standing prohibition on phrase-specific
special cases, per-word allowlists and test-only escape hatches.

`d17a620` ([w-d17a62](w-d17a62.md)) was steered separately and does not
touch this item's code: its `36c7b99` must have its bit-identity claim
re-measured against `3d0130a`, because that commit deliberately changes
which proposals are visible.

Left running. No source change on this pass from this front; the
integration that did land (`3d0130a`) was `w-04f83f`'s finished work.
