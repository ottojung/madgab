---
work_item: true
id: w-9c2d51
state: done
priority: normal
owner: agent-a1b2c303
updated: 2026-09-26T10:05:00Z
branch: madgab-clue-quality
worktree: /workspace/madgab-clue-quality
---

# Add a content-word prior to approximate clue scoring

## Goal

Approximate mode should not propose clues built out of English closed-class
words. It should prefer content words, so that the proposals it returns
read like something a person would actually say out loud as a puzzle
answer.

## Context

Continuation of [w-4b1e07](w-4b1e07.md). The current top proposals for
real targets are function-word salads:

- `It's just a stupid game` -> `it justice two bad aim`,
  `each thus tess to pad aim`
- `recognize speech` -> `wreck a nice each`
- `a whole lot of trouble` -> `so la cut are -able`

Both canonical answers are content-heavy by contrast: `hits justice dupe
hid came` has no closed-class item, `wreck a nice beach` has only the
article. Under a closed-class penalty the same prior that improves
general output quality also happens to lift both canonical cases, which is
why this is worth measuring properly rather than guessing at weights.

Antonina agent `a1b2c301` reached the same conclusion independently:
"closing that needs a *lexical quality* model (closed-class word class,
English bigram plausibility), not a better search".

The corpus (`open-english-pronouncing-dictionary` 0.1) already carries
`rarity` for every entry, so a small, standard closed-class word list is
the only new data needed, and no new dependency.

## Completion criteria

- A general closed-class / content-word signal participates in approximate
  clue scoring, derived from a linguistically standard closed-class list,
  not from any observed candidate set.
- The weight is chosen by measuring proposal quality over a spread of
  targets, and the measurement is recorded in this item. It must not be
  tuned against `recognize speech` or `It's just a stupid game`.
- A regression test expresses the property at an appropriate boundary:
  for a spread of targets, the proposals are predominantly content words
  and no single boundary structure occupies most of the proposal list.
- `cargo test --release` is green and search wall clock does not regress.
- If the evidence shows the axis does not improve aggregate quality, the
  item is closed as `superseded` with the measurements, not left half
  applied.

## Handoff / notes

In progress under Antonina agent `a1b2c303` (worktree
`/workspace/madgab-clue-quality`, branch `madgab-clue-quality`, base
`6250ba3`, which is now the content of `post-milestone-acceptance` at
d46d154). The claim was found unrecorded in the metadata even though the
agent had been running for over an hour; a later pass had started it and
exited before pushing the claim. Recorded here so the next pass does not
start a second agent on the same front.

As of 2026-09-26T08:50Z the working tree has uncommitted `src/lexical.rs`
(new module) and edits to `src/lib.rs`: a `CLOSED_CLASS_WEIGHT` scoring
axis plus a `closed_class_axis_is_not_covered_by_the_existing_axes` unit
test, with a standard closed-class list rather than any observed candidate
set, as the item requires. Nothing is committed yet, so if the agent dies
the work is lost — that is the main risk to watch on the next pass.

The agent's base predates the integration commit but not its content, so
`madgab-clue-quality` can be merged into `post-milestone-acceptance`
without a rebase. Expect a real merge conflict only in `src/lib.rs`
alongside the performance front ([w-7fa26c](w-7fa26c.md)), which is
editing `prune_partials` in the same file.

## Pass 2026-09-26T10:05Z (coordinator, integration review and close-out)

Integrated into `post-milestone-acceptance` as a merge of
`madgab-clue-quality` (4a0bedb), commit **50bdda1**, plus **9633013** for
two cosmetic comment fixes the front left behind (a duplicated block in
`prune_partials`, a missing newline after `mod axes {` — `cargo fmt` cannot
run here, see [../../environment-notes.md](../../environment-notes.md)).
`main` untouched. The branch was already pushed.

Review performed before integrating:

- read the full 1010-line diff: new `src/lexical.rs` (a standard,
  category-partitioned closed-class list with clitic and `n't` morphology
  derived by rule), a `CLOSED_CLASS_WEIGHT = 0.15` axis threaded through
  the final score, the per-span `quality` and `SlotAlt::contribution`
  proxies, `SpanEdge::min_closed`, both segmentation-DP bounds, the
  best-first `bound` closure, and a new `prune_partials` retention order,
  plus `examples/measure.rs`, the harness the weight was chosen with.
  Nothing in the production path is target-specific.
- `git grep -i -E "wreck|beach|recognize|justice|stupid|dupe|came|hid"`
  over the branch: hits only in `src/lexical.rs` and `src/lib.rs` unit
  tests, in explanatory comments, and in the pre-existing `src/main.rs`
  doc examples. Nothing in production code.
- the merge into `post-milestone-acceptance` (which had gained the
  exact-determinism fix `7fa8dc6` since the branch point) was clean, with
  no textual conflict in `src/lib.rs` or `tests/corpus_integration.rs`.

Validation re-run on the integrated tree, not taken from the agent:

- `cargo test --release --lib` — 19 passed, 0 failed (11 before this
  merge; the 5 new `lexical` tests plus 3 new closed-class tests).
- `cargo test --release --test exact_determinism` — 1 passed.
- `cargo test --release --test corpus_integration` — 5 passed, the 2
  known acceptance regressions still failing, and the new
  `approximate_proposals_are_predominantly_content_words` passing.
- Reproduced the agent's headline measurement independently on the
  integrated tree, `--approximate --top 50`, `MADGAB_TRACE_PHRASES`:
  `wreck a nice beach` raw rank 27 at score 0.918313 against a visible
  cutoff of 0.917046; `wreck a nice each` and `it justice two bad aim`
  are gone from the candidate pool entirely. Wall clock 4.7 s and 5.9 s,
  within the baselines recorded in [w-3c8e10](w-3c8e10.md) and
  [w-6d1f45](w-6d1f45.md).

Item state: **done**. Every criterion in the item body is met on its own
terms, with two honest caveats already documented by the agent and not
papered over here:

- "`cargo test --release` is green" is not met repo-wide; the two
  approximate-mode acceptance regressions fail, they are pre-existing on
  the parent commit, and they belong to [w-4b1e07](w-4b1e07.md). Same
  reasoning as the exact-determinism front ([w-5d03af](w-5d03af.md)).
- the weight is the one judgement call the agent flagged as re-measurable;
  `examples/measure.rs` is committed so re-running it is cheap.

`hits justice dupe hid came` is still **not generated at all**, so this
item was never going to be the whole of the primary milestone. The two
remaining blockers are each owned elsewhere and both were running when
this pass ended: the selector policy ([w-3c8e10](w-3c8e10.md), which is
now the *sole* blocker for `recognize speech`) and alignment
reachability ([w-6d1f45](w-6d1f45.md), the sole blocker for the
`It's just a stupid game` case).

