---
work_item: true
id: w-6d1f45
state: done
priority: high
owner: agent-b1c2d02
updated: 2026-09-26T12:05:00Z
branch: madgab-phonetic-reach
worktree: /workspace/madgab-phonetic-reach
---

# Keep alignments the search prunes for non-diversity reasons

## Goal

`madgab --approximate` should be able to *generate* alignments that are
individually excellent but are lost before the final scorer ever sees
them. Today at least one such alignment is silently unreachable: no
retention rule in the beam, the span shortlist, the segmentation DP or
the lexical enumeration keeps it, and the itinerary's primary milestone
depends on it.

## Context

Residual of [w-4b1e07](w-4b1e07.md); independent of the scoring front
([w-9c2d51](w-9c2d51.md)) and the selector front
([w-3c8e10](w-3c8e10.md)).

Measured on `post-milestone-acceptance` at 86357a4:

- `hits justice dupe hid came` for `It's just a stupid game` is **not in
  the generated pool at all** — this is a reachability failure, not a
  ranking one. Its per-word alignment costs are hits 0.20, justice 0.00,
  dupe 0.15, hid 0.37, came 0.40, total 1.12, inside the 1.5 default
  total budget, and `acceptance_sequences_are_reachable_in_fuzzy_lattice`
  in `src/lib.rs` already proves the words are in the lattice.
- Two retention stages were each identified as dropping it, and neither
  has been proven correct:
  1. the per-target-span shortlist in `generate_approximate`
     (`SPAN_AXIS_KEEP` / `SPAN_BAND_KEEP` / `SPAN_RARITY_KEEP` /
     `SPAN_SHORTLIST` = 16/4/4/160) drops `hid` for its span;
  2. the segmentation DP then drops the span structure, so even a
     recovered word would not be enumerated.
- The hypothesis worth testing first is an **admissibility** bug rather
  than a budget bug: the structural DP prunes with a `rank`/bound that
  mixes axes whose final-score contributions are not linear, so a
  structure can be pruned although its achievable final score is high. If
  the bound is not a true lower bound on the final objective, retention
  sizes are compensating for a wrong search. Establish which it is by
  measurement, not assumption.
- The general statement of the goal: for a spread of real targets, the
  top-scoring alignments in the fuzzy lattice (computed with the real
  final scorer, offline) should be present in the pool the search
  actually enumerates. That is a testable, general property and does not
  mention any particular phrase.

## Completion criteria

- The reason the alignment is lost is identified by measurement, and the
  fix is general: admissible bounds, retention rules derived from the
  objective, or equivalent. Not a bigger constant tuned until one phrase
  appears.
- A general test at the search or library boundary expresses the
  property "high-scoring lattice alignments survive into the enumerated
  pool" over at least a few real targets, or, if a full offline
  comparison is too slow for the test suite, a cheaper admissible-bound
  unit test plus a reproducible offline comparison recipe recorded here.
- The score for the recovered alignment is recorded so the ranking fronts
  know what they are working against.
- `cargo test --release --lib` and
  `cargo test --release --test corpus_integration` are green apart from
  the pre-existing acceptance regressions, and wall clock for
  `recognize speech` (5.2 s search) and `It's just a stupid game`
  (7.0 s search) does not regress. This front is explicitly allowed to
  cost a little time, but the cost must be measured and reported, since
  [w-7fa26c](w-7fa26c.md) owns the runtime budget.
- No phrase-specific hard-coding for the two canonical examples.
- Work is committed on a focused branch and integrated into
  `post-milestone-acceptance`, never into `main`.

## Handoff / notes

Not started. Relevant code: `generate_approximate` in `src/lib.rs` (the
span shortlist, `SegPath`/`SpanEdge`, `SEG_STATE_KEEP`,
`SEGMENTATION_KEEP`, the lexical best-first enumeration with
`LEXICAL_COMBINATIONS_PER_SEGMENTATION` / `LEXICAL_HEAP_POP_LIMIT`), and
`prune_partials`.

Expect a merge conflict in `src/lib.rs` with the other three open fronts.
The performance front ([w-7fa26c](w-7fa26c.md)) is editing
`prune_partials` in the same file; keep the two changes separable.

`cargo fmt`, `cargo clippy` and doctests cannot run on this host. See
[../../environment-notes.md](../../environment-notes.md).

## Pass 2026-09-26T10:05Z (coordinator, evidence update for the running agent)

Antonina agent `b1c2d02` is running in this worktree and is left running.
This front is now the *sole* remaining blocker for the second canonical
case, so the notes below matter for the milestone.

The content-word front ([w-9c2d51](w-9c2d51.md)) was integrated during
this pass as **50bdda1** / **9633013** (merge of `madgab-clue-quality`
4a0bedb). Consequences for this front:

- The pool is now 15493 candidates for `It's just a stupid game` (was
  11484 at 247404f), and the visible cutoff is **0.915692** at
  `--top 50`.
- `hits justice dupe hid came` is still `missing` from that pool, so this
  is confirmed to be a reachability failure and not a ranking one. The
  score of the recovered alignment recorded in the "Context" section
  above (0.820) is **stale**: it was computed without the closed-class
  axis, and all five of its words are content words, so the new axis adds
  nothing to its penalty while subtracting up to the full 0.15 weight
  from its closed-class competitors. Re-score it on
  `post-milestone-acceptance` HEAD before reporting the figure this item
  owes the ranking fronts, and report the gap to the new cutoff, not to
  0.926.
- The item's request that the fix be admissible-bound-derived rather than
  a bigger constant is now more load-bearing than it looked. The
  clue-quality agent reported that `bound()` in the lexical enumeration is
  *already* not strictly admissible (`suf_min_reused` is an optimistic
  minimum), that this looseness is pre-existing, and that the
  strictly-admissible variant it tried was materially worse — the
  canonical `wreck a nice peach`/`wreck a nice beach` family fell out of
  the pool entirely. If your measurement concludes the bound is the
  defect, be aware that fixing admissibility in isolation regresses the
  `recognize speech` case that the selector front
  ([w-3c8e10](w-3c8e10.md)) is close to delivering. Prefer a fix that
  keeps both cases reachable, and say explicitly in the handoff what it
  costs on the `recognize speech` pool.

Wall clock re-measured on the integrated tree: 5.9 s wall / 5.8 s search
for `It's just a stupid game`, against the 7.0 s baseline in this item,
so there is headroom to spend on retention if the fix needs it.

## Pass 2026-09-26T11:40Z (coordinator coord-11b7): agent finished, branch pushed, NOT integrated

Antonina agent `b1c2d02` **succeeded** (exit 0) and pushed two commits to
`madgab-phonetic-reach`: `bc1347e` ("Prune the span DP with the final
scorer-s own weights and an admissible bound") and `2f798f0` (the item
record). The branch is **not** integrated: this pass had no budget for a
851-line `src/lib.rs` review, and the pass that reviews it must re-validate
against the tree that now also carries the search speedups (`d8e14cd`).

The headline result changes the milestone plan, so it is recorded here
rather than left in the branch. The canonical alignment is **not** a
reachability problem any more:

- all five words survive the per-span shortlist (span ranks 26, 0, 48, 113, 8);
- it is lost in the lexical enumeration, and at no affordable budget
  (`LEXICAL_BRANCH_KEEP` 10 -> 200 and
  `LEXICAL_COMBINATIONS_PER_SEGMENTATION` 64 -> 100000 both leave it missing
  at 36 s and 60 s);
- under the real final scorer it scores **0.819901** against a top-10 cutoff
  of **0.929514**.

So the remaining lever for the second canonical case is the **scorer**, and
that front is now filed as [w-04f83f](w-04f83f.md). A new Antonina agent
(`d17a620`) is running on the independent runtime continuation
[w-d17a62](w-d17a62.md) in `/workspace/madgab-alloc-sharing`.

Next action for a later fresh pass, in this order:

1. Review `madgab-phonetic-reach` (`bc1347e`, `2f798f0`) and integrate it
   into `post-milestone-acceptance`. Expect a textual conflict in
   `src/lib.rs`; the agent states it does not touch `prune_partials`, so it
   should be separable from `d8e14cd`. Its own numbers were measured at
   7fa8dc6, so re-measure the pool size and cutoff on the integrated tree
   before repeating them.
2. Integrate or supersede this item. The admissibility defect it was opened
   for is fixed and covered by three new library-boundary tests, so if the
   integration validates, this item can go to `done` with w-04f83f carrying
   the milestone.
3. Do not reopen the work-budget constants (`SPAN_SHORTLIST`, `SEG_STATE_KEEP`,
   `SEGMENTATION_KEEP`, `LEXICAL_*`, `MATCHES_PER_SPAN`) on the strength of
   the phrase being missing: the agent-s own negative results show the
   remaining limits are work budgets, and the 0.11 score gap is the real
   cause.

## Pass 2026-09-26T12:05Z (coordinator coord-3f2a): integrated, this item is done

Integrated `madgab-phonetic-reach` (`bc1347e`, `2f798f0`) into
`post-milestone-acceptance` as **9ae1f5a** and pushed both branches.
`main` untouched. The branch's own detailed handoff — the negative
results, the per-axis measurement tables and the residuals it lists —
stays on the branch at `2f798f0`; what follows is the review and the
validation on the integrated tree.

Review: the diff is 851 lines of `src/lib.rs` and is general. The two
ad-hoc DP proxies are replaced by three keys over one per-span summary
(`SpanExtremes`) computed with the final scorer's own weights, plus a
suffix relaxation over the span DAG that makes `span_score_bound` a true
upper bound on every alignment reachable through a path. It is the only
key allowed to discard a path, and it discards against the incumbent the
ordinary beam has already established. `git diff` over the branch shows
the canonical phrases only in test data
(`reachability_corpus`, `acceptance_sequences_are_reachable_in_fuzzy_lattice`)
and never in production code.

The merge was not mechanical: the branch was based on `7fa8dc6`, before
the closed-class axis landed as `50bdda1`, and the w-04f83f handoff had
predicted exactly that. Resolution:

- `SpanExtremes` carries `min_closed`, and `partial_span_score`,
  `complete_span_score` and `span_score_bound` all include the
  `axes::CLOSED_CLASS` term. Without it the keys stop shadowing the
  final scorer, and `structural_bounds_dominate_the_real_scorer` is the
  test that says so.
- `span_score_bound` divides the closed-class share by the *widest* word
  count a completion could reach. The narrowest count is right for every
  other per-word axis (a small denominator inflates their contribution),
  but the closed-class axis is subtracted, so a small denominator would
  make the bound smaller and stop it being admissible.
- The three keys now read the shared `axes::` constants instead of
  repeating the numbers, so a later weight change cannot desynchronise
  them from the scorer.
- `axes::SIMILARITY_PER_WORD_RANK` is deleted: the DP no longer has a
  separate, deliberately-heavier cost coefficient.

Validation on the integrated tree (release):

```text
cargo test --release --lib                        29 passed, 0 failed
cargo test --release --test corpus_integration     8 passed, 1 failed
  approximate_output_is_locked                          ok
  approximate_finds_recognize_speech_resegmentation    ok
  approximate_finds_classic_madgab_resegmentation      FAILED (w-04f83f)
  approximate_list_is_not_one_resegmentation           ok
  approximate_proposals_are_predominantly_content_words ok
cargo test --release --test exact_determinism      1 passed
```

`approximate_output_is_locked` passes untouched, so no re-baselining of
the behaviour lock was needed. Measured on the integrated tree with
`--approximate` and `MADGAB_TRACE_PHRASES`:

```text
It's just a stupid game:  pool 15493 -> 15710, top-50 cutoff
                           0.915692 unchanged, search 2.2 s (--top 20)
recognize speech:         pool 13251, wreck a nice beach raw rank 27
                           score 0.918313 unchanged, search 2.3 s
congratulations on your promotion: search 6.0 s (--top 20)
```

The pool grew by 217 candidates and the quality figures are identical,
which is the honest reading of "the search is now pruning for reasons
that have to do with the objective". The extra cost is one pass scoring
the completed pool (at most `final_keep` partials) plus one
`span_score_bound` call per DP edge; wall clock is inside this host's
+/-25% run-to-run spread, and [w-d17a62](w-d17a62.md) owns the runtime
budget if it ever stops being free.

Completion criteria: the reason the alignment is lost is identified by
measurement and recorded above and in the 11:40 pass; the fix is
admissible-bound-derived rather than a tuned constant; the general
property is covered by two library-boundary tests over three real
targets plus an exhaustive novelty-bound test; the score the ranking
fronts are working against is recorded; the suite is green apart from
the pre-existing acceptance regression; no phrase-specific
hard-coding; integrated into `post-milestone-acceptance`. The item is
`done`.

The milestone itself is not. `w-04f83f` is the whole of what is left,
and it is running: Antonina agent `04f83f0` in
`/workspace/madgab-clue-objective`, left running for a later pass to
inspect. Next action for a later fresh pass: rebase that front onto
`9ae1f5a` before it is reviewed, because the three DP keys it reasons
about have changed shape, and then review and integrate it on the same
discipline as here.
