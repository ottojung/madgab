---
work_item: true
id: w-4b1e07
state: working
priority: high
owner: coord-3f9a
updated: 2026-09-26T16:58:00Z
branch: post-milestone-acceptance
worktree: /workspace/madgab
---

# Deliver the canonical Mad Gab resegmentations from approximate mode

## Goal

`madgab --approximate` should propose `wreck a nice beach` for
`recognize speech` and `Hits Justice Dupe Hid Came` for
`It's just a stupid game`, as a result of general improvements to
search, segmentation, scoring and ranking — never phrase-specific
hard-coding.

## Context

The itinerary's primary goal is
[../skills/itinerary-madgab.md](../skills/itinerary-madgab.md). The two
regression tests in `tests/corpus_integration.rs`
(`approximate_finds_recognize_speech_resegmentation`,
`approximate_finds_classic_madgab_resegmentation`) have never passed on
this branch.

Measured starting point on `post-milestone-acceptance` (commit 247404f):

- both desired word sequences are already reachable in the fuzzy matcher
  lattice inside the default budgets; `acceptance_sequences_are_reachable_in_fuzzy_lattice`
  in `src/lib.rs` proves it;
- `wreck a nice beach` was generated at raw rank 662 of 14068
  candidates, score 0.8946, against a 0.9063 cutoff — a ranking problem;
- `hits justice dupe hid came` was **not generated at all**; its
  best alignment costs 1.12 (hits 0.20, justice 0.00, dupe 0.15,
  hid 0.37, came 0.40) inside the 1.5 total budget, but the word `hid`
  was pruned out of the retained shortlist for its target span, and the
  segmentation itself did not survive the structural DP;
- the visible top-50 was pathologically redundant: for
  `It's just a stupid game`, 27 of the 50 proposals shared one boundary
  structure, because `select_diverse` scaled its redundancy penalty by
  the score spread *inside the requested top-N* (a few thousandths), and
  `dedup` treated `this` and `this'` as different clues;
- wall clock was 9.5s for `recognize speech` and 21.3s for
  `It's just a stupid game`, 48.5s for
  `congratulations on your promotion`.

## Completion criteria

- Both canonical clues appear in the approximate proposal set for their
  own target, with the rank chosen by evidence rather than asserted
  arbitrarily; a regression test states the boundary that actually
  matters and the reasoning.
- The implementation contains no phrase-specific special case for these
  examples, their words, or any substring of them.
- The proposal set is not redundant: proposals for one target span
  several distinct boundary structures, covered by a general test.
- `cargo test --release` is green.
- Search wall clock does not regress against the figures above.
- Work is committed on a focused branch and integrated into
  `post-milestone-acceptance`, never into `main`.

## Handoff / notes

Branch `madgab-approx-acceptance` (commit 6250ba3) lands the first slice,
by Antonina agent `a1b2c301`:

- the score's raw-IPA-length proxy is replaced by a syllable-count
  *rhythm* axis (weight 0.30), so the search no longer wins by shredding
  the target into one- and two-phone words;
- clue identity is now orthography-independent (`phrase_signature`), so
  spelling variants of one clue collapse;
- `select_diverse` measures its penalty against the whole candidate
  pool, maximises redundancy over everything picked rather than only the
  previous pick, and adds a 0.5-weighted boundary-cut Jaccard term;
- the per-segmentation lexical enumeration became a best-first search
  over prefixes with admissible suffix bounds, so emission order really
  is descending in final score;
- the per-span shortlist gained a least-familiar-candidate axis and a
  160-word fill, and segmentation retention doubled to 256;
- beam retention is now deterministic (deduped candidates sorted by path
  key, cell keys sorted) — `HashMap` iteration order had been leaking
  into results;
- `boundary_novelty` counts by merge instead of building `HashSet`s, and
  the target-word reuse test is memoised.

Measured result: `wreck a nice beach` raw rank 662 -> 52
(score 0.9277 against a 0.9279 cutoff) and *not* in the visible top 120
either, because MMR keeps at most one clue per boundary structure and two
siblings of the same structure already outscore it; top-50 structural
redundancy is gone; search is 3-5x faster
(`congratulations on your promotion` 48.5s -> 11.5s).

Residual, and the reason this item is still `working`:

- `hits justice dupe hid came` is still absent from the retained pool.
  It scores 0.820 against a 0.926 cutoff: it loses on acoustic cost
  (-0.057), boundary novelty (-0.050) and familiarity (-0.030) and wins
  only on the two weak axes, so no monotone reweighting of the current
  axes can surface it.
- The current winners are function-word salads — `it justice two bad
  aim`, `wreck a nice each`, `so la cut are -able` — that no human would
  use as a puzzle. A closed-class / content-word prior on clue quality is
  the honest next axis, and it is expected to help both canonical cases.
  Tracked as [w-9c2d51](w-9c2d51.md).

## Pass 2026-09-26T08:50Z (coordinator coord-9b41)

Integrated. `madgab-approx-acceptance` (6250ba3) is now merged into
`post-milestone-acceptance` as **d46d154** and pushed, together with the
child branch. `main` is untouched.

Review performed before integrating:

- Read the whole 757-line diff. The changes are general: a syllable-based
  rhythm axis in `src/approx.rs`, shortlist/beam/DP changes, best-first
  lexical enumeration, a rewritten `select_diverse`, and hot-path
  allocation removal. No target-specific special case.
- `git grep -i -E "wreck|beach|recognize|justice|stupid|dupe|came|hid"
  6250ba3 -- src/` returns hits only in unit tests, in explanatory
  comments, and in the pre-existing `src/main.rs` doc examples. Nothing in
  production code.
- Validation on the integrated tree: `cargo test --release --lib` 11/11
  pass; `cargo test --release --test corpus_integration` 4 pass, the 2
  known acceptance regressions fail. Nothing new broke.

Environment caveat for whoever picks this up next: `cargo fmt`,
`cargo clippy` and doctests cannot run on this host at all. See
[../../environment-notes.md](../../environment-notes.md). Do not report
those criteria as satisfied.

The residual is unchanged and is the whole of what remains for the primary
milestone: the two acceptance regressions. The clue-quality axis
([w-9c2d51](w-9c2d51.md)) is now under way in a separate worktree, and the
performance item ([w-7fa26c](w-7fa26c.md)) is running in parallel. Next
action for a later pass: read the clue-quality agent's result, and if the
closed-class axis alone does not surface `hits justice dupe hid came`,
reopen this item and re-measure the gap on top of d46d154 before choosing
the next axis.

## Pass 2026-09-26T10:05Z (coordinator coord-7e04)

Read the clue-quality agent's result, as the previous pass's next action
asked, and integrated it. The item stays `working`: the closed-class axis
alone did not surface the second canonical clue, exactly the branch the
previous pass predicted, so this item is re-measured here rather than
closed.

Integrated: `madgab-clue-quality` (4a0bedb) merged into
`post-milestone-acceptance` as **50bdda1**, plus **9633013** (two cosmetic
comment fixes). `main` untouched. Review and full validation detail are in
[w-9c2d51](w-9c2d51.md); in short: the diff is a general closed-class
word-class axis with no target-specific case, `cargo test --release --lib`
is 19/19, `exact_determinism` 1/1, `corpus_integration` 5 pass with the
2 known acceptance regressions still failing.

Re-measured on the integrated tree, `--approximate --top 50`,
`MADGAB_TRACE_PHRASES`:

```text
recognize speech:
  raw phrase="wreck a nice beach" rank=27 score=0.918313383
  raw phrase="wreck a nice each"  missing (13209 candidates)
  raw_cutoff rank=49 score=0.917045726
  wall 4.7s

It's just a stupid game:
  raw phrase="hits justice dupe hid came" missing (15493 candidates)
  raw phrase="it justice two bad aim" missing (15493 candidates)
  raw_cutoff rank=49 score=0.915691888
  wall 5.9s
```

So the residual has split cleanly into two *separate*, individually
sufficient blockers, one per canonical case, and neither is the score any
more:

1. `recognize speech` -> `wreck a nice beach` is **in the retained pool,
   above the visible cutoff**, at raw rank 27. The only thing that keeps
   it out of the visible list is `select_diverse`'s redundancy penalty.
   Its old competitor inside the same boundary structure
   (`wreck a nice each`, formerly raw rank 7) is gone from the pool
   entirely, so the selector front is now the whole of the remaining gap
   here. Owned by [w-3c8e10](w-3c8e10.md), agent `b1c2d01` running.
2. `It's just a stupid game` -> `hits justice dupe hid came` is still not
   generated at all, so nothing downstream of the pool can help it. Owned
   by [w-6d1f45](w-6d1f45.md), agent `b1c2d02` running.

Wall clock is a side effect of both fronts landing and is at 4.7 s and
5.9 s, better than the 5.2 s and 7.0 s baselines those items record.

The runtime front ([w-7fa26c](w-7fa26c.md), agent `a1b2c304` running) is
untouched by this integration; its notes now point at 9633013 as the
comparison base instead of `d46d154`.

All three agents were left running. Next action for a later fresh pass:
check the three fronts' branches and agents, integrate whichever of
[w-3c8e10](w-3c8e10.md) and [w-6d1f45](w-6d1f45.md) lands first with the
same review discipline used here, and re-run the milestone check after
each. Do not close this item until both regression tests in
`tests/corpus_integration.rs` pass on `post-milestone-acceptance` with no
phrase-specific case in `src/`.

## Pass 2026-09-26T10:45Z (coordinator coord-7e04): first acceptance test now passes

Integrated `madgab-selection-diversity` (71ebbe6, 6720bde) into
`post-milestone-acceptance` as **98c7d3b** and pushed. `main` untouched.
Review detail and the merge-conflict resolution are in
[w-3c8e10](w-3c8e10.md).

That closes blocker 1 above. Measured on the integrated tree:

```text
cargo test --release --lib                    21 passed, 0 failed
cargo test --release --test corpus_integration  7 passed, 1 failed
  approximate_finds_recognize_speech_resegmentation  ok   (was failing)
  approximate_list_is_not_one_resegmentation         ok   (new, this front)
  approximate_proposals_are_predominantly_content_words ok
  approximate_finds_classic_madgab_resegmentation     FAILED (still)
```

`wreck a nice beach` is visible for `recognize speech` (slot 46) and the
new structural-diversity test passes on five real targets, so the win is
not confined to the acceptance phrase.

Blocker 2 stands unchanged and is the whole of what remains:
`hits justice dupe hid came` is still not generated. The visible list for
`It's just a stupid game` is still function-word salad
(`it justice too bad aim`, `eat justice too bad aim`, ...), so the
content-word axis is not reaching this target. That is the search/scorer
front, [w-6d1f45](w-6d1f45.md), agent `b1c2d02` running in
`/workspace/madgab-phonetic-reach`.

Next action for a later fresh pass: integrate whichever of [w-6d1f45](w-6d1f45.md)
and [w-7fa26c](w-7fa26c.md) lands first — the runtime branch already has
`succeeded` agent `a1b2c304` behind commit 0ed6ca2 ("Speed up approximate
search without changing its output", a `prune_partials` fix in
`src/approx.rs`, 1.25-2.1x) and has not been reviewed or integrated yet.
Re-run `cargo test --release --test corpus_integration` after each. Do not
close this item until both acceptance tests pass with no phrase-specific
case in `src/`.

## Pass 2026-09-26T11:50Z (coordinator coord-11b7): runtime front landed; the last blocker is now the scorer

Integrated the runtime front ([w-7fa26c](w-7fa26c.md)) as **d8e14cd**;
review and validation detail are in that item. On the integrated tree:

```text
cargo test --release --lib                       26 passed, 0 failed
cargo test --release --test corpus_integration   8 passed, 1 failed
cargo test --release --test exact_determinism    1 passed
  approximate_finds_recognize_speech_resegmentation  ok
  approximate_finds_classic_madgab_resegmentation    FAILED (still)
```

`--approximate --top 20 "It-s just a stupid game"` is 2031 ms of search on
this branch, against the 21.3 s figure in this item-s context section, so
the runtime criterion has a lot of headroom again.

Blocker 2 has changed owner and changed kind. The reachability front
([w-6d1f45](w-6d1f45.md), agent `b1c2d02`, now **succeeded**) proved the
canonical alignment is not lost to a retention rule: all five words survive
the per-span shortlist, the lexical enumeration only misses it at
prohibitive cost, and under the real final scorer it scores **0.819901**
against a top-10 cutoff of **0.929514**. The lever is the objective, so the
scorer front is filed as [w-04f83f](w-04f83f.md) and is the milestone-s next
action.

This item stays `working`. It closes when
`approximate_finds_classic_madgab_resegmentation` passes on
`post-milestone-acceptance` with no phrase-specific case in `src/`, which is
now entirely w-04f83f-s to deliver, plus the review and integration of the
two branches that are currently pushed and pending (w-6d1f45-s
`madgab-phonetic-reach`, and w-a02d28-s decision on the two red tests).

## Pass 2026-09-26T12:05Z (coordinator coord-3f2a): the reach front is integrated; the milestone is one agent away

Integrated the reachability front ([w-6d1f45](w-6d1f45.md),
`madgab-phonetic-reach`) into `post-milestone-acceptance` as
**9ae1f5a** and pushed. `main` untouched. Review, the closed-class
resolution the merge needed, and full validation are in that item; the
short version is `cargo test --release --lib` 29/29,
`--test corpus_integration` 8 passed with only
`approximate_finds_classic_madgab_resegmentation` failing,
`--test exact_determinism` 1/1, and `approximate_output_is_locked`
unchanged.

Measured on the integrated tree:

```text
recognize speech:
  raw phrase="wreck a nice beach" rank=27 score=0.918313383
  raw_cutoff rank=49 score=0.917045726
  pool 13251, search 2.3 s (--top 20)

It's just a stupid game:
  raw phrase="hits justice dupe hid came" missing (15710 candidates)
  raw_cutoff rank=49 score=0.915691888
  pool 15710, search 2.2 s (--top 20)
```

So this item's blocker list is now one entry long and it is not a
search problem any more: the first canonical clue is generated and
visible, the second is reachable in the lattice, survives the per-span
shortlist, and is lost to the objective at 0.819901 against a top-10
cutoff of 0.929514. That is entirely [w-04f83f](w-04f83f.md), whose
agent `04f83f0` is running in `/workspace/madgab-clue-objective` and is
left running.

The runtime front ([w-d17a62](w-d17a62.md), agent `d17a620` in
`/workspace/madgab-alloc-sharing`) is independent of all of this and is
also left running. Its base is now one commit behind; if it reports
"output changed", compare against `9ae1f5a`, not `d8e14cd`, because the
span-path keys are a search change rather than a constant-factor one.

Next action for a later fresh pass: rebase `madgab-clue-objective` onto
`9ae1f5a` (its `SpanExtremes`-based keys are now the thing any scorer
change has to keep in step, and `structural_bounds_dominate_the_real_
scorer` is the test that enforces it), review it with the same
discipline as the reach front, and integrate it. Then this item closes,
together with [w-a02d28](w-a02d28.md), which closes on the same test
going green. Do not close either before
`approximate_finds_classic_madgab_resegmentation` passes on
`post-milestone-acceptance` with no phrase-specific case in `src/`.

## Pass 2026-09-26T12:40Z (coordinator coord-5e1d): reconciliation only, two fronts left running

No source change on this pass and nothing to review or integrate: both
integration candidates are still uncommitted inside their worktrees, so
there is no finished work to land. What this pass did:

- confirmed `post-milestone-acceptance` is at `45419a7` and pushed, and
  that `main` is untouched;
- confirmed both Antonina agents are alive and working, not hung:
  `04f83f0` in `/workspace/madgab-clue-objective` is sweeping objective
  weightings and still measures the canonical alignment short of the
  visible cutoff (`hits justice dupe hid came` rank 482, score 0.8800
  against a top-50 cutoff of 0.9400 in its latest `W0.30` sweep), and
  `d17a620` in `/workspace/madgab-alloc-sharing` has moved from reading
  to phase-timing instrumentation. Both were left running and both were
  prompted with the facts they need to land cleanly (base to compare
  against, and the instruction that temporary instrumentation and the
  `zz_dump_canon` survey test must not reach the accumulation branch);
- fixed durable state that had drifted: [w-d17a62](w-d17a62.md) said
  `open`/unowned while agent `d17a620` had been running against it for
  20 minutes, and is now claimed with its branch and worktree;
  [w-a02d28](w-a02d28.md) is now `blocked` with an objective unblock
  condition instead of `open`, so no later pass starts a duplicate of the
  scorer front; and the older continuation document
  [../continuation-approximate-search.md](../continuation-approximate-search.md)
  (`w-7c4a91`) is now `superseded` by this item, which has carried the
  same acceptance goal plus the live measurements since 2026-09-26T08:50Z;
- opened one new, deliberately non-contending front,
  [w-3d6c81](w-3d6c81.md), on a property the two running fronts could
  break silently: approximate-mode run-to-run determinism and `top_n`
  stability, asserted in `tests/` only, because both running agents have
  uncommitted `src/lib.rs` edits right now.

Nothing about the blocker changed: this item still closes when
`approximate_finds_classic_madgab_resegmentation` passes on
`post-milestone-acceptance` with no phrase-specific case in `src/`, which
is [w-04f83f](w-04f83f.md) to deliver and a coordinator to review and
integrate.

Next action for a later fresh pass, in priority order:

1. Check agent `04f83f0`. If it has finished, rebase
   `madgab-clue-objective` onto the current `post-milestone-acceptance`
   head, review the diff with the reach front's discipline (general axes
   only, no `zz_dump_canon`, no phrase-specific case), run
   `cargo test --release --lib` and `--test corpus_integration`, and
   integrate. That is the milestone.
2. Check agent `d17a620` the same way; its change must be
   behaviour-preserving, so compare output against the current
   accumulation head and not against its own stale base.
3. Land [w-3d6c81](w-3d6c81.md) whenever its agent reports, expecting a
   rebase because the scorer front will move the axes it observes.
4. Close this item and [w-a02d28](w-a02d28.md) together once the
   acceptance test passes on its own merits.

## Pass 2026-09-26T13:50Z (coordinator coord-5e1d): determinism front landed; the last blocker is now disputed

Integrated [w-3d6c81](w-3d6c81.md) as **76c693c** (agent `3d6c810`,
commit `0f317bc`, `tests/approx_determinism.rs` only, `src/` untouched)
and pushed both branches. The only merge conflict was that item's own
file. `cargo test --release --test approx_determinism` is 2/2 on the
integrated tree (~46s). `main` untouched. That front's validation of the
rest of the suite: `--lib` 29/29, `--test exact_determinism` 1/1,
`--test corpus_integration` 8 passed / 1 failed.

The real result of this pass is a **contradiction between two fronts
about the same clue on the same head**, which is more useful than either
measurement alone:

- [w-04f83f](w-04f83f.md), the scorer front, has been measuring for half
  an hour that all five canonical words survive the per-span shortlist
  and that the alignment is lost to the objective — 0.819901 against a
  0.929514 cutoff — and has been sweeping axis weightings accordingly.
- The determinism front, which could not touch `src/` and therefore
  measured the *real* release binary instead, finds the phrase absent
  from the **deduplicated candidate pool** even at `--top 2000`, and
  that `dupe` and `hid` never occur in any visible list at all while
  `hits` and `came` do.

No reweighting can surface a word the matcher never returns, so at most
one of these is right, and the second is the cheaper explanation. The
most likely reconciliation is that the scorer front's reachability
evidence came from a harness with a wider budget than the default path
— which is exactly the mistake its own item warned about, so the
contradiction is a warning about evidence quality rather than a reason
to discard the front.

Actions taken:

- opened [w-7b41d2](w-7b41d2.md) to settle the dispute by measurement,
  claimed it, and assigned agent `e5a71b` in
  `/workspace/madgab-canon-reach` (branch `madgab-canon-reach`, clean
  tree from `76c693c`, `src/` writable). Its first deliverable is
  diagnosis — the exact point in `FuzzyLexicon::matches_at` under the
  *default* configuration where `dupe` and `hid` fail to enter the
  returned shortlist — plus a bisect against `9ae1f5a`, which may show
  the reach front itself is what stopped them being returned.
- steered the running `04f83f0` with the conflicting measurement, the
  reasoning above, and a request to re-verify reachability against the
  default configuration before spending more turns on weightings. It
  acknowledged and began re-checking within seconds. Left running.

This item stays `working`, and its blocker is now correctly described:
not "one agent away" but **one disputed measurement away**. The first
canonical case is still generated and visible; the second is claimed by
two fronts to fail for two different, mutually exclusive reasons, and
until that is settled no scorer change can be reviewed on its merits.

Next action for a later fresh pass, in priority order:

1. Read [w-7b41d2](w-7b41d2.md) and agent `e5a71b`. Its diagnosis is the
   critical path; treat a decisive "reachability, here is the line" as
   the answer and route the fix to whichever front owns `src/approx.rs`
   without waiting for the scorer sweep to finish.
2. Read `04f83f0`'s re-verification. If it retracts the reachability
   claim, close w-04f83f as superseded by w-7b41d2 rather than
   integrating a weighting that cannot work.
3. `d17a620` (w-d17a62) is independent of the dispute and should be
   reviewed against the current head when it reports.
4. Close this item and [w-a02d28](w-a02d28.md) together once
   `approximate_finds_classic_madgab_resegmentation` passes on
   `post-milestone-acceptance` on its own merits, with no phrase-specific
   case in `src/`.


## Pass 2026-09-26T14:10Z (coordinator coord-6f0a): the dispute is settled, by measurement

The blocker on this item is no longer disputed. The coordinator re-ran the
release binary built from the current head `434312e` on the **default**
approximate path:

```text
$ MADGAB_TRACE_PHRASES="hits justice dupe hid came" \
    ./target/release/madgab --approximate --top 200 "It's just a stupid game"
MADGAB_TRACE raw phrase="hits justice dupe hid came" missing candidates=17405
MADGAB_TRACE raw_cutoff rank=199 score=0.906262887 phrase="it justice too uhh dame"
(corpus loaded in 499ms; search 3007ms)
```

The canonical alignment is absent from the **deduplicated candidate pool**, not
merely from the visible list, exactly as [w-3d6c81](w-3d6c81.md) reported.
That falsifies [w-04f83f](w-04f83f.md)'s premise — that the five words
survive the shortlist and the clue is lost to the objective at 0.819901
against 0.929514 — because a candidate that is never enumerated cannot be
reweighted into the pool. So the last blocker is **lexical reachability inside
`FuzzyLexicon::matches_at`**, it is owned by [w-7b41d2](w-7b41d2.md), and no
scoring change can be reviewed on its merits until that is fixed. This item's
blocker list is unchanged in count and changed in kind.

Three agents were live at the start of this pass and all three were left
running, each steered with the facts above:

- `e5a71b` ([w-7b41d2](w-7b41d2.md), the critical path) — told that
  `git bisect` on this tree is untrustworthy without a forced rebuild,
  because the boundary commits are docs-only and cargo reuses the previous
  binary (it had already drawn a "first bad commit is a docs commit"
  conclusion from exactly that), and asked for the exact rejection point —
  line, span, and the value of the quantity that rejects `dupe` — ahead of any
  fix.
- `04f83f0` ([w-04f83f](w-04f83f.md)) — told to stop sweeping weights, to
  record that its premise does not hold on the default path, and to land the
  general defect it *did* find (the diversity policy and the
  structural-diversity test re-deriving resegmentations by summing clue-word
  IPA lengths instead of reading `madgab::Clue::cuts`) as a commit that
  explicitly does not make the acceptance test pass.
- `d17a620` ([w-d17a62](w-d17a62.md), independent of the milestone) —
  answered the steer within seconds: committed and rebased onto the current
  head as **22212b5** "Reuse span-path allocations across candidate scoring"
  on `madgab-alloc-sharing`, working tree clean, previously a 271-insertion
  uncommitted `src/lib.rs` diff that was one reboot away from being lost.
  It has **not** been pushed yet and its branch still needs the push, plus
  the verification and item update listed there. Not integrated on this pass:
  an unreviewed, unpushed, self-rebased commit from a still-running agent
  should be reviewed as a diff on the next pass, not merged blind.

No source change on this pass and nothing integrated. `main` untouched;
`post-milestone-acceptance` stays the only accumulation branch.

Next action for a later fresh pass, in priority order:

1. `e5a71b`. Its diagnosis is the milestone. If it reports "reachability,
   here is the line", review and route the fix; do not wait for the scorer
   front, which is now narrowed to an unrelated, independently valuable fix.
2. `04f83f0`'s narrowed `cuts` fix on `madgab-clue-objective`: review it as a
   diff for generality (no phrase-specific case, no re-baselining of the
   acceptance test, no `zz_dump_canon`), re-run `--lib`,
   `--test corpus_integration`, `--test exact_determinism` and
   `--test approx_determinism`, and integrate. The re-baselined output lock
   is expected and is not by itself a reason to reject.
3. `d17a620`: confirm `madgab-alloc-sharing` is pushed, check no `axes::*`
   constant moved and no phase-timing instrumentation survived, then review
   and integrate. Behaviour-preserving, so it must not be allowed to delay
   items 1 and 2.
4. Close this item and [w-a02d28](w-a02d28.md) together once
   `approximate_finds_classic_madgab_resegmentation` passes on
   `post-milestone-acceptance` on its own merits, with no phrase-specific
   case in `src/`.

## Pass 2026-09-26T15:10Z (coordinator coord-8b31): 3d0130a integrated, both running fronts steered, one new item filed

This pass changed the accumulation branch for the first time in two
passes, and it took the item the previous pass listed as its second
priority off the board.

**Integrated and pushed: `3d0130a`** (merge of `madgab-clue-objective`,
carrying `adfc7c4` and `79014de`). `adfc7c4` makes `Clue` carry the
target-stream offsets the search aligned at, as `Clue::cuts`, and makes
`select_diverse` and the structural-diversity test read them instead of
re-deriving offsets by summing clue words' own IPA lengths. The review
verdict and the validation it was given are in
[w-04f83f](w-04f83f.md)'s 15:00Z pass; in short, it is general, moves no
weight, and its re-baselined `approximate_output_is_locked` was
re-measured on the merged tree rather than copied forward.

That commit also closed a durability hazard: agent `04f83f0` had
finished on a **detached HEAD**, so its two commits were unreferenced by
any branch. `madgab-clue-objective` is now fast-forwarded to `79014de`
and pushed, with `4a0bedb` verified as an ancestor so nothing was
dropped. A finished agent's work on a detached head is unrecoverable
from a later pass's point of view, and the general lesson for whoever
coordinates: check `git branch --contains` for a finished agent's HEAD
before treating its branch as safe.

**Suite state on `3d0130a`:** `--lib` 30/30, `--test exact_determinism`
1/1, `--test approx_determinism` 2/2, `--test corpus_integration` 8
passed / 1 failed. The single failure is
`approximate_finds_classic_madgab_resegmentation` — this item's target,
owned by [w-7b41d2](w-7b41d2.md), whose top-50 for that target is still
`it justice too <X> aim` wordings. So this item is **one integration away
from done** and no further.

**The blocker has been localised, by two independent measurements.** Both
`04f83f0`'s and `e5a71b`'s work says the same thing about the same clue:
the structural DP keeps the canonical segmentation (rank 151 of 256) and
all five words survive the per-span shortlist (ranks 22, 0, 64, 85, 7 of
160), but `LEXICAL_BRANCH_KEEP = 10` truncates every slot to its ten
best alternatives before enumeration, so `dupe` and `hid` are never
expanded. The apparent contradiction between that and `e5a71b`'s beam
probe is very likely an instrumentation-ordering artefact — its own log
shows `in=0` because `prune_partials` consumed `beam[p]` first — and the
agent has been asked to settle it by line and by ordering rather than to
leave two of this repository's documents disagreeing.

**Filed: [w-2e5b93](w-2e5b93.md)**, open, from the measurement
`04f83f0` recorded while falsifying its own premise. Over the 2000 best
candidates of ten real targets, `RHYTHM` and `WORD_NOVELTY` have a range
of exactly 0.0000 on 8 of 10, and a large share of the score is constant
across the population the score is supposed to rank. That is
scorer-sensitivity work with real general value, and it is explicitly
*not* to be started while `w-7b41d2` is still on the critical path,
because moving weights now would re-baseline the output lock for no
measurable gain.

**Both running agents were steered, non-blocking, and left running:**

- `e5a71b` ([w-7b41d2](w-7b41d2.md), critical path) — given the
  `LEXICAL_BRANCH_KEEP` measurement with a pointer to the item section
  holding the full table, told to reconcile its own probe against it,
  told to report what the cap exists to bound (memory, time, or pool
  size) so a fix replaces the reason rather than deleting the cap, told
  to prefer budget-derived retention over a raised constant, told to
  rebase onto `3d0130a` before final validation because `Clue::cuts`
  changed the shape of the code it is editing, and reminded of the ban on
  phrase-specific special cases.
- `d17a620` ([w-d17a62](w-d17a62.md)) — its `36c7b99` is pushed and its
  rebase verified, but its bit-identity and wall-clock claims were
  measured against a base that `3d0130a` has superseded, since that
  commit changes which proposals are selected. Asked to re-measure
  against the current head; if output is *not* bit-identical there, the
  reuse is entangled with selection order and that is a finding, not a
  nuisance. Given an explicit collision boundary away from
  `LEXICAL_BRANCH_KEEP` and the diversity policy.

Next action for a later fresh pass, in priority order:

1. `e5a71b`. Still the milestone and still the only thing that closes
   this item. Review the fix as a diff for generality — the cap's purpose
   must still be served — then run the full suite and integrate.
2. `d17a620`. Review `madgab-alloc-sharing` as a rebased diff: confirm
   no `axes::*` constant moved, no phase-timing instrumentation
   survived, and that the re-measured bit-identity claim against
   `3d0130a` holds. Behaviour-preserving work must not delay item 1.
3. Once `approximate_finds_classic_madgab_resegmentation` passes on
   `post-milestone-acceptance` on its own merits, with no phrase-specific
   case in `src/`, close this item and [w-a02d28](w-a02d28.md) together
   and re-check the itinerary's milestone conditions, including that the
   work is on `post-milestone-acceptance` and not on `main`.
4. Only then start [w-2e5b93](w-2e5b93.md).

## Pass 2026-09-26T15:20Z (coordinator coord-9c17): the last blocker is enumeration only, now measured

No source change on this pass and nothing integrated. `post-milestone-acceptance`
is at `e44269a` and pushed; `main` untouched. Suite state re-measured on
that head by the coordinator, not copied forward:

```text
cargo test --release --test corpus_integration   8 passed, 1 failed
  approximate_finds_classic_madgab_resegmentation  FAILED (still)
  the visible list for its target is still "it justice too <X> aim" wordings
```

The useful result of this pass is a measurement that removes the last
reason to doubt the standing plan. The previous passes treated
"the canonical clue scores 0.8199 against a 0.9157 visible cutoff" as
evidence that it could not surface without a scoring change. That is not
how the selector works, and the following measurement, taken on `e44269a`
through the public API with `madgab::Clue::cuts`, shows it:

```text
target                        top_n  visible  distinct_structures  max_per_structure
It's just a stupid game          50       50            6                     17
It's just a stupid game          20       20            4                      7
recognize speech                 50       50            5                     17
recognize speech                 20       20            3                      7
a whole lot of trouble           50       50            6                     15
a whole lot of trouble           20       20            5                      6
```

17 is exactly `share_cap(50, available) = 50 / STRUCTURE_FLOOR(3)` and 7 is
`share_cap(20, available)`, so the share cap is binding in every case.
Step 1 of `select_diverse` admits one representative per structure from
inside the score cutoff; because the cutoff holds only 5-6 distinct
structures it fills 5-6 slots, not all 50, so **step 2 runs and walks the
whole deduplicated pool**, admitting any candidate whose structure is
unrepresented or under its cap. The visible list is therefore not decided
by the score cutoff at all.

Consequences, which are what `e5a71b` was steered with:

- getting the canonical clue into the **pool** is sufficient. It does not
  have to outscore anything, and no axis weight needs to move — which also
  means the fix will not re-baseline `approximate_output_is_locked` and
  will not make [w-2e5b93](w-2e5b93.md)'s measurement ambiguous;
- the one question left is whether the canonical segmentation's cuts are
  one of the 6 visible structures at its 17-cap, which the agent can check
  in a minute with the same reading of `Clue::cuts`;
- the itinerary's completion criterion — the requested clue appears in the
  generated proposal set — is reachable by enumeration alone, so this item
  stays **one integration away from done** and no further.

Both running agents were left running:

- `e5a71b` ([w-7b41d2](w-7b41d2.md), critical path) — steered with the
  table above, with the instruction not to touch `axes::` weights, with the
  remaining cap question, and with the cost model: the recovery
  enumeration pushes ~8.9M heap branches per search, and `d17a620`'s
  unreviewed branch makes that same enumeration ~39% cheaper, so a wider
  fan-out has a concrete offsetting win to cite — while not assuming it is
  integrated.
- `d17a620` ([w-d17a62](w-d17a62.md)) — nudged for durability only: its
  rebased `889923e` was unpushed and is now pushed, and it is finishing the
  bit-identity re-measurement against `3d0130a`.

Next action for a later fresh pass, in priority order:

1. `e5a71b`. Review the enumeration fix as a diff for generality — the cap's
   stated purpose must still be served, no phrase-specific case, no
   `zz_dump_canon` — then run the full suite and integrate. That is the
   milestone.
2. `d17a620`. Review `madgab-alloc-sharing` (`889923e`) as a rebased diff:
   no `axes::*` constant moved, no phase-timing instrumentation survived,
   and the re-measured bit-identity claim against `3d0130a` holds. It must
   not delay item 1.
3. Once the acceptance test passes, close this item and
   [w-a02d28](w-a02d28.md) together, then start [w-2e5b93](w-2e5b93.md),
   whose gate ("not while `w-7b41d2` is delivering") is then lifted.

## Pass 2026-09-26T15:50Z (coordinator coord-4e2a): 7faa74c integrated; the blocker is named precisely

**Integrated and pushed: `7faa74c`**, the merge of
`madgab-alloc-sharing` (`889923e`, agent `d17a620`, item
[w-d17a62](w-d17a62.md)). This is the second priority of the previous pass,
done first this pass precisely because it buys the budget the first priority
needs. `main` untouched; `post-milestone-acceptance` remains the only
accumulation branch.

Review, done on the diff rather than on the agent's report:

- 276 insertions / 68 deletions, `src/lib.rs` only; `src/approx.rs` untouched.
- Mechanism: `Rc<[usize]>` span-path prefixes in the lexical best-first heap
  (so a branch is one allocation instead of two and the heap's pop order and
  the `seen` membership test are unchanged), a persistent `WordNode` clue-word
  list for `Partial` with a `WordIter` that yields clue order and terminates,
  `cuts` behind one `Rc`, and in-place path-key construction replacing the
  `format!` pair. `FxHasher` on the `seen` set.
- Nothing ranking-related moved: a scan of the whole diff for `axes::`,
  `MADGAB_*`, `eprintln!`, phase/timing and the retention constants
  (`LEXICAL_BRANCH_KEEP`, `SPAN_SHORTLIST`, `SEGMENTATION_KEEP`) returns no
  changed line. That is the property that mattered, because this item's
  blocker is an enumeration-budget problem.
- New unit test `shared_word_list_yields_clue_order_and_ends` covers both the
  ordering and the "walked twice must not restart" hazard the agent flagged.

Validation run by this coordinator on the merged tree:

```text
cargo test --release --lib                       31 passed, 0 failed
cargo test --release --test corpus_integration    8 passed, 1 failed
  approximate_finds_recognize_speech_resegmentation  ok
  approximate_finds_classic_madgab_resegmentation    FAILED (still)
cargo test --release --test approx_determinism    2 passed
cargo test --release --test exact_determinism     1 passed
```

**`e5a71b` produced the pass's most useful result, and it is a negative one.**
Its cost sweep says the obvious fix is not available: dropping the per-slot cap
entirely takes 135 s against a 2.0 s budget, and an incumbent-bar variant
takes 35 s and still misses. The general finding it distilled is the real
blocker, and it is worth stating in the item's own words because it retires the
whole family of "just widen it" fixes:

> a best-first enumeration with a per-slot cap can only ever find wordings
> that are near-optimal *at every slot simultaneously*; the canonical is
> 3rd/2nd/10th/99th/11th, so it needs the fan-out to reach rank 99 at slot 3,
> and for a 160-wide shortlist that is 160^5 combinations.

So the last blocker is not "the cap is too small", it is the *shape* of a
single-segmentation best-first product enumeration. The budget the previous
passes hoped would pay for a wider fan-out now genuinely exists — the
integrated commit makes that same enumeration ~39% cheaper — but at 135 s for
full fan-out it is not enough on its own, and `e5a71b` has been told to
re-measure the curve on `7faa74c` rather than on its stale `3d0130a` base
before it claims the fix is unaffordable.

Both agents were steered non-blockingly and left running:

- `e5a71b` — told that the item-2 offset is now integrated, that its negative
  result is accepted as the finding, and that measurement-only is no longer an
  acceptable end state: the deliverable is a landed, general change that
  removes the simultaneous-near-optimality requirement while still bounding
  memory/time/pool size, with the cap's post-change purpose recorded; failing
  that, an implementation-ready specification (exact lines, budget arithmetic
  per design against `7faa74c` timings, one recommended design) so a later
  agent does not repeat the sweep. Three design families were named for it:
  sharing the enumeration budget across many segmentations instead of 64
  combinations for one; ordering each per-slot list by actual final-score
  contribution rather than the current proxy, so rank-99-of-160 words are not
  structurally excluded; and spending budget on unrepresented boundary
  structures, which the selector admits for free. Constraints restated: no
  phrase-specific case, no `axes::*` moves, no output-lock re-baselining, no
  `zz_dump_canon`, rebase onto `7faa74c` because `Partial`'s shape changed.
- `d17a620` — told its work is integrated and to stop re-measuring, to close
  [w-d17a62](w-d17a62.md) with `889923e`, `7faa74c` and the validation above,
  and not to open a new front. Its item was deliberately **not** edited by this
  pass to avoid two writers on one file; until it reports, w-d17a62 still reads
  `working`, and that is stale-but-harmless.

Nothing else was started. [w-2e5b93](w-2e5b93.md) remains deliberately gated,
because any weight move now would re-baseline the output lock while the blocker
is still enumeration shape. This item therefore stays **one landed fix away
from done**, and the count of blockers is unchanged at one.

Next action for a later fresh pass, in priority order:

1. `e5a71b` on `7faa74c`. If it landed a fix, review it as a diff for
   generality (the cap must still bound something, no phrase-specific case, no
   `zz_dump_canon`), run the suite above, and integrate. If it returned only
   the specification, either launch a fresh agent on that specification or
   claim it yourself; do not let the milestone idle behind a spent agent.
2. `d17a620`: confirm w-d17a62 is `done` and its branch is pushed. If it is
   still `working` with a clean tree and a finished job, close the item rather
   than waiting again.
3. Once `approximate_finds_classic_madgab_resegmentation` passes on
   `post-milestone-acceptance` on its own merits, with no phrase-specific case
   in `src/`, close this item and [w-a02d28](w-a02d28.md) together, re-check
   the itinerary's milestone conditions including that the work is not on
   `main`, and only then start [w-2e5b93](w-2e5b93.md).

## Pass 2026-09-26T16:20Z (coordinator coord-3d7a): second enumeration front opened; blocker is unchanged

No source change on this pass and nothing integrated, because there is
nothing finished to integrate: `w-d17a62` is now genuinely `done` and
integrated, and the only live source work is
[w-7b41d2](w-7b41d2.md)'s agent, which is still measuring. This item is
re-claimed from `coord-4e2a` to `coord-3d7a` and its blocker is unchanged
in count: **one** blocker, `approximate_finds_classic_madgab_resegmentation`.

State confirmed on arrival rather than assumed:

- `post-milestone-acceptance` is at `c191eaf` and pushed; `main` untouched.
  The only commits since the previous pass's measurement at `745295a` are
  `bceef9a` and `c191eaf`, which are docs-only, so the previous pass's
  suite figures and cost curve still describe this tree.
- `madgab-alloc-sharing` is pushed, clean, and `889923e` is an ancestor of
  the accumulation head, so the durability hazard from the previous passes
  is closed.
- `e5a71b` ([w-7b41d2](w-7b41d2.md)) is alive on `madgab-canon-reach`,
  rebased onto `7faa74c`, and mid-sweep. Its re-measurement on the
  integrated head confirms the previous pass's arithmetic and closes the
  last "just widen it" door: full fan-out is 17.2 s (down from 37.9 s on
  `3d0130a`, so the ~39% allocation win is real), and even with the
  emission budget at 4096 per segmentation the pool reaches 26993
  candidates in 17.6 s **without** the canonical clue. The best-first order
  plus the pop limit never reaches it, which is the same conclusion the
  previous pass reached from the other direction.

**New front: [w-6b2f04](w-6b2f04.md)**, claimed and assigned to agent
`6b2f04` in `/workspace/madgab-enum-portfolio` (branch
`madgab-enum-portfolio`, from `c191eaf`). It is deliberately *not* a
duplicate of `e5a71b`: `e5a71b` owns the shape of the product search
*within one segmentation*, and the new front owns the *global* budget
across all retained segmentations and how it is steered toward
under-represented boundary structures. That split is motivated by
measurement rather than by convenience — the search currently pays a full
fan-out budget up to 256 times over, while `select_diverse` can only ever
show 5-6 distinct structures, so most of that budget is spent on wordings
that compete for the same structure slots. Two mechanisms, one milestone;
if only one lands, the milestone still moves.

`e5a71b` was steered non-blockingly and left running: its negative cost
result is accepted, it must not re-run the sweep this pass has now
extended, and its deliverable is a landed general change or an
implementation-ready specification — not a third round of measurement. It
is warned that the pop-limit dimension it is about to measure is
unlikely to be the answer, given the 4096-emission run.

Next action for a later fresh pass, in priority order:

1. `e5a71b` and `6b2f04`. Review whichever lands first as a diff for
   generality — a bound must still be bounded, no phrase-specific case, no
   `axes::*` move, no `zz_dump_canon` — then run the four suites above and
   integrate onto `post-milestone-acceptance`, never `main`. If one front
   is still measuring while the other has landed, integrate the landed one
   and do not wait.
2. Check both agents' worktrees are on a branch, not a detached HEAD,
   before treating either as safe.
3. Only after the acceptance test passes on its own merits, close this item
   and [w-a02d28](w-a02d28.md) together, and then start
   [w-2e5b93](w-2e5b93.md), whose per-axis spread table must be re-taken
   on the settled pool.

### Objective confirmation on the pass's own head (`ac72f77`)

Re-measured by this coordinator on the pushed head rather than carried
forward, because the milestone claim in this item should never rest on a
copied number:

```text
cargo test --release --test corpus_integration approximate_finds
  approximate_finds_recognize_speech_resegmentation  ok
  approximate_finds_classic_madgab_resegmentation    FAILED (still)
  visible list for its target is still "it justice too <X> aim" wordings
  (2.43s for the two tests)
```

So the first canonical case is generated and visible, and the second is
absent from the pool exactly as both live fronts report. `madgab-alloc-sharing`
is pushed as a child branch and `889923e` is an ancestor of this head.

## Pass 2026-09-26T11:52Z (coordinator coord-8f52): both fronts steered, and the blocker restated as "the cap is uniform"

No source change on this pass and nothing integrated: both live fronts are
still measuring, so there is no finished work to land. This item is
re-claimed from `coord-3d7a` to `coord-8f52` and its blocker count is
unchanged at **one**,
`approximate_finds_classic_madgab_resegmentation`.

State confirmed on arrival rather than assumed:

- `post-milestone-acceptance` is at `ac58702` and pushed; `origin` matches
  the local branch exactly, so nothing is unpushed. There is no `main`
  branch on this host at all, local or remote, so the "never merge
  scheduled work into `main`" condition holds trivially and the
  accumulation branch is unambiguous.
- Both working fronts are on a *named branch*, not a detached HEAD — the
  durability hazard called out in the 15:10Z pass is not present on either
  (`madgab-canon-reach` at `7faa74c`, `madgab-enum-portfolio` at
  `c191eaf`). Neither branch has unmerged commits. `madgab-canon-reach`
  does have an uncommitted instrumented `src/lib.rs` and
  `madgab-enum-portfolio` an untracked `tests/zz_scratch_analysis.rs`;
  both were told explicitly that these must not reach the accumulation
  branch.
- Suite re-measured by this coordinator on `ac58702`, not copied forward:
  `cargo test --release --lib` 31/31; `--test corpus_integration
  approximate_finds` 1 passed / 1 failed, the failure being this item's
  target, whose top-50 is still
  `["it justice too bad aim", "it justice too pad aim", ...,
  "it justice too bad came"]`. Note `came` *is* reachable, which is
  consistent with the per-slot rank table below.
- A correction to durable state: the `updated` fields in these work items
  have been running several hours ahead of real time (this pass is
  11:52Z, the previous pass's item claims 16:20Z, and the real commit
  times are 11:39Z-11:46Z). Timestamps in this repository are therefore
  not a reliable signal of recency or abandonment; use commit times and
  `antonina agent status` instead.

**The useful result of this pass is a reframing of the blocker that both
fronts can act on immediately.** The previous passes have been treating
"the per-slot cap of 10 is too small" as the blocker. The per-slot rank
table already in [w-7b41d2](w-7b41d2.md) says the canonical alignment sits
at per-slot ranks **3 / 2 / 10 / 99 / 11** over the 160-wide shortlist.
Four of the five slots are already inside a cap of 11; exactly one slot —
the `hid` slot, at rank 99 — is out of reach. So the clue does not require
simultaneous deviation across slots, which is what
[the 15:50Z pass](w-4b1e07.md) concluded the best-first product search
could not do. It requires **one slot to be allowed much deeper than the
others**, and the reason it cannot get that is that
`LEXICAL_BRANCH_KEEP` is a single constant applied uniformly to every slot
(`src/lib.rs:1089`). The uniform cap *is* the simultaneous-near-optimality
requirement; the fix is to make the cap per-slot and budget-derived rather
than to raise it.

That is also consistent with the second measurement, taken from
`e5a71b`'s own log on `7faa74c`: widening the uniform cap **shrinks** the
candidate pool, because the per-segmentation pop limit binds.

```text
fanout=10   2309ms  pool 12358
fanout=16   2900ms  pool  9017
fanout=32   4871ms  pool  6994
fanout=64   7858ms  pool  5890
fanout=160 17238ms  pool  5366
fanout=160 emit=256   16884ms  pool 10889
fanout=160 emit=4096  17644ms  pool 26993
```

So "raise `LEXICAL_BRANCH_KEEP`" is refuted by measurement rather than
merely priced out, which closes the last "just widen it" door in the
direction the earlier passes were pushing it. Because emission cost is
additive and already compared against `total_budget` at `src/lib.rs:1063`,
per-slot affordability is directly computable: with `m_j` the minimum cost
in slot `j`, slot `k` can admit candidate `i` whenever
`sum_{j != k} m_j + cost_{k,i} <= total_budget`. This is a cap derived
from the bound the search already respects, it widens precisely the slot
where budget slack exists, and it is nearly free in the common case
because most slots can only afford a handful of alternatives.

Both agents were steered with this, non-blockingly, and left running. The
separation of the two fronts was made explicit in both directions, so they
compose instead of duplicating:

- `e5a71b` ([w-7b41d2](w-7b41d2.md)) — owns the **per-slot cap shape**.
  Given the widening table, the 3/2/10/99/11 reading, and the
  affordability inequality; told explicitly *not* to implement a global
  budget and not to change `LEXICAL_COMBINATIONS_PER_SEGMENTATION` or the
  pop limit; told that if its change needs a global bound to stay
  affordable it should say so in the item and let the coordinator sequence
  the two.
- `6b2f04` ([w-6b2f04](w-6b2f04.md)) — owns the **global budget across
  segmentations**. Given the widening table, the selector measurement from
  the 15:20Z pass (the visible list is not decided by the score cutoff, so
  pool membership is sufficient and no weight needs to move), and the
  asymmetry that is its justification: 256 retained segmentations are
  funded a full emission budget each while the selector can only ever show
  5-6 distinct boundary structures. Told to quantify that asymmetry itself
  rather than assume it, told *not* to implement a per-slot cap change, and
  told that its first measurement ran in Exact mode and every number must
  be Approximate.

Nothing was started and nothing was closed. [w-2e5b93](w-2e5b93.md) remains
deliberately gated, and that gate is now sharper: with pool *membership*
established as the whole of the requirement, moving an axis weight would
re-baseline `approximate_output_is_locked` for no gain at all.

Next action for a later fresh pass, in priority order:

1. `e5a71b` and `6b2f04`. Review whichever lands first as a diff for
   generality — the cap must still bound something with named arithmetic, no
   phrase-specific case, no `axes::*` move, no sweep script or `zz_*` test
   on the branch, no re-baselined output lock — then run `--lib`,
   `--test corpus_integration`, `--test exact_determinism` and
   `--test approx_determinism` and integrate onto
   `post-milestone-acceptance`, never `main`. If one front is still
   measuring while the other has landed, integrate the landed one.
2. Check both worktrees are still on a named branch, not a detached HEAD,
   and that neither has left its instrumented or scratch files uncommitted.
3. Only after `approximate_finds_classic_madgab_resegmentation` passes on
   its own merits, close this item and [w-a02d28](w-a02d28.md) together,
   and then start [w-2e5b93](w-2e5b93.md) with its per-axis spread table
   re-taken on the settled pool.

## Pass 2026-09-26T12:15Z (coordinator coord-7a31): one front recovered and closed, one steered, one opened; the blocker is unchanged

Re-claimed from `coord-8f52`. The blocker count is unchanged at **one**,
`approximate_finds_classic_madgab_resegmentation`, and no source change was
integrated on this pass: both fronts that had finished were measurement
work, and the one live front is still mid-measurement.

State confirmed on arrival rather than assumed:

- `post-milestone-acceptance` was at `8efae0f` and fully pushed; `origin`
  matched the local branch exactly. There is still no `main` branch on this
  host, local or remote, so the "never merge scheduled work into `main`"
  condition holds trivially and the accumulation branch is unambiguous.
- Re-measured by this coordinator on the pushed head, not carried forward:
  `cargo test --release --test corpus_integration approximate_finds` is
  **1 passed / 1 failed** in 2.22 s, the failure being this item's target,
  whose visible top 50 is still
  `["it justice too bad aim", "it justice too pad aim", ...,
  "it justice too bad came"]`. `wreck a nice beach` for `recognize speech`
  is present and visible, so the first canonical example is unaffected by
  anything on this branch.
- `MADGAB_TRACE_PHRASES="hits justice dupe hid came"` on the release binary
  at `8efae0f` reports `missing candidates=15710`: the clue is absent from
  a 15710-candidate pool, so the loss is still in enumeration, exactly as
  the previous pass concluded. (The pool is larger than the 12358 recorded
  earlier; the shape of the result is what matters, and it is unchanged.)
- Both live worktrees were on *named branches*, not detached HEADs. The
  durability hazard from the 15:10Z pass is still absent.

**The per-slot cap front is finished and refuted, and is now closed.**
`e5a71b` (this item's [w-7b41d2](w-7b41d2.md)) exited 0 having implemented,
measured and reverted the budget-derived per-slot cap that the previous pass
asked for: 15-16 s, pool 5396, clue still absent, because per-word costs of
0.0-0.5 against a `total_budget` of 1.5 make total-cost affordability
vacuous. Its measurement record is durable on this branch as the merge of
`madgab-canon-reach` (`bca4f83`, docs only, `src/` clean, pushed, never
merged anywhere else), and the item is now `superseded` by
[w-6b2f04](w-6b2f04.md) with a reconciliation note, so no later pass revives a
refuted mechanism or inherits a dead owner. The previous pass's reframe of
the blocker — that the uniform cap at `src/lib.rs:1089` is the problem — is
therefore *not* the answer, and this item's record of it should be read as a
hypothesis that has now been tested and rejected, not as a pending fix.

**`6b2f04` ([w-6b2f04](w-6b2f04.md)) is the sole owner of the fix** and is
mid-implementation, not idle: its worktree holds a 89-insertion
`src/lib.rs` change that implements mechanism 2, a *structure-aware* global
budget. It names `LEXICAL_GLOBAL_POP_BUDGET` as the product of the two
constants that were already the implicit worst case, adds a
`structure_wording_allowance(top_n)` derived from the same `share_cap`
contract the display policy uses, and funds each boundary structure's
best-ranked segmentation the full allowance while later segmentations of the
same structure get what is left, with a global pop ceiling. It is
uncommitted, and its worktree also carries a modified `src/lib.rs` plus
untracked `.bench/` binaries and `tests/zz_scratch_analysis.rs`; it was told
explicitly that none of those may reach the branch and to commit the real
change early. It was steered non-blockingly with the facts it may use
without re-deriving (per-slot ranks 6-8 / 0 / 10-18 / 99 / 11, the rank-151
segmentation, the innocent matcher, the 15710 pool), with the one thing its
front must answer explicitly — what happens to a rank-151-of-256
segmentation under a global budget, with numbers, because a share rule that
starves the tail misses for the same reason the per-slot cap did — and with
the standing bars. Left running.

**New front: [w-3e5c7](w-3e5c7.md)**, claimed and assigned to agent `c4d91a`
in `/workspace/madgab-recall-audit` (branch `madgab-recall-audit`, from
`4f30fdf`). It exists because the whole milestone had come to rest on one
running agent, which is a single point of failure for the itinerary's
primary goal. It is fenced to be non-contending rather than duplicative: it
must independently reproduce the established measurements, and it may only
land a recall mechanism that is *not* the global enumeration budget — the
per-span shortlist composition, the retention/ordering of segmentations, or
pruning that removes work instead of reallocating it. Global counters,
per-segmentation shares, structure-aware allocation and shared-prefix work
are explicitly reserved to `w-6b2f04`. A refutation recorded there is an
acceptable outcome.

Nothing was started on [w-2e5b93](w-2e5b93.md) and nothing was closed on
[w-a02d28](w-a02d28.md): with pool *membership* established as the whole of
the requirement, moving an axis weight would re-baseline
`approximate_output_is_locked` for no gain, so that gate still holds.

Next action for a later fresh pass, in priority order:

1. `6b2f04` and `c4d91a`. Review whichever lands first as a diff for
   generality — the bound must still be bounded with named arithmetic, no
   phrase-specific case, no `axes::*` move, no re-baselined output lock, no
   `zz_*` or sweep file on the branch — then run `--lib`,
   `--test corpus_integration`, `--test exact_determinism` and
   `--test approx_determinism` and integrate onto
   `post-milestone-acceptance`, never `main`. If one front is still
   measuring while the other has landed, integrate the landed one and do not
   wait for the other.
2. Check both worktrees are on a named branch, not a detached HEAD, and that
   neither has left instrumentation or scratch files uncommitted.
3. Only after `approximate_finds_classic_madgab_resegmentation` passes on
   its own merits, close this item and [w-a02d28](w-a02d28.md) together,
   then start [w-2e5b93](w-2e5b93.md) with its per-axis spread table
   re-taken on the settled pool.

## Pass 2026-09-26T12:30Z (coordinator coord-4f19): the second front closes with a refutation; the first is steered, reviewed and still the only owner

This pass integrated one finished front, reviewed the live front's in-progress
diff, and made its uncommitted work recoverable. The blocker count is
unchanged at **one**, `approximate_finds_classic_madgab_resegmentation`.

**Integrated and pushed: `24f3cf7`**, the merge of `madgab-recall-audit`
(`7f08dfb`, agent `c4d91a`, item [w-3e5c7](w-3e5c7.md)). `main` untouched.
Review was on the diff: `git diff 3f53848 7f08dfb -- src tests Cargo.toml`
is **empty**, so the whole commit is one work item, `src/` is byte-identical
to the head, no test or sweep file was added, and there is nothing in it that
could be phrase-specific. The branch was already pushed by the agent; the
merge exists to carry the refutation and the item's `done` state onto the
accumulation branch, so the finding is durable for a pass that never looks at
a child branch.

The refutation is the most valuable thing this pass recorded, because it
closes the whole family of "just reach deeper in one slot" fixes with a
measurement rather than a price. Forcing the canonical alignment to index 0
of **all five** slots — the strongest possible shortlist reordering, with the
per-segmentation emission and pop caps lifted for that one segmentation —
puts it at **emission 23962, pop 28930, pool rank 28573** of its own
segmentation, against a real emission budget of 64. The same front confirmed
every established figure with no corrections (walk ranks 7/0/13/99/11,
canonical segmentation retained at rank 151 of 256, pool 15710 for the
canonical target), and resolved a long-standing confusion in this repository's
own documents: the "hid rank 85" and "hid rank 99" figures are the same word
in the `SPAN_SHORTLIST` order and the `contribution` walk order, not a
disagreement between agents.

Consequences, all of which are now steering input rather than open questions:
the binding quantity is enumeration **depth** per segmentation, not any
per-slot width; the shortlist-ordering, walk-rank and `LEXICAL_BRANCH_KEEP`
sweeps are refuted and must not be re-run; segmentation retention and pruning
are both already non-binding for this clue; and a share rule that gives a
structure's first segmentation the whole allowance will still miss a
rank-151-of-256 segmentation for exactly the reason the per-slot cap did.
`w-6b2f04` is therefore the sole remaining owner of the milestone, and
mechanism 2 (structure-aware allocation) is better motivated than a plain
global round-robin.

Coordinator re-measurement on the merged head, not carried forward:

```text
cargo test --release --test corpus_integration approximate_finds   1 passed, 1 failed
  approximate_finds_recognize_speech_resegmentation  ok
  approximate_finds_classic_madgab_resegmentation    FAILED (still)
  its top 50 is still ["it justice too bad aim", ... "it justice too bad came"]
  (2.08s; the tree was touched first, because the merge is docs-only and
   cargo would otherwise reuse the previous binary)
```

**Durability action on the live front.** `6b2f04` ([w-6b2f04](w-6b2f04.md))
is alive and mid-implementation, but its whole change — 141 insertions in
`src/lib.rs` — was uncommitted, on a branch whose remote head is still
`c191eaf`. A coordinator snapshot of its working tree now exists as
`refs/heads/wip/enum-portfolio-6b2f04` (`144d03e`), taken with
`git stash create` so the agent's index and working tree were not touched at
all. That is a safety net, not a deliverable: the snapshot ref was not
pushed and the agent was told to commit and push `madgab-enum-portfolio`
itself, and to verify `git branch --contains` before it finishes.

**Review findings on that in-progress diff**, all steered to `6b2f04`:

1. the named ceiling is not a ceiling. `SEG_STATE_KEEP` 32 -> 4096 and
   `SEGMENTATION_KEEP` 256 -> 4096 make
   `LEXICAL_GLOBAL_POP_BUDGET = SEGMENTATION_KEEP * LEXICAL_HEAP_POP_LIMIT`
   16,384,000 pops, against the 1,024,000 implicit worst case it names — a
   16x larger worst case presented as a bound. Retention of 4096 also moves
   the canonical segmentation off rank 151, so every depth claim must be
   re-measured at the retention actually shipped;
2. `emit_allowance.min(LEXICAL_COMBINATIONS_PER_SEGMENTATION)` can only lower
   spend, and a 64-deep window is what the refutation just priced as ~375x too
   shallow, so pool size and wall clock must be reported before and after for
   at least three non-canonical targets including
   `congratulations on your promotion`; if the pool shrinks, the structure
   allowance is the wrong knob;
3. the new unit test asserts `deepest <= allowance` over the **whole** pool,
   including beam-path clues this loop only appends to — scope it to what the
   enumeration funds, or prove the wider claim;
4. a doc comment in `src/lib.rs` links `../../docs/work/items/w-6b2f04.md`,
   which resolves outside the crate from `src/`;
5. deliverable order: prove enumeration first with
   `MADGAB_TRACE_PHRASES="hits justice dupe hid came"` on the release binary
   and report rank, pool size and wall clock, before polishing tests.

The agent acknowledged within the pass and is already renaming the ceiling to
an emission budget, adding a `structure_depth_ceiling` helper and fixing the
doc link, i.e. findings 1, 3 and 4 are being acted on. It was left **running**
for a later fresh pass, and its item was deliberately not edited here to
avoid two writers on one file.

Nothing was started on [w-2e5b93](w-2e5b93.md) and nothing was closed on
[w-a02d28](w-a02d28.md): pool membership is still the whole of the
requirement, so moving an axis weight would re-baseline
`approximate_output_is_locked` for no gain.

Next action for a later fresh pass, in priority order:

1. `6b2f04` on `madgab-enum-portfolio`. Check `git status` and `git log` in
   `/workspace/madgab-enum-portfolio` first: if it has committed and pushed,
   review the diff for the five findings above plus generality, run `--lib`,
   `--test corpus_integration`, `--test exact_determinism` and
   `--test approx_determinism` from the rebased tree, and integrate onto
   `post-milestone-acceptance`, never `main`. The acceptance measurement —
   the canonical clue enumerated and visible from the default path — is what
   closes the item; a passing unit test is not.
2. If it has not committed, take a second snapshot ref and steer again rather
   than merging uncommitted work. If it reports a refutation like
   `w-3e5c7`'s, close `w-6b2f04` with that record and open the next front
   from the evidence, do not let the milestone idle behind a spent agent.
3. Once `approximate_finds_classic_madgab_resegmentation` passes on its own
   merits, with no phrase-specific case in `src/`, close this item and
   [w-a02d28](w-a02d28.md) together, re-check the itinerary's milestone
   conditions including that the work is on `post-milestone-acceptance` and
   not `main`, and only then start [w-2e5b93](w-2e5b93.md).

## Pass 2026-09-26T12:25Z (coordinator coord-5b7e): a second front opened on enumeration *order*; the budget front is steered with the arithmetic that bounds it

No source change on this pass and nothing integrated: the sole live front is
mid-implementation with nothing committed. This item is re-claimed from
`coord-4f19` to `coord-5b7e`. The blocker count is unchanged at **one**,
`approximate_finds_classic_madgab_resegmentation`.

State confirmed on arrival rather than assumed:

- `post-milestone-acceptance` is at `25adfbe` and pushed; `origin` matches
  the local branch exactly. There is still no `main` branch on this host, local
  or remote, so the "never merge scheduled work into `main`" condition holds
  trivially.
- `6b2f04` ([w-6b2f04](w-6b2f04.md)) is alive on a **named branch**
  (`madgab-enum-portfolio`, HEAD `c191eaf`, remote head absent — never
  pushed) with 220 uncommitted insertions in `src/lib.rs`, plus untracked
  `.bench/` and `tests/zz_scratch_analysis.rs`. It has been running about an
  hour and has committed nothing, so the durability hazard is live again. A
  coordinator snapshot now exists as `wip/enum-portfolio-6b2f04` = `60afc66`
  and is **pushed to origin** (the previous pass's snapshot was local only),
  taken with `git stash create`, so the agent's index and working tree were
  not touched.

**Review of that in-progress diff, done on the diff rather than on the
agent's report.** It is a coherent, general mechanism and it fixes three of
the findings the previous pass raised:

- `LEXICAL_GLOBAL_EMISSION_BUDGET = SEGMENTATION_KEEP * LEXICAL_COMBINATIONS_PER_SEGMENTATION`
  (16384) and `LEXICAL_GLOBAL_POP_BUDGET = SEGMENTATION_KEEP * LEXICAL_HEAP_POP_LIMIT`
  (1024000) are named as the *old* implicit worst case, so the previous
  finding — that a raised `SEGMENTATION_KEEP` made the "ceiling" sixteen times
  larger than the bound it claimed — is genuinely fixed: the arithmetic is
  identical and only the distribution of the spend moves;
- the budget is now shared per boundary structure, keyed on the segmentation's
  own span list so the search and `select_diverse` count the same structure,
  with a breadth-first schedule over structures;
- the unit tests now assert the pure arithmetic of the two new helpers rather
  than over-claiming a bound over the merged pool, and the doc comment's
  out-of-crate relative link is gone. The agent additionally tightened
  `emit_allowance` from `.clamp(breadth, 64)` to `.clamp(1, 64)` mid-review,
  which closes a real leak: the old floor let a structure exceed its own
  ceiling by `breadth` for every extra alignment of that structure.

**The blocking arithmetic, which the previous passes had not put together.**
`w-3e5c7` measured that the canonical alignment, forced to index 0 of all
five slots with that segmentation's caps lifted, still sits at emission 23962
and pop 28930 of its own segmentation. The in-progress change clamps every
segmentation at 64 emissions and leaves the per-segmentation pop limit at
4000, while its depth ceiling `ceil(16384 / 137..180 structures) = 91..119`
is *above* 64, so each structure's first alignment still receives exactly the
64 it received before. Per-segmentation depth is therefore essentially
unchanged from a shape that has already been refuted, and no redistribution
across 256 segmentations can buy 28930 pops inside one of them: at most about
35 segmentations could be funded at that depth out of 1024000 pops. So the
change as written is unlikely to make the acceptance test pass, and the
mechanism that could — a per-segmentation pop and emission allowance *derived*
from the global pop budget, i.e. depth for a few structures — was named to
the agent, together with the instruction that the release-binary
`MADGAB_TRACE_PHRASES` measurement is the deliverable and comes before any
further test or doc polish. That measurement has now been asked for across two
passes without happening, so it is stated as the first deliverable again.
`6b2f04` was steered with this and is acting on it (restoring the shippable
version of the change, committing, then measuring).

**New front: [w-8f3c61](w-8f3c61.md)**, claimed and assigned to agent
`8f3c61` in `/workspace/madgab-depth-order` (branch `madgab-depth-order`,
from `25adfbe`). It owns the enumeration **order**, not the budget: visiting
the index tuples of a segmentation by max-index-first stratification (a
diagonal) rather than by best-first prefix bound, so that a wording which is
deep in exactly one slot costs additively instead of multiplicatively in the
levels. That is the direct consequence of the measurement above — the canonical
tuple's max index is 99 while the search pays 28930 pops to reach it — and it
is a different mechanism from `w-6b2f04`'s, so the two fronts compose rather
than duplicate. It is fenced explicitly: the global budgets, the per-structure
schedule and both helper functions belong to `w-6b2f04` and must not be
edited, and a refutation with numbers is an acceptable outcome.

Nothing was started on [w-2e5b93](w-2e5b93.md) and nothing was closed on
[w-a02d28](w-a02d28.md): pool membership is still the whole of the
requirement, so moving an axis weight would re-baseline
`approximate_output_is_locked` for no gain.

Next action for a later fresh pass, in priority order:

1. `6b2f04` and `8f3c61`. Review whichever lands first as a diff for
   generality — the bound must still be bounded with named arithmetic, no
   phrase-specific case, no `axes::*` move, no re-baselined output lock, no
   `zz_*`/`.bench` file on the branch — then run `--lib`,
   `--test corpus_integration`, `--test exact_determinism` and
   `--test approx_determinism` and integrate onto `post-milestone-acceptance`,
   never `main`. If one front is still measuring while the other has landed,
   integrate the landed one and do not wait.
2. Check both worktrees are on a named branch, not a detached HEAD, and that
   neither has left instrumentation or scratch files uncommitted. If a front
   reports a refutation with numbers, close its item with that record and let
   the other front carry the milestone; do not let the milestone idle behind a
   spent agent.
3. Only after `approximate_finds_classic_madgab_resegmentation` passes on its
   own merits, with no phrase-specific case in `src/`, close this item and
   [w-a02d28](w-a02d28.md) together, re-check the itinerary's milestone
   conditions including that the work is on `post-milestone-acceptance` and
   not `main`, and only then start [w-2e5b93](w-2e5b93.md).

## Pass 2026-09-26T13:10Z (coordinator coord-c3d81): the budget front closes with a refutation, its substrate lands, and the milestone gets a second live owner

This pass integrated one finished front, closed it, opened one new front, and
steered the one that was already running. The blocker count is unchanged at
**one**, `approximate_finds_classic_madgab_resegmentation`. `main` untouched;
`post-milestone-acceptance` is the only accumulation branch.

**Integrated and pushed: `eaab259`**, the merge of
`madgab-enum-portfolio` (`bff4938` + `a2da159`, agent `6b2f04`, item
[w-6b2f04](w-6b2f04.md), now `done`). Coordinator decision, which the agent
explicitly left open: **integrate**, because the commit is measured
output-neutral rather than merely small, it names the search's real worst case
as a property of the search instead of as the product of two constants each
written to bound one segmentation, and it is the substrate any later depth
work has to respect.

Validation re-run by this coordinator on that tree, not copied forward:

```text
cargo test --release --lib                        34 passed, 0 failed
cargo test --release --test exact_determinism      1 passed, 0 failed
cargo test --release --test approx_determinism     2 passed, 0 failed
cargo test --release --test corpus_integration     8 passed, 1 failed
  approximate_finds_recognize_speech_resegmentation  ok
  approximate_output_is_locked                        ok   (NOT re-baselined)
  approximate_finds_classic_madgab_resegmentation    FAILED (this item's target)
```

On the diff: `src/lib.rs` only, +251/-2; no `axes::*` constant and no scoring
function touched; the total budgets are arithmetically identical to the old
per-segmentation caps multiplied by `SEGMENTATION_KEEP`, so the bound is
unchanged and only its distribution moves. Three new unit tests, including one
external floor (`approximate_proposals_span_several_boundary_structures`) that
observes the visible list rather than the policy's belief about it.

**The refutation is the durable result, and it retires a whole family.** The
global emission budget, the global pop budget, per-segmentation depth,
per-slot width, and every allocation of them across 256 segmentations are all
refuted by measurement, not by price:

- per-segmentation pop ceiling raised to 29_000 (a 7.25x increase on the
  global pop budget): 721 ms of search, pool 17_405 -> 22_400, still missing;
- `LEXICAL_BRANCH_KEEP` raised to the full 160-wide shortlist: **20.0 s**, a
  10x wall-clock regression that this item's own criterion forbids, pool
  20_555, **still missing**;
- combined with `w-3e5c7`'s integrated measurement that the canonical
  alignment forced to index 0 of all five slots sits at emission 23_962 and
  pop 28_930 of its own segmentation, and that it is rank 151 of 256 by
  `span_objective`, no budget-derived quantity can prefer it.

The one piece of evidence that a fix exists is that same forced run *did*
reach the clue. So the remaining blocker is **algorithmic shape, not budget**:
a per-segmentation search that can reach a node which is cheap overall and
locally bad in one slot without enumerating the 23_961 better-bound nodes in
front of it.

**New front: [w-9d4e17](w-9d4e17.md)**, claimed and assigned to agent
`9d4e17` in `/workspace/madgab-slot-width` (branch `madgab-slot-width`, from
`eaab259`, pushed). It is not a duplicate of [w-8f3c61](w-8f3c61.md) and the
split is forced by the measurements above, not by convenience:

- `w-8f3c61` (agent `8f3c61`, still running) owns the **visit order** of the
  index tuples, so that depth in one slot is additive rather than
  multiplicative in the levels;
- `w-9d4e17` owns the **truncation point**. `LEXICAL_BRANCH_KEEP = 10` is
  applied to every slot's list *before* the traversal exists, so the
  canonical word at walk rank 99 is never pushed and **no visit order can
  reach it**. Uniform width is refuted and budget-derived per-slot width is
  refuted, but "keep the list and let the traversal's own budget stop reading
  it" has not been tried, and under the new order the extra width is only paid
  for at levels actually visited.

Neither front is sufficient alone, and the milestone therefore no longer
rests on a single live agent.

Both agents were steered and left running:

- `8f3c61` — told that its neighbour front is closed and integrated and that
  the budget family is refuted, and given the measurement that matters most to
  it: the 20.0 s full-width figure was measured under the *current*
  traversal, so it is a property of the order, not of the width, and its
  diagonal stratification is exactly what makes that width cost a sum. Also
  told, explicitly, that if its order change needs a width `9d4e17` has not
  delivered it must say so in its item rather than reach into the other
  worktree. Both fences restated in both directions so they compose.
- `9d4e17` — given the same rank table, the innocent-matcher and rank-151-of-
  256 facts, the refuted per-slot cap (costs 0.0-0.5 against `total_budget`
  1.5 make affordability vacuous), the 20.0 s full-width number, and the
  fence: list contents and truncation point are its own, ordering and heap key
  belong to `8f3c61`. Deliverable order is the release-binary
  `MADGAB_TRACE_PHRASES` measurement first, then pool and clock before/after on
  at least three non-canonical targets, then tests.

Nothing was started on [w-2e5b93](w-2e5b93.md) and nothing was closed on
[w-a02d28](w-a02d28.md): pool membership is still the whole of the
requirement, so moving an axis weight would re-baseline
`approximate_output_is_locked` for no gain.

Next action for a later fresh pass, in priority order:

1. `8f3c61` and `9d4e17`. Review whichever lands first as a diff for
   generality — a bound must still be bounded with named arithmetic, no
   phrase-specific case, no `axes::*` move, no re-baselined output lock, no
   `zz_*`/sweep/`.bench` file on the branch — then run the four suites above
   and integrate onto `post-milestone-acceptance`, never `main`. If one front
   has landed and the other is still measuring, integrate the landed one.
2. Check both worktrees are on named branches, not detached HEADs, and that
   neither has left instrumentation or scratch files uncommitted. If a front
   reports a refutation with numbers, close its item with that record and let
   the other front carry the milestone; do not let the milestone idle behind a
   spent agent.
3. Only after `approximate_finds_classic_madgab_resegmentation` passes on its
   own merits, with no phrase-specific case in `src/`, close this item and
   [w-a02d28](w-a02d28.md) together, re-check the itinerary's milestone
   conditions including that the work is on `post-milestone-acceptance` and
   not `main`, and only then start [w-2e5b93](w-2e5b93.md), whose per-axis
   spread table must be re-taken on the settled pool.

## Pass 2026-09-26T12:53Z (coordinator coord-7d42): the truncation front is refuted too, and a fifth mechanism is opened

No source change on this pass and nothing integrated: the only front with
committed work has a test that does not discriminate, so integrating it now
would import a non-guard. The blocker count is unchanged at **one**,
`approximate_finds_classic_madgab_resegmentation`. `main` is untouched (it
exists on `origin` at `c0ecd7c`; this host's fetch refspec only tracks
`post-milestone-acceptance`, which is why earlier passes recorded "no `main`
branch on this host" — that was a fetch artefact, not an absence, and the
"never push scheduled work to `main`" condition is real).

State confirmed on arrival rather than assumed:

- `post-milestone-acceptance` was at `85a8a60` and pushed; `origin` matched.
- Three agents are live and all three were left running: `8f3c61`
  ([w-8f3c61](w-8f3c61.md)), `9d4e17` ([w-9d4e17](w-9d4e17.md)) and the new
  `e07c42` ([w-e07c42](w-e07c42.md)).
- Suite state re-measured by this coordinator on two release binaries built
  from the true base and from `madgab-slot-width`'s head, not carried forward:

  ```text
                                   base 85a8a60    staged 8582525
  It's just a stupid game            2132 ms         2158 ms   pool 15710 -> 15710
  recognize speech                   2010 ms         2013 ms   pool 13251 -> 13279
  congratulations on your promotion  3354 ms         3418 ms   pool  7725 ->  7725
  MADGAB_TRACE "hits justice dupe hid came"   missing in both
  ```

**`9d4e17`'s front is finished, general, and refuted as the blocker.** It
replaced the uniform `LEXICAL_BRANCH_KEEP = 10` pre-filter with a staged width
schedule (`LEXICAL_BRANCH_STAGE_0 = 10`, growth 4, saturating at the widest
list) that only widens when the traversal has run out of nodes at the current
width and still wants wordings, reusing already-expanded nodes so a widening
does not re-walk the lattice. The diff is general: no phrase-specific case (the
`wreck|beach|recognize|justice|stupid|dupe|hid|came|hits` scan hits only unit
tests, comments and pre-existing `src/lexical.rs` doc examples), no `axes::*`
move, no re-baselined output lock, two real unit tests on the schedule's
arithmetic. It is **not** the milestone, and its own instrumentation says why:
the widening fires **0 times** on the six real multi-sentence targets and 1-6
times only on short ones, because the traversal's emission/pop bound always
binds first. That is a refutation of the truncation point as the blocker, and
with [w-8f3c61](w-8f3c61.md)'s order result it means the uniform pre-filter,
the per-slot cap, the global budget family and the visit order are *all* now
closed.

Review findings sent to `9d4e17` rather than fixed here, because its branch is
already pushed ahead of this coordinator's copy (`origin/madgab-slot-width` is
at `c683aa3`) and the agent owns the file:

1. the new test `approximate_pool_reaches_matches_deep_in_a_span` **passes on
   the parent commit**. Its observable (a clue *ending* in `lovey` / `meow`)
   is identical in base and staged for both its targets; the only pool
   difference is interior words on the first target (`ann anne app ask ass aye
   bad die ice lie s wow`), and `play games with me now` is word-for-word
   identical. Its doc comment's claim that the clue "was absent while every
   slot was truncated" is therefore false as written;
2. the `expanded` set is an `Rc` clone per pop on the common path, paid for a
   widening that almost never fires;
3. a doc comment became `//////` in `tests/corpus_integration.rs`.

**New front: [w-e07c42](w-e07c42.md)**, claimed and assigned to agent `e07c42`
in `/workspace/madgab-emit-spread` (branch `madgab-emit-spread`, from
`4da5517`). It owns the one mechanism no front has tried: **what the
per-segmentation emission allowance is spent on.** The allowance is 64
wordings taken as the cost-best tuples, and the canonical sits at emission
23 962 of its own segmentation; the fix is to make the allowance buy coverage
of the index-tuple space (for example by depth profile — which slots are deep)
rather than only its cheapest corner, keeping part of the allowance on the
cost-best tuples so ordinary quality does not regress. This rests on the
established measurement that **pool membership is the whole requirement**:
`select_diverse` admits a candidate from anywhere in the deduplicated pool
when its boundary structure is unrepresented or under its `share_cap` (17 at
`top_n = 50`, 7 at `top_n = 20`, against a cutoff holding only 5-6 distinct
structures), so the clue does not have to outscore anything. It is fenced
against both live fronts and against the integrated budgets.

`8f3c61` was also warned about its own evidence quality: its log contains a
run where the build had failed and the previous binary executed, and a row
whose parse concatenated the target-phrase trace line with the raw-cutoff rank
line, so its closed-form refutation — now the most load-bearing record in this
repository — should be re-measured from a confirmed build and quoted from the
target phrase's own trace line. It was also asked for the one thing only it can
say: whether *emission selection* can work at all given its own arithmetic, as
a bounded, named warning in its item, since `w-e07c42` is betting on it.

Nothing was started on [w-2e5b93](w-2e5b93.md) and nothing was closed on
[w-a02d28](w-a02d28.md): pool membership is still the whole of the
requirement, so moving an axis weight would re-baseline
`approximate_output_is_locked` for no gain.

Next action for a later fresh pass, in priority order:

1. `e07c42` ([w-e07c42](w-e07c42.md)) is the milestone's only untried
   mechanism. Review whatever lands as a diff for generality — an emission
   allowance must still be bounded with named arithmetic, no phrase-specific
   case, no `axes::*` move, no `zz_*`/`.bench`/`MADGAB_*` diagnostic on the
   branch — then run `--lib`, `--test corpus_integration`,
   `--test exact_determinism`, `--test approx_determinism` and integrate onto
   `post-milestone-acceptance`, never `main`.
2. `9d4e17` ([w-9d4e17](w-9d4e17.md)): once it has folded in the three review
   findings, review the branch and integrate it as a general improvement with
   its refutation on record. It is **not** the milestone and should not be
   allowed to delay item 1.
3. `8f3c61` ([w-8f3c61](w-8f3c61.md)): confirm it reverted the
   `MADGAB_DIAG_*` code, removed the `target-base/`/`target-staged/`
   directories, pushed, and closed its item as a refutation. Do not let the
   order family be revived by a later pass.
4. Only after `approximate_finds_classic_madgab_resegmentation` passes on its
   own merits, with no phrase-specific case in `src/`, close this item and
   [w-a02d28](w-a02d28.md) together, re-check the itinerary's milestone
   conditions including that the work is on `post-milestone-acceptance` and not
   on `main`, and only then start [w-2e5b93](w-2e5b93.md), whose per-axis
   spread table must be re-taken on the settled pool.

## Pass 2026-09-26T13:01Z (coordinator coord-b1f7): the order family is closed durably, and the last untried lever is opened

This pass integrated one finished front and opened one new front on the
only lever the refutations leave. The blocker count is unchanged at
**one**, `approximate_finds_classic_madgab_resegmentation`. `main` is
untouched; `post-milestone-acceptance` is the only accumulation branch,
now at `0f36c2e`.

**Integrated and pushed: `1ff73bb`**, the merge of `madgab-depth-order`
(`af56ecd`, agent `8f3c61`, item [w-8f3c61](w-8f3c61.md)). Review was on
the diff: `git diff 4da5517 af56ecd` touches **only**
`docs/work/items/w-8f3c61.md`, so `src/` is byte-identical and nothing in
it can be phrase-specific; the item's state moves `working` -> `done` on
the accumulation branch, so no later pass revives the enumeration-order
family. The branch is pushed (`origin/madgab-depth-order` = `af56ecd`).
The agent's own summary is right that there is no code here to integrate —
what the merge carries is the *refutation*, which is now on the branch a
pass reads by default rather than on a child branch it has to know about.

**That refutation is what this pass is really about, because it closes the
enumeration side of this item in closed form.** §8 of that item, measured
and re-verified from a confirmed build: the canonical tuple needs **>=
404,081 emissions** of its own segmentation to be emitted, the cheapest
depth-profile stratification is bounded below by **134,400 emissions**
(the coordinate-wise down-set, i.e. the best case over all stratified
schedules), against a per-segmentation allowance of **64** and a whole-search
global budget of **16,384**. So the residual cannot be bought with a
selection rule or a visit order at any affordable budget. Its §8.3 names
the only two remaining levers, and both were fenced when it was written:
change what the enumeration optimises, or grow the budget ~8x.

**New front: [w-3b8e15](w-3b8e15.md)**, claimed and assigned to agent
`3b8e15` in `/workspace/madgab-fuzzy-cost` (branch `madgab-fuzzy-cost`,
from `1ff73bb`, pushed). It takes the first lever, and it is the one thing
every previous front was barred from touching: **the quantity the
enumeration optimises** — the fuzzy matcher's own cost. The canonical clue
`hits justice dupe hid came` is a real, near-exact homophonic
resegmentation, yet the matcher charges it `hits 0.20, justice 0.00, dupe
0.15, hid 0.37, came 0.40`, total **1.12** against a `total_budget` of
1.5. In a correct cost model a homophone is nearly free, and because the
traversal is best-first in exactly that cost, overcharging it is what
buries it at emission 23 962 — a lower cost moves the clue into the
traversal-s cheapest corner and reaches it inside the budgets the search
already respects, with no budget, order, width or scoring change at all.
The mechanism is general by construction: a phonemic-confusability
substitution cost derived from the corpus alphabet instead of
`phonetics::distance` between two single IPA symbols, and/or a folding of
the IPA on both sides before comparison. It is fenced to `src/approx.rs`
so it does not contend with the two live fronts, both of which edit
`src/lib.rs`. Unlike every earlier front, a cost change is expected to
re-baseline `approximate_output_is_locked`; the item says so explicitly
and requires a justified before/after table rather than a silent
re-baseline.

**The two live fronts were steered, non-blockingly, and left running:**

- `e07c42` ([w-e07c42](w-e07c42.md)) — given §8-s bound and §8.3-s
  necessary condition, told that its mechanism is now a *bounded warning*
  rather than a veto, and given the honest, general bar that remains
  worth landing: slot depth 20+ within 64 emissions per segmentation
  against 1-3 today under per-slot round-robin, on real targets, no
  wall-clock regression, no pool shrinkage. Asked to record the milestone
  clue as out of reach for emission selection, with the arithmetic, so a
  later pass does not re-open it there. It acknowledged within the pass
  and was already fixing the ladder in its selection rule.
- `9d4e17` ([w-9d4e17](w-9d4e17.md)) — the three open review findings
  (the test that passes on its parent and whose doc comment is false as
  written; the `Rc` clone of the replay set on the common path; the
  `//////` doc comment in `tests/corpus_integration.rs`), plus the fact
  that `origin/madgab-slot-width` is still at `14986b2` so its new work is
  unpushed, and the untracked `target-base/`/`target-staged/` build trees
  to remove. Told explicitly not to rebase mid-restructure; the
  coordinator will sequence that integration.

**Housekeeping for whoever reads the durable state next:** the enumeration
and budget families are now closed, so the queue of *mechanisms* is
`w-3b8e15` (cost model, the milestone) and `w-e07c42`/`w-9d4e17` (general
enumeration quality, not the milestone). [w-2e5b93](w-2e5b93.md) remains
gated for the same reason as before — a weight move re-baselines the
output lock — but note that the gate is now weaker than it was, because
the lock will move anyway when `w-3b8e15` lands; start it after that
integration, with its per-axis spread table re-taken on the settled pool.
`w-04f83f` and `w-a02d28` stay `blocked`/open exactly as they are.

Next action for a later fresh pass, in priority order:

1. `3b8e15` ([w-3b8e15](w-3b8e15.md)) is the milestone-s only remaining
   owner. Review whatever lands as a diff for generality — a rule over IPA
   symbols or symbol classes, no word, phrase or substring named in
   `src/`, no `axes::*` move, no `zz_*`/`.bench`/`MADGAB_*` diagnostic on
   the branch — and check the per-word cost table and the before/after
   pool and wall-clock table before believing any claim. If the cost model
   cannot be improved, its refutation closes the milestone-s mechanism
   search and the item should say so plainly rather than spawn a sixth
   enumeration front.
2. `e07c42` and `9d4e17`: integrate whichever lands first as a general
   improvement, with its refutation on record, and do not let either delay
   item 1. Check both worktrees are on named branches with clean trees
   and no untracked build trees.
3. Only after `approximate_finds_classic_madgab_resegmentation` passes on
   its own merits, with no phrase-specific case in `src/`, close this item
   and [w-a02d28](w-a02d28.md) together, re-check the itinerary-s
   milestone conditions including that the work is on
   `post-milestone-acceptance` and not on `main`, then start
   [w-2e5b93](w-2e5b93.md).

## Pass 2026-09-26T13:20Z (coordinator coord-5e22): the untried budget lever is measured beside the cost model, and the three live fronts are steered for durability

Fresh reconciliation pass. Nothing was integrated, because nothing finished:
all three fronts from the previous pass were still running with uncommitted
work, and no branch was in a reviewable state. Blocker count is unchanged at
**one**, `approximate_finds_classic_madgab_resegmentation`. `main` is
untouched; `post-milestone-acceptance` advanced to `657c5a3` (this item plus
the new front below).

**State found on arrival, from repository and agent state rather than
timestamps.** `3b8e15` (madgab-fuzzy-cost, [w-3b8e15](w-3b8e15.md)) running
with `src/approx.rs` modified and *no commit of its own past the base
`0f36c2e`*; `e07c42` (madgab-emit-spread, [w-e07c42](w-e07c42.md)) running
with `src/lib.rs` modified on pushed base `4da5517`; `9d4e17`
(madgab-slot-width, [w-9d4e17](w-9d4e17.md)) running with `src/lib.rs`
modified on pushed `14986b2` **and three untracked build trees**
(`target-base/`, `target-staged/`, `target-staged2/` — one more than the
previous pass reported). The previous pass's concern that `madgab-slot-width`
was unpushed is resolved: `origin/madgab-slot-width` is at `14986b2`.

**New front: [w-be6d21](w-be6d21.md)**, claimed and assigned to agent
`be6d21` in `/workspace/madgab-budget-envelope` (branch
`madgab-budget-envelope`, from `aac97e1`). It takes the *second* of the two
levers [w-8f3c61](w-8f3c61.md) §8.3 named and that item explicitly refused to
take: "raising the total to ~8x is a budget decision that belongs to the
coordinator". It has never been measured, so the coordinator was deciding it
from a ratio. This front measures the envelope — global emission/pop budget
at 1x/2x/4x/8x/16x, search wall clock, pool size, and whether
`hits justice dupe hid came` is *enumerated* and separately whether it is
*ranked* into the visible 50, on four or more real targets — and returns a
verdict: affordable, not affordable, or affordable-but-insufficient.

It runs *beside* the cost model rather than after it for two reasons. The
levers are independent, and the budget answer is needed whichever way
`w-3b8e15` turns out; and the budget front's most likely finding is the
enumerated-but-cut case, which would relocate the milestone from a budget
problem to a scoring problem and hand the next pass a much sharper
[w-04f83f](w-04f83f.md) / [w-2e5b93](w-2e5b93.md) question than the one
currently written down. It is fenced to **documentation-only** commits
(`docs/work/budget-envelope.md`) so it adds a fourth `src/lib.rs`-touching
branch to a board that already has two, without inheriting an unreviewed
constant change.

**The three live fronts were steered, non-blockingly, and left running:**

- `3b8e15` — the milestone's only owner. Told to commit and push before its
  next experiment, because uncommitted work on a named branch is still lost if
  the agent finishes on it; told that its `zz_probe` test is currently spliced
  into `src/approx.rs` and must not survive into any commit (scratch belongs in
  `/tmp`); told it does not need an in-`src` probe to read the per-word cost
  table, since `MADGAB_TRACE_PHRASES` and the public API already expose it;
  and asked to report **enumerated** and **ranked** separately, since
  [w-8f3c61](w-8f3c61.md) measured the clue at 0.819901 against a top-50
  cutoff near 0.9063. An enumerated-but-cut result is a more useful outcome
  than a silent partial win, because it names the real blocker.
- `9d4e17` — durability housekeeping (three untracked build trees now, not
  two; commit and push before the next large step; clean tree and pushed
  branch at handoff; no mid-restructure rebase) and a request to state plainly
  which of the three open review findings are fixed: the test that passes on
  its parent with a doc comment that is false as written, the `Rc` clone of the
  replay set on the common path, and the `//////` doc comment in
  `tests/corpus_integration.rs`.
- `e07c42` — its bar restated unchanged (depth 20+ within 64 emissions of a
  segmentation on real targets versus 1-3 today, no wall-clock regression, no
  pool shrinkage, milestone clue recorded out of reach with the arithmetic),
  plus durability, and explicit permission to close as a refutation with
  numbers rather than manufacture a win.

**Unchanged on purpose:** [w-2e5b93](w-2e5b93.md) stays `open` and gated — its
per-axis spread table is only meaningful on a settled pool, and the pool is
about to move twice more (once per integration of `9d4e17`/`e07c42`, once for
whatever `w-3b8e15` and `w-be6d21` conclude). `w-04f83f` and `w-a02d28` stay
`blocked`. `w-a02d28`'s unblock condition is unchanged and objective:
`approximate_finds_classic_madgab_resegmentation` passing on
`post-milestone-acceptance` with the assertion unmodified.

Next action for a later fresh pass, in priority order:

1. Read what `3b8e15` and `be6d21` report, in that order. Between them they
   decide the milestone: one is the cost model, the other the budget. If
   either lands a reviewable commit, review the diff for generality first — a
   rule over IPA symbols or symbol classes, no word, phrase or substring of a
   canonical example named in `src/`, no `axes::*` move, no `zz_*`/`.bench`/
   `MADGAB_*` diagnostic left on the branch — then check its before/after cost
   or budget table before believing any claim. A refutation from either is a
   legitimate close-out and should be recorded as one, not worked around.
2. Integrate `9d4e17` and `e07c42` as general enumeration improvements, with
   their refutations on record, and in that order of readiness rather than of
   merit. Neither may delay item 1. Check each worktree is on a named branch
   with a clean tree and no untracked build trees.
3. Once the blocker is objectively gone, close this item and
   [w-a02d28](w-a02d28.md) together, re-check every milestone condition in
   [../skills/itinerary-madgab.md](../skills/itinerary-madgab.md) including
   that the work is on `post-milestone-acceptance` and not `main`, and only
   then start [w-2e5b93](w-2e5b93.md) with its spread table re-taken on the
   settled pool.

## Pass 2026-09-26T14:45Z (coordinator coord-7a31): the emission-spread front is reviewed, merged and closed; the other three are steered and left running

Fresh reconciliation pass. **Something was integrated this time**, unlike
the last two. `post-milestone-acceptance` advanced `f0ec2c1` -> `6d1d58d` and
is pushed. `main` is untouched. Blocker count is unchanged at **one**,
`approximate_finds_classic_madgab_resegmentation`.

**Integrated: `madgab-emit-spread` as `6d1d58d`** (branch commits `19f6c2e`
+ `d494ae9`), closing [w-e07c42](w-e07c42.md) as `done`. A reserved slice of
each segmentation's 64 emissions now buys coverage of the index-tuple space
— depth profiles at a geometric index ladder — instead of all of it going to
a best-first walk that only ever reaches the cost-best corner. Measured by
the agent: deepest slot index reached inside a segmentation rises from 9 to
80, pool grows on every real target, visible top 50 byte-identical on six
targets, no wall-clock regression, milestone clue still not enumerated with
the arithmetic on record.

Review before merging, all negative: no canonical phrase, word or substring
of one in the production region (every hit is inside a `#[cfg(test)]` module
past line 1236); no `zz_`/`MADGAB_TRACE_*`/`.bench` diagnostic added (the
`MADGAB_TRACE_*` env reads in the region are pre-existing base code); no
`axes::*` move; no new `tests/` churn. Independent validation run by the
coordinator on the merged tree, not taken on the agent's word:
`cargo test --release --lib` 36/36 ok; `--test corpus_integration` 8 passed /
1 failed, the single failure being the canonical blocker and identical to
base; `--test exact_determinism` 1/1; `--test approx_determinism` 2/2. This
is an integration as a general coverage improvement with a refutation on
record, **not** progress toward the acceptance case.

**A durability gap was found and closed.** `origin/madgab-emit-spread` was at
`7bfd169` while the worktree held `19f6c2e` unpushed — a real, reviewable
commit one force of bad luck from being lost. The coordinator pushed it, and
the agent then recorded the item close-out as `d494ae9`, which was also
pushed. Lesson applied to the fronts below: unpushed work on a named branch
is lost work, and this pass checked `git ls-remote` rather than local refs
(local remote-tracking refs are absent for these branches because the pushes
used explicit refspecs, which is why the previous pass's durability audit was
partly blind).

**Steered, non-blocking, all left running:**

- `3b8e15` (madgab-fuzzy-cost, [w-3b8e15](w-3b8e15.md)) — the milestone's
  only owner. `8b1a61f` is pushed, so the per-word cost table and the
  codepoint-distance finding (mean 0.7825 across distinct symbols; `k` vs
  `U+0261` measures 1.000) are durable. Steered with `--steer` because the
  agent was mid-run: keep the confusability cost a general articulatory
  metric rather than a rule naming a symbol or symbol class, keep canonical
  strings out of production `src/`, report **enumerated** and **ranked**
  separately, and commit-and-push before the next large experiment and again
  before stopping.
- `be6d21` (madgab-budget-envelope, [w-be6d21](w-be6d21.md)) — its
  documentation-only fence is currently **broken**: the worktree has
  `M src/lib.rs`. Told to get the multiplier experiment out of every commit
  (`git checkout -- src/lib.rs` before committing, or park it on a scratch
  branch that is never merged) and to park it as a document rather than an
  unreviewed constant, and to push before stopping. It is not on `origin` at
  all yet.
- `9d4e17` (madgab-slot-width, [w-9d4e17](w-9d4e17.md)) — still carrying
  uncommitted `M src/lib.rs`, `M tests/corpus_integration.rs` and three
  untracked build trees on top of pushed `14986b2` after three passes of
  durability reminders, so it was told plainly that only the last build tree
  may remain at handoff and it must be gitignored. Given the new coupling:
  the merged `EMIT_PROFILE_RESERVE` and its geometric `LEXICAL_BRANCH_KEEP`
  widening **share the same 64 emissions**, so the two pool deltas are not
  additive and its next number must come from the composition or be labelled
  single-change. Its three open review findings (a test whose doc comment is
  false as written, the `Rc` clone of the replay set on the common path, and
  the `//////` at `tests/corpus_integration.rs:282`) are still unaddressed
  and it must state which are fixed.

**Unchanged on purpose.** [w-2e5b93](w-2e5b93.md) stays `open` and gated on a
settled pool, and the pool just moved again with this merge. `w-04f83f` and `w-a02d28` stay `blocked`; `w-a02d28`'s unblock
condition is unchanged and objective: the blocker test passing on
`post-milestone-acceptance` with its assertion unmodified.

Next action for a later fresh pass, in priority order:

1. Read what `3b8e15` and `be6d21` report, in that order. Between them they
   decide the milestone — cost model and budget. `be6d21`'s enumerated-but-cut
   outcome is the most valuable thing it can return, because it relocates the
   milestone from a budget problem to a scoring problem. Check `git ls-remote`
   for both branches, not local refs, before believing any durability claim.
2. Integrate `9d4e17` next, as a general enumeration improvement with its
   refutation on record, only after it has committed and pushed the work that
   is currently uncommitted and resolved the composition question above. If
   its numbers turn out to be single-change and it cannot measure the
   composition, integrate it anyway but say so in the merge message, exactly
   as this pass did for the enumerated-but-unranked refutation.
3. When the blocker is objectively gone, close this item and
   [w-a02d28](w-a02d28.md) together, re-verify every milestone condition in
   [../skills/itinerary-madgab.md](../skills/itinerary-madgab.md) including
   that the work is on `post-milestone-acceptance` and not `main`, and only
   then start [w-2e5b93](w-2e5b93.md) with its spread table re-taken on the
   settled pool.

## Pass record, 2026-09-26T15:55Z (coordinator coord-4d92)

### Integrated: w-be6d21, the budget family, closed with a number

`be6d21` **succeeded** (exit 0) and its branch was clean: the entire diff
against its base `aac97e1` is one added file, `docs/work/budget-envelope.md`
(431 lines), documentation only, with `git diff HEAD -- src/` empty at every
commit — the fence held. Reviewed and merged into `post-milestone-acceptance`
as **`89c85ff`**; the branch is now durable. [w-be6d21](w-be6d21.md) is
marked `done`.

The verdict, and it is the most useful number this milestone has produced
since the order family closed:

- **Affordable but insufficient**, and *not* 8x short — the global budget is
  **unreachable, not short**. `LEXICAL_GLOBAL_EMISSION_BUDGET` is already at
  its maximum reachable value at 1x, because the whole search can spend at
  most `SEGMENTATION_KEEP * LEXICAL_COMBINATIONS_PER_SEGMENTATION =
  256 * 64 = 16,384` emissions; `emit_allowance` is clamped per segmentation
  at `src/lib.rs:1015-1017`. Every multiple above 1x multiplies a number that
  cannot be spent, and the search confirms it byte-for-byte: 1x, 2x, 4x, 8x
  and 16x give the **same 15,710-candidate pool, the same rank-49 cutoff
  0.915691888 and the same verdict on all five targets**. The pool saturates
  at **177,029** candidates by 64x.
- **No budget knob should be landed.** The shipped constants are correct and
  provably non-binding. Its handoff item 4 — a one-line comment in
  `src/lib.rs` recording the `256 * 64 = 16,384` identity — is a genuine
  improvement and is left to `w-3b8e15`, which already owns `src/lib.rs`, so
  that it becomes a small additive change to a reviewed diff rather than a
  branch of its own.
- **The three regimes, measured and stable at every budget point.** This is
  the operative finding for the milestone:
  1. *enumerated-and-ranked* — `wreck a nice beach` for `recognize speech` is
     **already correct at 1x, at rank 28**. That half of the milestone is
     solved on the current head, and the itinerary's `wreck a nice beach`
     condition is met.
  2. *enumerated-and-cut* — `congratulations new wrap a motion` at rank 439,
     `now ill of` at rank 299. A real, measured, recoverable population that a
     **scoring** change can address and **no width or budget** change can.
  3. *not-enumerated* — the canonical `hits justice dupe hid came`. Blocked
     by `LEXICAL_BRANCH_KEEP = 10`, i.e. per-slot **width**, not depth.
- Consequently the milestone is **two problems in series, in two different
  places**: reach (width/order — `w-9d4e17`, `w-3b8e15`) and then ranking
  (score, gap 0.0958 against the 0.9157 cutoff — `w-04f83f`,
  `w-2e5b93`). Fixing either alone does not close it. The budget family is
  closed for good; do not open a third front on it.

### Steered, left running

- `3b8e15` (madgab-fuzzy-cost, [w-3b8e15](w-3b8e15.md)) — running, steered
  with `--steer` (4 prompts, turn restarted this pass). Told the budget lever
  is dead so it stops experimenting on it, that width is `w-9d4e17`'s
  constant and not its own, that it is now the only non-width lever left, and
  asked to commit, push, update its item with objective numbers and report. A
  refutation stated plainly is an acceptable result. Carried over from
  `coord-7a31`: keep the confusability cost a general articulatory metric
  rather than a rule naming a symbol, keep canonical strings out of production
  `src/`, and report **enumerated** and **ranked** separately.
- `9d4e17` (madgab-slot-width, [w-9d4e17](w-9d4e17.md)) — running, steered
  with `--steer` (5 prompts, turn restarted this pass). The new measurement
  names its own constant: the pool saturates *because* `LEXICAL_BRANCH_KEEP =
  10`, so width is now the single binding constraint on the milestone. Asked
  to stop opening new experiment families, commit, push, update its item with
  the width-vs-runtime frontier and report; explicitly told a refutation or a
  precise statement of the affordable frontier is a full result, and that
  `w-6b2f04`'s full width at 17.2 s (10x wall clock) is not affordable while
  its own 0.7 s holds only in combination with the non-myopic order that
  `w-8f3c61` refuted in closed form. Its three review findings are still
  unaddressed and it must state which are fixed.

### Unchanged on purpose

[w-2e5b93](w-2e5b93.md) stays `open` and gated: its own note says the
per-axis spread is only meaningful on a settled pool, and this merge moved the
pool's ceiling arithmetic rather than settling it. `w-04f83f` and `w-a02d28`
stay `blocked`; `w-a02d28`'s unblock condition is unchanged and objective —
the blocker test passing on `post-milestone-acceptance` with its assertion
unmodified.

### Next action for a later fresh pass, in priority order

1. Read what `3b8e15` and `9d4e17` report, cost model first. Check
   `git ls-remote` for both branches, not local refs, before believing any
   durability claim.
2. Integrate `9d4e17` as a general enumeration improvement with its refutation
   on record, once it has committed and pushed and answered the composition
   question `coord-7a31` raised (the merged `EMIT_PROFILE_RESERVE` and its
   geometric `LEXICAL_BRANCH_KEEP` widening **share the same 64 emissions**,
   so the two pool deltas are not additive; a single-change number must be
   labelled as one).
3. Re-verify the itinerary's `wreck a nice beach` condition against the
   current head rather than trusting the rank-28 report, and fold the
   `src/lib.rs` non-binding-budget comment into whichever front next owns
   `src/lib.rs`.
4. Only once the pool is settled, start [w-2e5b93](w-2e5b93.md) with its
   spread table re-taken — and note from this pass's regime 2 that it is a
   *scoring* item for a population that already exists, which strengthens its
   case for starting once regime 3 is closed.

## Pass 2026-09-26T13:45Z (coordinator coord-5e19): both fronts closed, one integrated, the cost lever refuted, and the milestone's last untried lever opened

This pass changed the accumulation branch twice and changed the milestone's
understanding of itself once. `post-milestone-acceptance` advanced `0e9ece9` ->
`d33d9c4` and is pushed; `main` is untouched at `c0ecd7c` and this host's
fetch refspec does not track it, so nothing scheduled can reach it by accident.

**Integrated: `4c2200a`**, the merge of `madgab-slot-width` (`1da9d48` +
`c794ea3`, agent `9d4e17`, item [w-9d4e17](w-9d4e17.md), now `done`).
`LEXICAL_BRANCH_KEEP`'s uniform pre-filter is replaced by a staged width that
the traversal opens at 10 and widens only when it has drained at the current
width and still wants wordings. Reviewed on the diff — general, no canonical
phrase, word or substring in production `src/`, no `axes::*` move, no scoring
change, no re-baselined output lock, no diagnostic or build tree left on the
branch — and measured by this coordinator on the release binary built from
`1da9d48`:

```text
recognize speech          'wreck a nice beach'  rank=27 score=0.918313383  (identical to base)
It's just a stupid game  'hits justice dupe hid came'  missing, pool 18926  (unchanged)
```

So the itinerary's **first** canonical condition is preserved exactly and the
**second** is untouched: a general improvement with its refutation on record,
not progress toward the milestone. The agent's own scoped caveat is the durable
part and is in its item: the widening fires 0 times on multi-clause targets
and +25..+114 candidates at 0% wall clock on short ones, so the refutation is
a statement about the *current* traversal, and the widening fire count is the
number to re-measure first if the traversal ever changes.

**Closed as a refutation: [w-3b8e15](w-3b8e15.md)**, the fuzzy matcher's cost
model, the last untried lever the enumeration families left. Its close-out
(`0969565`) is cherry-picked as `71cf4e4`, and only the close-out: the
confusability table and the indel weighting are **not** landed, because this
coordinator reproduced, on the agent's own binary and against a base whose
`src/` is byte-identical, the regression it reported:

```text
base    recognize speech  'wreck a nice beach'  rank=27 score=0.918313383
8bfecb0 recognize speech  'wreck a nice beach'  missing candidates=16415
base    canonical target  'hits justice dupe hid came'  missing candidates=18926
8bfecb0 canonical target  'hits justice dupe hid came'  missing candidates=18797
```

A rebuilt articulatory-feature substitution cost lowered the canonical clue's
price 3.07x (1.1195 -> 0.3650) and changed nothing, because it rescaled every
candidate by about the same factor rather than discriminating: three of its
five slots moved *later* in rank (22->27, 64->92, 85->86) while getting
cheaper. Cheaper is not earlier. And it cost the one canonical case that
worked. Its own recommendation — re-land the mechanism on top of a width or
reach fix, never before one — is adopted and recorded in its item.

What *is* landed from that branch is the one-hunk `src/lib.rs` comment
recording [w-be6d21](w-be6d21.md)'s measurement that
`LEXICAL_GLOBAL_EMISSION_BUDGET` and `LEXICAL_GLOBAL_POP_BUDGET` are provably
non-binding and inert from 1x to 256x, so no later pass spends a turn tuning
them. w-be6d21 had deliberately left that comment to this front because it
already owned `src/lib.rs`.

**Suite state on `c4b6290`, re-measured by this coordinator, not carried
forward:**

```text
cargo test --release --lib                       39 passed, 0 failed
cargo test --release --test corpus_integration   9 passed, 1 failed
  approximate_finds_recognize_speech_resegmentation  ok
  approximate_output_is_locked                        ok  (NOT re-baselined)
  approximate_finds_classic_madgab_resegmentation    FAILED (this item's target)
cargo test --release --test exact_determinism     1 passed, 0 failed
cargo test --release --test approx_determinism    2 passed, 0 failed
search wall clock                               1.07 s / 1.21 s on the two canonical targets
```

**The state of the milestone, stated plainly because it changed:** with both
live fronts finished, *no running agent could move it*, and the reason is now
a single sentence rather than a list of hypotheses. The canonical clue is not
lost to the objective, not lost to the matcher's cost, not lost to the
traversal's order, not lost to the budget's *size*, and not lost to a slot's
width. It is lost to the **definition** of the budget: `emit_allowance` is
`depth_ceiling.clamp(1, LEXICAL_COMBINATIONS_PER_SEGMENTATION)`, so the whole
search can never spend more than the sum of those clamps, and
`LEXICAL_GLOBAL_EMISSION_BUDGET = SEGMENTATION_KEEP * 64 = 16,384` is already
at its maximum reachable value. That is why w-be6d21's 1x..256x sweep was
inert — it multiplied a number that cannot be spent — and it is consistent
with every refutation on record: the canonical alignment, forced to index 0 of
all five slots, needs emission 23,962 and pop 28,930 of its own segmentation
([w-3e5c7](w-3e5c7.md)), the pop budget of 1,024,000 can pay for that, and
only the emission ceiling cannot.

**New front: [w-5f1c04](w-5f1c04.md)**, claimed, assigned to agent `5f1c04` in
`/workspace/madgab-approx-budget` (branch `madgab-approx-budget`, from
`c4b6290`). It owns the decoupling of the two constants and nothing else, and
it is fenced against every closed family by name. This is also the lever
[w-8f3c61](w-8f3c61.md) §8.3 named and refused as a coordinator budget
decision, so taking it here is the decision, made on the arithmetic rather
than on a ratio. It is the only live route to the second canonical condition,
and it runs beside nothing: `src/lib.rs` is free now that `w-9d4e17` is
integrated and `w-3b8e15` is closed.

**Unchanged on purpose.** [w-2e5b93](w-2e5b93.md) stays `open` and gated: the
pool is about to move again, and its per-axis spread table is only meaningful
on a settled one. `w-04f83f` and [w-a02d28](w-a02d28.md) stay `blocked`, and
`w-a02d28`'s unblock condition is unchanged and objective —
`approximate_finds_classic_madgab_resegmentation` passing on
`post-milestone-acceptance` with its assertion unmodified. Note for whoever
lifts that gate: [w-be6d21](w-be6d21.md)'s regime 2 (`enumerated-and-cut`, e.g.
`congratulations new wrap a motion` at rank 439 against a 0.9157 cutoff) is a
real population that a scoring change can address and no width or budget
change can, so the ranking axis is the *next* family after reach, not an
alternative to it.

Next action for a later fresh pass, in priority order:

1. `5f1c04` ([w-5f1c04](w-5f1c04.md)) is the milestone's only owner. Review
   whatever lands as a diff for generality — a bound must still be bounded
   with named arithmetic, no phrase-specific case, no `axes::*` move, no
   re-baselined output lock, no `zz_*`/`.bench`/`MADGAB_*` diagnostic on the
   branch — and check its before/after pool and wall-clock table before
   believing any claim. The acceptance measurement is the release-binary
   `MADGAB_TRACE_PHRASES` result, not a passing unit test.
2. `9d4e17` and `3b8e15` are finished and closed; confirm their worktrees are
   clean and their branches contain their heads, and do not let either
   front be revived. `madgab-fuzzy-cost` keeps three code commits that are
   deliberately unlanded; the re-land condition is written in its item.
3. Only after `approximate_finds_classic_madgab_resegmentation` passes on its
   own merits, with no phrase-specific case in `src/`, close this item and
   [w-a02d28](w-a02d28.md) together, re-check every milestone condition in
   [../skills/itinerary-madgab.md](../skills/itinerary-madgab.md) including
   that the work is on `post-milestone-acceptance` and not on `main`, and only
   then start [w-2e5b93](w-2e5b93.md) with its spread table re-taken on the
   settled pool.

## Pass 2026-09-26T19:45Z (coordinator coord-a13c): the budget-decoupling front is being refuted in flight, and the traversal's *shape* is opened as a second front

Fresh reconciliation pass. Nothing was integrated — no front had a
reviewable commit. `post-milestone-acceptance` advanced `769733d` -> `b9a31ca`
(this record plus the new item) and is pushed. `main` untouched. Blocker
count unchanged at **one**, `approximate_finds_classic_madgab_resegmentation`.
Claim transferred from `coord-4d92` to `coord-a13c`.

**State found on arrival, from repository and agent state.** `5f1c04`
([w-5f1c04](w-5f1c04.md), `/workspace/madgab-approx-budget`) running with
`M src/lib.rs` — 84 lines, all of it `zz_env_usize`/`zz_phrase`/`zz_note`/
`ZZ_*` instrumentation — **no commit of its own past base `c4b6290`** and
**not on `origin` at all**. Every other front from the previous three passes
is `succeeded` and closed (`9d4e17`, `3b8e15`, `be6d21`, `e07c42`, `6b2f04`,
`8f3c61`), so the milestone rested on one agent, unpushed, doing measurement.

**What `5f1c04` found, and why it relocates the milestone.** It decoupled the
emission ceiling for real (`ZZ_GEMIT` above the
`SEGMENTATION_KEEP * 64 = 16,384` identity) and funded *one* boundary
structure deeply. At `ZZ_DEEPCAP` = 65,536 / 131,072 / 262,144 the
whole-search emissions are 79,711 / 80,367 / 80,367 — the funded structure
saturates at ~66,176 emissions against a per-slot product on the order of
10^9, and **`hits justice dupe hid came` is still not reached at any of
them**. So [w-5f1c04](w-5f1c04.md)'s premise was half right in an instructive
way: the global budget really was unreachable by construction, but
decoupling it does not buy the clue, and the residual is not budget at all.
It is the traversal's *shape* — best-first by cumulative cost, with a
stage-0 width of 10 that [w-9d4e17](w-9d4e17.md) records as widening
**0 times** on multi-clause targets, which is exactly the canonical target.
That closes the budget family for good, second and final time.

**New front: [w-c1d3a7](w-c1d3a7.md)**, claimed and assigned to agent
`c1d3a7` in `/workspace/madgab-adjacency` (branch `madgab-adjacency`, from
`769733d`). It owns the one mechanism no front has tried and the measurements
point straight at: an **adjacency operator** — from wordings already emitted
into the pool, substitute one slot at a time with that slot's alternative,
re-score, admit. Depth in one slot becomes additive rather than
multiplicative, so the node that [w-3e5c7](w-3e5c7.md) reached by *forcing*
index 0 of all five slots is one substitution away from a candidate that
already exists. It is a different shape from every closed family — none of
them changed what a single edge of the search costs — and it is fenced away
from the traversal loop in `src/lib.rs`, which `5f1c04` owns. A refutation
with numbers is an acceptable outcome, as it was for six families here.

**`5f1c04` was steered and left running**, with the refutation stated as the
expected result rather than a failure: write up the saturation arithmetic from
a confirmed build, commit and push **before anything else** (its work is one
force of bad luck from being lost), keep the `zz_*` scaffolding out of any
commit destined for `post-milestone-acceptance`, land the decoupling only if
it is worth landing on its own merits, do not keep grinding the depth sweep,
and report the per-slot widths line for two or three non-canonical targets
because that bounds how far a substitution operator can move in one step.

**Unchanged on purpose.** [w-2e5b93](w-2e5b93.md) stays `open` and gated: the
pool is moving again and its per-axis spread is only meaningful on a settled
one. `w-04f83f` and [w-a02d28](w-a02d28.md) stay `blocked`, with
`w-a02d28`'s unblock condition unchanged and objective —
`approximate_finds_classic_madgab_resegmentation` passing on
`post-milestone-acceptance` with its assertion unmodified.

**Suite state, not re-measured this pass:** no `src/` change landed on the
accumulation branch since `4c2200a`, on which the previous pass measured
`wreck a nice beach` for `recognize speech` at rank 27, score 0.918313383,
identical to base. That figure still stands but should be re-verified rather
than assumed once the next front lands.

Next action for a later fresh pass, in priority order:

1. `5f1c04` and `c1d3a7`, whichever lands first. Review as a diff for
   generality — a bound must still be bounded with named arithmetic, no
   phrase-specific case, no `axes::*` move, no re-baselined output lock, no
   `zz_*`/`.bench`/`MADGAB_*` diagnostic or build tree on the branch — then
   run `--lib`, `--test corpus_integration`, `--test exact_determinism` and
   `--test approx_determinism` and integrate onto `post-milestone-acceptance`,
   never `main`. If `5f1c04` closes as a refutation, integrate its record
   even with no `src/` change; that is what keeps a family from being
   reopened.
2. Check `git ls-remote` for both branches, not local refs, before believing
   any durability claim. `madgab-approx-budget` was absent from `origin`
   entirely when this pass started.
3. Only after `approximate_finds_classic_madgab_resegmentation` passes on its
   own merits, with no phrase-specific case in `src/`, close this item and
   [w-a02d28](w-a02d28.md) together, re-check every milestone condition in
   [../skills/itinerary-madgab.md](../skills/itinerary-madgab.md) including
   that the work is on `post-milestone-acceptance` and not `main`, and only
   then start [w-2e5b93](w-2e5b93.md).

## Pass 2026-09-26T20:10Z (coordinator coord-7b30): milestone now rests on one agent, and its region is uncontested

Fresh reconciliation pass. No source change and nothing integrated: no front has a
reviewable commit, so there was nothing to land. `post-milestone-acceptance` stays
the only accumulation branch and `main` is untouched. Blocker count unchanged at
**one**, `approximate_finds_classic_madgab_resegmentation`. Claim transferred from
`coord-a13c` to `coord-7b30`.

**State found on arrival, from repository and agent state, not from the previous
pass-s summary.**

- `5f1c04` (`/workspace/madgab-approx-budget`, branch `madgab-approx-budget`) is
  `running` and has **still made no commit of its own**. `git ls-remote` shows the
  branch at `1e0a743`, and `git diff c4b6290..madgab-approx-budget` touches
  `docs/work/items/*` only — i.e. the branch carries the coordinator-s commits, not
  the agent-s work. Its tree is clean apart from an in-progress edit to its own item
  file, so the `zz_*` instrumentation the previous pass found in `src/lib.rs` has
  been moved out to `/tmp` as instructed. Its own log now records that `src/` and
  `tests/` are byte-identical between `c4b6290` and `5f7df59` and that its binary
  hash matches, which is the provenance the previous pass asked for; the saturation
  arithmetic is therefore measured on the final rebased tree and is ready to write up.
- `c1d3a7` (`/workspace/madgab-adjacency`, branch `madgab-adjacency`, pushed at
  `6de84cb`) is `running` on the adjacency operator: `src/adjacency.rs` untracked
  and `src/lib.rs` modified, 218 insertions. It is **editing `src/lib.rs` despite the
  fence in its item**, which that fence permits only while `5f1c04` owns the
  traversal loop. The fence is therefore lifted explicitly this pass rather than left
  ambiguous, because `5f1c04` is closing.
- `madgab-adjacency` and `madgab-approx-budget` are both present in `origin`, so the
  durability hazard from the 19:45Z pass is closed. Both worktrees are on named
  branches, not detached HEADs.

**Milestone re-measured on the pushed head `5f7df59` by this coordinator, not copied
forward.**

```text
cargo test --release --test corpus_integration approximate_finds   1 passed, 1 failed (2.34s)
  approximate_finds_recognize_speech_resegmentation  ok
  approximate_finds_classic_madgab_resegmentation    FAILED (still)
  its visible list is still it justice too <X> aim wordings
MADGAB_TRACE raw phrase="hits justice dupe hid came" missing candidates=17231
MADGAB_TRACE raw_cutoff rank=49 score=0.915691888 phrase="it justice too day mm"
```

**Actions taken.** Both agents steered and left running.

- `5f1c04` — its measurement is accepted as a complete refutation, with the explicit
  instruction that a refutation is a first-class result on this front and not a
  failure. Ordered: write the close-out into its own item with the saturation
  arithmetic and the binary-hash provenance, **commit and push it before anything
  else**, then decide the decoupled emission ceiling on its own merits and revert the
  scaffolding if it is not worth landing (reverting is the expected outcome), then
  stand down. It has begun the item file; the worktree shows it uncommitted, so the
  next pass must confirm the push with `git ls-remote`, not a local ref.
- `c1d3a7` — told it is now the single front carrying the milestone, that its fence
  on `src/lib.rs` is lifted because `5f1c04` is standing down, and given three
  facts that should shorten it: pool membership alone closes the milestone because
  `select_diverse` step 2 admits from anywhere in the deduplicated pool for an
  unrepresented or under-capped boundary structure, so no `axes::*` move and no
  output-lock re-baseline; `wreck a nice beach` is enumerated and visible at raw rank
  27 / score 0.918313383 and losing it is a rejection rather than a trade; and
  ENUMERATED and RANKED must be reported separately, as conflating them has cost a
  pass before.

**Deliberately not started.** No sixth enumeration front: the budget family is closed
with a number and the remaining lever is the traversal-s *shape*, which `c1d3a7`
owns. [w-2e5b93](w-2e5b93.md) stays `open` and gated, because the pool is moving and
its per-axis spread is only meaningful on a settled one. `w-04f83f` and
[w-a02d28](w-a02d28.md) stay `blocked` with unchanged, objective unblock conditions.

Next action for a later fresh pass, in priority order:

1. `5f1c04` first, because it is finishing. Confirm with `git ls-remote --heads
   origin madgab-approx-budget` that the docs-only refutation record is pushed,
   review it, and integrate it even though it carries no `src/` change — that is
   what stops the budget family being reopened a third time. Confirm the branch
   carries no `zz_*`, `MADGAB_*` or `scratch/` content and that its worktree is
   clean.
2. `c1d3a7` as soon as it reports. Review as a diff for generality — a bound must
   still be bounded with named arithmetic, no phrase-specific case in `src/`, no
   `axes::*` move, no re-baselined `approximate_output_is_locked`, no `zz_*`/
   `.bench`/`MADGAB_*` diagnostic or build tree on the branch — then run `--lib`,
   `--test corpus_integration`, `--test exact_determinism` and `--test
   approx_determinism` and integrate onto `post-milestone-acceptance`, never
   `main`. If it is still mid-measurement, integrate `5f1c04`-s record and leave it
   running rather than waiting.
3. Only after `approximate_finds_classic_madgab_resegmentation` passes on its own
   merits, with no phrase-specific case in `src/`, close this item and
   [w-a02d28](w-a02d28.md) together, re-check every milestone condition in
   [../skills/itinerary-madgab.md](../skills/itinerary-madgab.md) including that the
   work is on `post-milestone-acceptance` and not `main`, and only then start
   [w-2e5b93](w-2e5b93.md).

## Pass 2026-09-26T20:55Z (coordinator coord-2f6a): the budget family is closed for good, one front left plus a review front

Fresh reconciliation pass, and the first one with something to land: the previous
pass's action 1 was "integrate `5f1c04`'s refutation even though it carries no
`src/` change", and that is what this pass did. `post-milestone-acceptance`
remains the only accumulation branch; `main` is untouched. Blocker count
unchanged at **one**, re-measured on the merged head. Claim transferred from
`coord-7b30` to `coord-2f6a`.

### Integrated

```text
fdc2769  Merge madgab-approx-budget: the emission ceiling is refuted as the
          binding wall, the budget family closes for good (w-5f1c04 close-out)
          parents eefda6f (coord-7b30 record) + 12318cb (agent 5f1c04 close-out)
          docs/work/items/w-5f1c04.md only; src/ and tests/ byte-identical to 5f7df59
3ec1ced  Open w-b4e8d1: a review front
```

Reviewed before merging, as a diff:

- the commit touches **one file**, `docs/work/items/w-5f1c04.md`;
  `git diff --stat 5f7df59 fdc2769 -- src tests Cargo.toml` is **empty**, so no
  source change was smuggled in with the record;
- `git grep -E 'zz_|ZZ_|MADGAB_' 12318cb -- src tests` returns only the standing
  `MADGAB_TRACE_*` diagnostics, which are present on the base too, so no
  scaffolding is being carried in;
- `madgab-approx-budget` was already durable: `git ls-remote --heads origin`
  shows `refs/heads/madgab-approx-budget` at `12318cb`, and the agent's
  `scratch/w-5f1c04-depth-probe` at `f2746f5` is a separate ref, as intended.

**Verifier, on the merged head `fdc2769`, this pass, not copied forward:**

```text
cargo test --release --test corpus_integration   9 passed, 1 failed  (10.15s)
  approximate_finds_classic_madgab_resegmentation  FAILED (still)
  got: ["it justice too bad aim", ..., "it justice too pug aim", "it justice too bad came"]
```

The first canonical condition still holds as recorded (`wreck a nice beach` at
rank 27 / 0.918313383 on `5f7df59`, `src/` unchanged since), and is re-measured
by the review front rather than assumed.

### Record correction that a later pass must not miss

The `5f1c04` numbers that were in flight when the previous pass wrote its record,
**and that are still quoted in [w-c1d3a7](w-c1d3a7.md)**, are **superseded
artefacts of a buggy build** and must not be carried forward:

| | superseded (buggy build) | confirmed (clean build, `f2746f5`) |
|---|---|---|
| whole-search emissions at `ZZ_DEEPCAP` 131,072 | 80,367 | **145,247** |
| at 262,144 | 80,367 | **276,319** |
| "saturation" | ~66,176 | **none — the traversal never saturates** |

The bug was a `LEXICAL_GLOBAL_EMISSION_BUDGET` reference split across two lines
that a string-level override missed, so the real 16,384 ceiling was still live at
one traversal exit and the "saturation point" was that ceiling rather than a
property of the traversal. The **conclusion is unchanged** — the clue is absent at
every depth, 4,096 / 16,384 / 65,536 / 131,072 / 262,144 / 1,000,000 / 2,000,000
— but the mechanism is different and stronger: at 2,000,000 the traversal has
covered 0.075% of its own 2,666,496,000-leaf product, and the wall is
`LEXICAL_BRANCH_STAGE_0 = 10` plus drain-before-widen, not a budget.
`w-c1d3a7`'s owner has been told to correct its own file; this entry exists
because that file is mid-edit by a running agent and must not be written by a
coordinator.

### `5f1c04`: stood down, worktree retired from the critical path

Agent `5f1c04` is `succeeded` (exit 0) and its item is `done` as a refutation.
`src/lib.rs` is byte-identical to `c4b6290`, so the decoupled emission ceiling
was deliberately **not** landed, on the measured grounds in its §5: the global
budget is already slack at 1x (14,239 of 16,384 spent), so the change would be
a comment-level fix with no test that could distinguish it, and the version of
it that *would* change behaviour cannot reach the clue at an affordable price.
The budget family is now closed twice over — [w-be6d21](w-be6d21.md) and
[w-5f1c04](w-5f1c04.md) — and `madgab-approx-budget` is archived rather than
carried forward.

### `c1d3a7`: steered, still the only front that can move the milestone

State found on arrival, from repository and agent state:

- `running`, 3 prompts at the start of this pass, now 4 — the steer below landed;
- its WIP commit `c71e699` ("adjacency operator module and call site, canonical
  not yet reached") is on the branch but **not on `origin`**, and
  `git ls-remote --heads origin madgab-adjacency` shows `7e49cc6`, which is
  **not an ancestor** of `c71e699`. So `origin` does not currently hold the
  adjacency module, and that WIP is one bad pass from being unrecoverable.
  Durability was made its first order.
- its uncommitted `src/lib.rs` diff is **entirely `ZZ_SCRATCH` measurement
  scaffolding** (`ZZ_MINCOST`, `ZZ_INJECT`, `SEGMENTATION_KEEP 2048`), which is
  the right place for it and the wrong place for it to stay.
- its in-flight `SEGMENTATION_KEEP = 2048` experiment: pool 56,697, search
  16.6 s, clue still missing, three segmentations host the canonical
  (structural ranks 151, 187, 284 of 2048, slot ranks `[22, 0, 64, 85, 7]` for
  the first). That is a **budget-size** change, i.e. the family closed above, and
  an 8x wall-clock regression is a rejection rather than a trade. It was told to
  report the measurement and not land it.

Steered with the four confirmed numbers above, the per-slot width table (median
160 = `SPAN_SHORTLIST`, saturated, and `hid` at walk rank 99 is *inside* it, so
one substitution step reaches an index traversal depth provably cannot — the
quantitative case for its own mechanism), the instruction to finish on the
committed baseline rather than on top of its scaffolding, and the unchanged
acceptance criteria. **Left running.** Its region is uncontested: `5f1c04` has
stood down, so the fence on `src/lib.rs` stays lifted.

### New front, parallel and non-contending: [w-b4e8d1](w-b4e8d1.md)

Claimed, pushed (`3ec1ced`) and launched as agent `b4e8d1` in
`/workspace/madgab-review` on branch `madgab-review-b4e8d1`, from `fdc2769`. Two
obligations nobody else owned, both read-only with respect to code, fenced away
from `c1d3a7`'s worktree and from `src/`:

1. **the no-hard-coding audit.** The itinerary's milestone condition "the
   implementation does not contain phrase-specific hard-coding for these
   examples" has been asserted in prose for many passes and never *checked*
   against the tree. The review front must check it, record a re-runnable
   command, and individually justify the standing `MADGAB_TRACE_*` exceptions as
   observation-only by reading them.
2. **a fence review of `c71d3a7`'s WIP commit `c71e699`** — the adjacency diff, reviewed for bounds-with-named-arithmetic, no phrase case, no `axes::*` move,
   no re-baselined `approximate_output_is_locked`, no diagnostic left behind, no
   `HashMap` iteration order reaching output, and no unpaid wall-clock or pool
   regression. This exists so integration is not the first time anyone reads
   that diff, and so the single live front is not also its own reviewer.

**Deliberately not started.** Still no sixth enumeration front: the budget family
is closed with a number and the remaining lever is the traversal's *shape*, which
`c1d3a7` owns. [w-2e5b93](w-2e5b93.md) stays `open` and gated, because the pool
is moving and its per-axis spread is only meaningful on a settled one.
[w-a02d28](w-a02d28.md) and `w-04f83f` stay `blocked` with unchanged, objective
unblock conditions.

### Next action for a later fresh pass, in priority order

1. **`c1d3a7`.** Confirm with `git ls-remote --heads origin madgab-adjacency`
   that the adjacency code is actually pushed, since as of this pass it is not.
   Review as a diff for generality, run `--lib`, `--test corpus_integration`,
   `--test exact_determinism` and `--test approx_determinism`, then integrate
   onto `post-milestone-acceptance`, never `main`. A refutation with numbers on
   three non-canonical targets is an acceptable outcome and should be integrated
   the same way `5f1c04`'s was.
2. **`b4e8d1`.** Read its pushed review; if the audit finds a real
   phrase-specific case in `src/`, that is a rejection of whatever produced it
   and takes priority over integration.
3. Only after `approximate_finds_classic_madgab_resegmentation` passes on its
   own merits, with no phrase-specific case in `src/`, close this item and
   [w-a02d28](w-a02d28.md) together, re-check every milestone condition in
   [../skills/itinerary-madgab.md](../skills/itinerary-madgab.md) including that
   the work is on `post-milestone-acceptance` and not `main`, and only then start
   [w-2e5b93](w-2e5b93.md).

### Addendum, same pass, after the steer landed

The durability hazard above was **closed inside this pass**: `madgab-adjacency`
was re-pushed as `e5d77e3`, whose first parent is `fdc2769`, i.e. it rebased onto
the merged head and its tree contains `src/adjacency.rs`, with `src/lib.rs` at
blob `198aca3` — the clean, un-scaffolded version, so the `ZZ_SCRATCH` and
`SEGMENTATION_KEEP 2048` edits are **not** in the pushed commit. The local
worktree still shows `M src/adjacency.rs` and `M src/lib.rs`, which is
in-flight work, not lost work. Note that `e5d77e3` is a rewrite of `c71e699`
rather than a descendant of it, so a later pass must read `e5d77e3` and must not
try to cherry-pick the older sha.

## Pass 2026-09-26T21:05Z (coordinator): the review target moved, and was corrected before the review landed

Fresh reconciliation pass. State found: `post-milestone-acceptance` clean at
`aa453c1`; two Antonina agents alive (`c1d3a7` on the adjacency front, 34m and
4 prompts; `b4e8d1` on the review front, 11m and 1 prompt); `w-2e5b93` open and
still gated; `w-a02d28` and `w-04f83f` blocked with unchanged unblock
conditions; `main` untouched.

### The review front was pointed at a dead commit, and is now pointed at the live one

`w-b4e8d1` part 2 instructs the agent to fence-review `c71e699`. That commit
was **rewritten** in the previous pass: the branch now carries `e5d77e3` (whose
first parent is `fdc2769`) and, since this pass began, `9767caf`. A review of
`c71e699` would have been a review of text that no longer exists, and its
"do not cherry-pick the older sha" warning had not reached the agent.

Steered `b4e8d1` (via `--steer`; the plain `prompt` refused because the agent
was mid-turn) to review `e5d77e3` and `9767caf` instead, to mark any `c71e699`
findings as historical if it had already made them, and to keep parts 1 and 3
unchanged. The steer landed; `prompts` went 1 -> 2. The agent is running the
new instruction and its `--lib` run is green at 39/39.

The steer also handed it one observation to verify independently rather than
trust: `src/adjacency.rs` uses `HashSet` only for membership (`held`, `seen`)
and never iterates either, and ordering comes from `BinaryHeap` pops plus an
explicit sort, so the determinism fence looks satisfied. That is a claim, not
a verdict, and this front exists to check claims.

### `c1d3a7` has a second commit; the branch is now fully durable

`9767caf` "fund the adjacency operator from the global budget, and correct its
two bounds": `ADJACENCY_POPS` 96 -> 24, `ADJACENCY_PER_SLOT` 4 -> 2, and the
operator's bound moved from a carve-out of the per-segmentation allowance to
`LEXICAL_GLOBAL_EMISSION_BUDGET`, because the carve-out cost a deep-in-a-span
match (`approximate_pool_reaches_matches_deep_in_a_span`). The agent's own
fence scan of its additions for the canonical words came back clean, and its
module tests now assert the invariant the mechanism actually provides (every
admission is one single-slot substitution from a held wording) rather than a
slot index the walk does not reach.

**Coordinator action:** pushed `madgab-adjacency` to `9767caf`
(`e5d77e3..9767caf`), so both commits are on the remote and the previous
pass's durability hazard cannot recur. The agent is still running; it may
commit more.

### Deliberately not done

No integration yet: `c1d3a7` is mid-flight and `b4e8d1` has not reported, so
there is nothing reviewed to land, and the itinerary requires review before
integration. No new front: the budget family is closed, the remaining lever is
the traversal's *shape*, which `c1d3a7` owns, and `w-2e5b93`'s per-axis spread
is only meaningful on a settled pool. No weight sweeps.

### Next action for a later fresh pass, in priority order

1. **`b4e8d1`.** Read its pushed review of `e5d77e3` + `9767caf`. A real
   phrase-specific case in `src/` is a rejection and outranks integration. If
   it files a new work item for a mechanism defect, claim that next.
2. **`c1d3a7`.** When it finishes, review as a diff for the fences, then run
   `--lib`, `--test corpus_integration`, `--test exact_determinism` and
   `--test approx_determinism` yourself before integrating. A refutation with
   numbers on three non-canonical targets is an acceptable outcome and is
   integrated the same way `5f1c04`'s was.
3. Only after `approximate_finds_classic_madgab_resegmentation` passes on its
   own merits with no phrase-specific case in `src/`, close this item and
   `w-a02d28` together, re-check every milestone condition in
   [../skills/itinerary-madgab.md](../skills/itinerary-madgab.md), and only
   then start `w-2e5b93`.

## Pass 2026-09-26T22:55Z (coordinator `coord-5e19`): the blocker moved from
reach to visibility, the review landed, and the two records that disagree about
*why* are reconciled into one measurable front

Fresh reconciliation pass. Found on arrival: `post-milestone-acceptance` clean at
`3ad01a1`; two Antonina agents alive (`c1d3a7` on the adjacency front, 38m and
4 prompts; `b4e8d1` on the review front, 16m and 2 prompts); both had *finished
their substance* since the last pass and pushed it; `w-2e5b93` open and still
gated; `w-a02d28` and `w-04f83f` blocked with unchanged objective unblock
conditions; `main` untouched at `c0ecd7c`.

### The review front landed, and it is now integrated

`b4e8d1` pushed `1992b76` on `madgab-review-b4e8d1` (docs only, 726 insertions):
the **no-hard-coding audit of `src/` and `tests/` on `aa453c1` is clean**, with
the method recorded as one re-runnable `git grep`, and the two
`MADGAB_TRACE_*` blocks justified individually as observation-only by reading
them. That discharges the itinerary's "no phrase-specific hard-coding" milestone
condition as a checked fact rather than an assertion, which several earlier
passes only asserted in prose.

Its fence review of the adjacency WIP found two real mechanism defects and filed
them as their own item, `w-6f3a91`, precisely so they would not die with a
review item. Merged here as `662a532`, so the defects are discoverable from the
accumulation branch and not only from a review branch. `w-b4e8d1` stays
`working` until the agent process exits; a later pass flips it to `done` with the
audit's re-runnable command quoted.

### The adjacency front closed as a refutation, and the refutation is sound

`c1d3a7` pushed `413113a`: `src/` unchanged from `9767caf`, plus a 243-line
close-out. The result is the most useful thing this repository has produced in
several passes, because it is a *negative* result with numbers:

* the second canonical clue's own alignment **is in a retained pool** — slot
  indices `[7, 0, 13, 99, 11]`, per-word costs totalling 1.120 inside the 1.5
  budget;
* **injected and re-scored, its real final score is 0.819901291, at raw rank
  8,967 of 17,553.** A perfect enumerator still would not show it. So for this
  clue the order is score-then-reach, not reach-then-score, and no further
  reach work can close the target;
* the axis table is the actionable part: the clue is strictly **worse** than the
  cutoff-setting clue on *boundary novelty* (0.667 vs 1.000) and *word
  familiarity* (0.399 vs 0.768), equal on novelty and rhythm, and strictly
  *better* on the acoustic aggregate. It loses on structure and commonness, not
  on pronunciation;
* the first canonical condition is **preserved**: `wreck a nice beach` for
  `recognize speech` at raw rank 27, score 0.918313383, identical with and
  without the operator. Not a trade;
* `SEGMENTATION_KEEP` = 2,048 was measured on a scratch branch and **rejected**:
  pool 56,697, 16.6 s, clue still absent. Consistent with `w-be6d21` and
  `w-5f1c04`; the reach families are closed for good.

Suites: `--lib` 46 passed; `corpus_integration` 9 passed / 1 failed (the known
blocker, output verbatim in the close-out); `exact_determinism` 1;
`approx_determinism` 2. No fmt/clippy on this host.

The coordinator checked the two claims that would have invalidated it and both
hold: `SEGMENTATION_KEEP` is 256 on the branch (the 2,048 was scratch-only), and
the decisive measurement is **injection-based**, so it does not depend on the
adjacency walk admitting anything. Defect 1 in `w-6f3a91` — `pops` spent on
seeds, so the operator is a no-op whenever `|pooled| >= pops`, which is most
segmentations — therefore makes the operator *worth less* than its close-out
claims, and leaves the score-bound conclusion exactly as true. The five
`// ZZ_SCRATCH` markers are committed at `413113a` (`src/lib.rs` lines 1055,
1316, 1699, 1740, 1755) but are bare comments with no instrumentation behind
them. So: `w-6f3a91` is claimed for `c1d3a7` with a cleanup commit as its next
action, and **`madgab-adjacency` is not proposed for integration in this pass** —
a branch carrying a comment that contradicts its own constant is not a
reviewable integration candidate. Its measured win on
`congratulations on your promotion` (two wordings at 0.888 entering at ranks 3-4,
no target's list worse, pool +0.8..12.9%, wall clock within noise) is preserved
on the branch for a later pass to land after the cleanup.

### The conflict this pass found, and the front it produced

`413113a` §8.3 concludes the clue "is cut, by 0.0958" against the visible cutoff
and names "the visible cutoff" as a lever. [w-2e5b93](w-2e5b93.md)'s status note
says the opposite — the list is not decided by the cutoff, because step 2 walks
the whole pool. Reading `select_diverse` on this head shows **both are
incomplete**: step 2 is `for &i in &order` over the whole pool, gated only by
`counts[structure] < cap` with `cap = share_cap(top_n, available.len())` and
`available` the pool-wide structure count. So a below-cutoff candidate *is*
reachable, and what actually binds is the candidate's **rank within its own
boundary structure** against that cap. The first claim treats step 1's bound as
if it governed the whole list; the second never names the within-structure rank
that does.

That is a precise, testable, and *general* question — it is about how selection
composes a global score order with a per-structure quota, not about any
sentence — so it is filed as [w-7b2d40](w-7b2d40.md), claimed in the same push
that created it, and assigned to a new agent `7b2d40` in
`/workspace/madgab-capfront` on branch `madgab-cap-front-7b2d40`. Its first
criterion is to *measure* the within-structure rank distribution and the cap on
six targets and report the share of below-cutoff candidates that are visible. If
that share is already healthy, it closes as a refutation with numbers, and the
score deficit goes to `w-2e5b93` as a new item instead. Fences: no `axes::*`
weight sweep (that question is already owned and a sweep was refuted once), and
nothing in `src/adjacency.rs`.

`w-2e5b93` stays `open` and stays gated, and the gate is now *sharper*: its
per-axis spread table must be re-taken on a settled pool, and the pool is now
settled in the only sense that matters — the reach families are closed and the
residual is score and selection. It starts after `w-7b2d40` reports, not before,
and only if the measurement puts the residual on the score side.

### Deliberately not done

No integration of `madgab-adjacency` (cleanup owed, see above). No weight
sweep. No change to `w-a02d28` or `w-04f83f`, whose unblock conditions are
objective and unchanged. No attempt to close this item: the milestone condition
on the second example is still unmet, and closing it on the strength of a
refutation would be exactly the "done" that is not done.

### Next action for a later fresh pass, in priority order

1. **`7b2d40`.** Read its measurement. If the canonical clue's within-structure
   rank is the binding number, the fix is a selection-policy question with a
   named cap; if the score deficit is, it hands off to `w-2e5b93` with numbers.
2. **`c1d3a7`.** Confirm the cleanup commit for `w-6f3a91` is pushed, review it
   as a diff, then integrate `madgab-adjacency` as a *general* mechanism with
   the refutation documented, or record the refusal to integrate with numbers.
   Either way the first canonical condition must still hold at rank 27.
3. **`b4e8d1`.** Once its process exits, flip `w-b4e8d1` to `done` quoting the
   audit's re-runnable command.
4. Only after `approximate_finds_classic_madgab_resegmentation` passes on its
   own merits with no phrase-specific case in `src/`, close this item and
   `w-a02d28` together, re-check every milestone condition in
   [../skills/itinerary-madgab.md](../skills/itinerary-madgab.md) including that
   the work is on `post-milestone-acceptance` and not `main`.

## Pass 2026-09-26T14:45Z (coordinator `coord-7d3f1`): both live fronts steered, one in-flight tree made durable, one permanently-stuck item un-stuck

Fresh reconciliation pass, coordination only. Nothing was integrated, because
nothing had finished: the two live fronts are both still mid-work. The blocker
count is unchanged at **one**,
`approximate_finds_classic_madgab_resegmentation`. `main` is untouched and
`post-milestone-acceptance` was clean, pushed, and at `3496b86` on arrival
(`origin/post-milestone-acceptance` matched local exactly, so nothing was
unpushed).

**Objective state re-measured on this pass-s own head, not carried forward:**

```text
cargo test --release --test corpus_integration approximate_finds   1 passed, 1 failed (2.02s)
  approximate_finds_recognize_speech_resegmentation  ok
  approximate_finds_classic_madgab_resegmentation    FAILED
    top 50 is still ["it justice too bad aim", "it justice too pad aim",
    "it justice too bad same", ..., "it justice too pug aim",
    "it justice too bad came"]
```

Both live fronts, and what each actually owns:

* `c1d3a7` (`madgab-adjacency`, `413113a`, 5 prompts) is alive and mid-cleanup
  on [w-6f3a91](w-6f3a91.md). Its worktree holds **236 insertions / 162
  deletions uncommitted** across `src/adjacency.rs` (+229/-33) and
  `src/lib.rs` (-169), and the diff is pointed in the right direction: it
  deletes the `ZZ_SCRATCH`, `ZZ_SEGS`, `ZZ_SLOTS`, `ZZ_ADJ` and `ZZ_TOTALS`
  instrumentation that the previous pass recorded as committed at `413113a`,
  and rewrites the operator. Nothing in it touches `axes::*`, names a
  canonical word or phrase, or re-baselines the output lock — a scan of the
  whole diff for those, for `eprintln!` additions and for `MADGAB_*` reads
  returns no added hit.
* `7b2d40` (`madgab-cap-front-7b2d40` at `a4d92b7`, clean tree) is 6 minutes
  in on [w-7b2d40](w-7b2d40.md) and has committed nothing yet, which is correct
  for a measurement front. Its base is the accumulation head-s parent, so no
  rebase is needed at this point.

**A durability hazard was found and closed.** `c1d3a7`-s entire cleanup — the
thing that would make `madgab-adjacency` a reviewable integration candidate —
was uncommitted on a named branch, which is the exact state that cost this
repository a front in the 15:10Z pass on 2026-09-26. A coordinator snapshot now
exists as `refs/heads/wip/c1d3a7-cleanup-7d3f1` = `d049ab9`, **pushed to
origin**, taken with `git stash create` so the agent-s index and working tree
were not touched. That is a safety net, not a deliverable: the branch itself
still has to be committed and pushed by the agent, which it has been told.

**Both agents were steered and left running**, each with a bar stated as a
review criterion rather than a request:

* `c1d3a7` — told the seven things this pass will check on its report: both
  defects, not just Defect 1, with before/after allocation counts per
  segmentation for the w-d17a62 fence; Defect 1-s arithmetic and a comment that
  agree with each other; an explanation or a correction for the **red**
  seed-heavy test it wrote (8 pops against 40 seeds admitted five new
  wordings, which is what the item-s accounting says cannot happen) rather than
  a relaxed assertion; the first canonical condition quoted at a recorded rank;
  ENUMERATED and RANKED/VISIBLE reported separately; the standing fences; and a
  rebase onto `3496b86` with the four suites re-run. `cargo fmt` and
  `clippy` cannot run on this host and must not be claimed.
* `7b2d40` — measurement-first bar restated, on a confirmed build of
  `3496b86`: distinct structures, the cap that follows, the within-structure
  rank distribution, the share of below-cutoff candidates that are still
  visible, and which of the two numbers the canonical example fails. ENUMERATED,
  RANKED and VISIBLE as three separate numbers, because they have been
  conflated before and it cost a pass. A refutation close-out with numbers stays
  an acceptable outcome and hands the residual to [w-2e5b93](w-2e5b93.md).

**Durable state corrected: [w-04f83f](w-04f83f.md) is `superseded`, not
`blocked`.** Its unblock condition was “when `w-7b41d2` lands a general
lexical-reachability fix”, and `w-7b41d2` is closed as a refutation with the
whole reach family behind it, so the condition can no longer fire and a later
pass would otherwise keep re-reading it as a live dependency. Its surviving
general half — the per-axis spread table over the 2000 best candidates of ten
real targets — is already filed as [w-2e5b93](w-2e5b93.md), which is where the
weight question now lives.

### Deliberately not done

No integration of `madgab-adjacency`: the cleanup commit does not exist yet, and
a branch that ships a comment contradicting its own constant is not a
reviewable candidate. No new front: the two untried questions are already
owned — visibility/cap by `7b2d40`, and the score side by `w-2e5b93`, whose
gate is `7b2d40`-first. Opening a third `src/`-touching front now would contend
with both and re-baseline the output lock for no gain. No weight sweep, no
change to `w-a02d28`, whose unblock condition is still objective and unchanged.
No attempt to close this item: the milestone-s second condition is still unmet
on its own merits, and closing it on the strength of a refutation would be
exactly the `done` that is not done.

### Next action for a later fresh pass, in priority order

1. **`7b2d40`.** Read its measurement. If the canonical clue-s within-structure
   rank is the binding number, the fix is a selection-policy question with a
   named cap; if the score deficit is, it hands off to [w-2e5b93](w-2e5b93.md)
   with numbers, and that item-s gate is then lifted.
2. **`c1d3a7`.** If the cleanup commit is pushed, review it as a diff for
   generality and for both criteria 2 and 3 above, re-run the four suites, and
   integrate `madgab-adjacency` as a general mechanism with the refutation
   documented — or record the refusal to integrate with numbers. Either way
   `wreck a nice beach` for `recognize speech` must still hold at a recorded
   rank.
3. Only after `approximate_finds_classic_madgab_resegmentation` passes on its
   own merits with no phrase-specific case in `src/`, close this item and
   [w-a02d28](w-a02d28.md) together, re-check every milestone condition in
   [../skills/itinerary-madgab.md](../skills/itinerary-madgab.md) including that
   the work is on `post-milestone-acceptance` and not `main`, and only then
   start [w-2e5b93](w-2e5b93.md).

## Pass 2026-09-26T14:53Z (coordinator `coord-5a11c`): the adjacency front is integrated; the blocker is now named as score-then-selection

This pass landed work rather than only steering it. `main` untouched;
`post-milestone-acceptance` is at `661e312` and pushed; it is still the only
accumulation branch. The blocker count is unchanged at **one**,
`approximate_finds_classic_madgab_resegmentation`.

**Integrated: `8cc71c4`**, the merge of `madgab-adjacency` (`2944011`, agent
`c1d3a7`, items [w-c1d3a7](w-c1d3a7.md) and [w-6f3a91](w-6f3a91.md)), pushed.
This is what the previous pass listed as its second priority, and its
condition — a pushed cleanup commit that settles Defect 1 with arithmetic
that agrees with its own comment — was met. Review detail and the fence scan
are in [w-6f3a91](w-6f3a91.md); the four suites were re-run on the merged
tree:

```text
cargo test --release --lib                       50 passed, 0 failed
cargo test --release --test corpus_integration    9 passed, 1 failed
  approximate_finds_recognize_speech_resegmentation  ok
  approximate_finds_classic_madgab_resegmentation    FAILED (still)
cargo test --release --test exact_determinism     1 passed
cargo test --release --test approx_determinism    2 passed
```

`tests/` is untouched by that branch, so `approximate_output_is_locked` passes
without re-baselining — the useful signal that this was a mechanism and not a
disguised selection change. Measured on the merged tree with
`MADGAB_TRACE_PHRASES`: `wreck a nice beach` for `recognize speech` is raw
rank 27, score 0.918313383 against a 0.917045726 cutoff, i.e. the first
canonical condition holds at exactly its pre-merge figure, while
`hits justice dupe hid came` is `missing` from a pool of 18,065 candidates
against a 0.915691888 cutoff. Both were re-measured on this head rather than
carried forward from the agent's notes.

**The blocker is now described by mechanism rather than by symptom**, which is
the most durable thing this repository has learned about it. From
[w-c1d3a7](w-c1d3a7.md) §8, re-derived by this pass against the diff: the
canonical alignment *is* in the pool, at index `[7,0,13,99,11]` of a retained
segmentation, one substitution per slot from the corner. It is 0.0958 below
the visible cutoff, and it is **better** than the cutoff clue on acoustic
cost. Its deficit lives on two closed scoring axes — boundary novelty
(0.667 vs 1.000, because a five-word clue that reproduces only two of the
target's inner boundaries is penalised for it) and word familiarity (0.399
vs 0.768) — and it sits under roughly 10^4 better wordings of the same
boundary structure. Three consequences, and they close doors rather than open
them:

1. No further *reach* work on this clue can help. A bounded score-ordered
   walk cannot promote a candidate that is 10^4 siblings down inside its own
   structure; a coverage rule that ignores score has no way to prefer index
   99 in one slot over the other 159. The reach family — `w-6d1f45`,
   `w-7b41d2`, `w-6b2f04`, `w-5f1c04`, `w-c1d3a7`, `w-6f3a91` — is closed.
2. `SEGMENTATION_KEEP` 256 -> 2048 was measured and rejected on this head
   (pool 56,697, search 16.6 s, clue still absent), so the budget-size family
   is closed a third time. It is recorded as measured-and-not-landed, not as
   an untried idea.
3. What remains is **score, then selection**, and each already has an owner:
   the score question is [w-2e5b93](w-2e5b93.md)'s, and the visibility
   question — whether a 17,553-candidate pool should have `select_diverse`
   step 1 reach past the cutoff at all, given it is bounded by the `top_n`
   best-scoring candidates — is [w-7b2d40](w-7b2d40.md)'s, whose criterion 2
   asks exactly that question with numbers.

**`7b2d40` was steered and left running** (prompts 2 -> 3), non-blockingly:
rebase onto `8cc71c4` because the merge changes pool and structure counts;
the reach refutation above, so it does not re-derive it; keep the harness on
`scratch/7b2d40-measure`, which this pass pushed, and keep instrumentation
off the integration branch; the fences unchanged (`axes::*`, closed class,
`src/adjacency.rs` and the `ADJACENCY_*` constants all off limits, no
phrase-specific case, no relaxed test, no unexamined output-lock
re-baselining); and an explicit bar — the criterion-1 table over six targets
with ENUMERATED, RANKED and VISIBLE as three separate numbers, then a landed
general change or a close-out with numbers that makes the next front
unambiguous. A third round of measurement is not an acceptable end state.
If its measurement puts the residual on the score side, its gate condition
for [w-2e5b93](w-2e5b93.md) is met and that item starts.

**A durability hazard was found and closed.** `origin/madgab-adjacency` still
points at `413113a`, which the agent's rebase replaced, so the finished
front's work existed only on a local branch; and the `7b2d40` harness existed
only on a scratch branch. Both are now on the remote: the adjacency branch as
`origin/wip/adjacency-5a11c` at `2944011` (deliberately *not*
force-pushed over `origin/madgab-adjacency`) and
`origin/scratch/7b2d40-measure` at `15396cf`.

### Deliberately not done

No new front. The two untried questions are owned — visibility and cap by
`7b2d40`, the score side by `w-2e5b93` — and opening a third `src/`-touching
front now would contend with both and re-baseline the output lock for no
measurable gain. No weight sweep. No change to `w-a02d28`, whose unblock
condition is objective and unchanged. No attempt to close this item: the
milestone's second condition is still unmet on its own merits, and closing it
on the strength of a refutation would be exactly the "done" that is not done.

### Next action for a later fresh pass, in priority order

1. **`7b2d40`.** Read its criterion-1 table on `8cc71c4`. If the canonical
   clue's within-structure rank against the cap is the binding number, the fix
   is a selection-policy question with a named cap; if the score deficit is,
   hand it to [w-2e5b93](w-2e5b93.md) with the numbers and start that item —
   its gate ("not while the reach front is delivering") is now lifted for good,
   because no reach front remains.
2. **Close `w-2e5b93`'s stale notes.** Its "re-take the spread table on a
   settled pool" instruction is now satisfiable: the pool is settled on
   `8cc71c4`, and [w-c1d3a7](w-c1d3a7.md) §8 supplies a per-axis deficit
   breakdown for this clue that the spread table should be consistent with.
3. **Re-measure the first canonical condition** after any selection or score
   change: `wreck a nice beach` currently holds at raw rank 27 and is the
   thing most likely to be traded away by a change made to reach the second.
4. Only after `approximate_finds_classic_madgab_resegmentation` passes on
   `post-milestone-acceptance` on its own merits, with no phrase-specific case
   in `src/`, close this item and [w-a02d28](w-a02d28.md) together and
   re-check every milestone condition in
   [../skills/itinerary-madgab.md](../skills/itinerary-madgab.md), including
   that the work is on `post-milestone-acceptance` and not `main`.

## Pass 2026-09-26 (coordinator `coord-3b0f`): the selection front is integrated and closed as a refutation; the residual is a 0.0376 score deficit

One thing changed on the accumulation branch this pass, and it changes the shape
of the remaining job rather than its size.

**Integrated and pushed: `a8bfc27`**, the `--no-ff` merge of
`madgab-cap-front-7b2d40` ([w-7b2d40](w-7b2d40.md), commits `c202663` and
`352f1d1`). The diff is docs-only — this file's sibling
[w-3f9c02](w-3f9c02.md) and the cap front's own record — so there is no
production region in which phrase-specific hard-coding could have been
introduced, and no selector, retention or `axes::*` line moved. Review detail
and the post-merge scratch check are in that item. No re-validation was run,
because a docs-only merge cannot change `--lib`, `corpus_integration`,
`exact_determinism` or `approx_determinism`.

**What the measurement settles.** The premise of
[w-7b2d40](w-7b2d40.md) was that something in selection bounds a proposal's
visibility. It is not the share cap, and the control is the useful part: the
canonical alignment injected at **within-structure rank 2** under **cap = 17**
is still invisible. The only gate left is the **fill rank**, 49-101 against
pools of ~16k, and the clue sits at global rank 9,517 of 18,066. So the whole
"represent more structures" / "relax the cap" family is refuted before a front
is spent on it, and this item's blocker stays at one:
`approximate_finds_classic_madgab_resegmentation`.

**Where the residual actually is.** The canonical structure `[3,10,13,15]` is
**#94 of 333** by best-member score, best member **0.878128** against a
**0.915692** cutoff. That is the honest statement of the remaining job: not the
canonical clue's own 0.8199, but a **0.0376 family-level** deficit that has to
be closed by the *best* wording that resegmentation can produce. The
residual is therefore a scoring question, filed as [w-3f9c02](w-3f9c02.md) by
the front that was fenced out of fixing it.

### State of the fronts

- `2e5b93` ([w-2e5b93](w-2e5b93.md)) — **running**, in
  `/workspace/madgab-scorespread` on `madgab-score-spread`, clean tree, no
  commits yet. It is measuring the per-axis spread table, which is the right
  next measurement and is now the only live front. Left running, with a
  non-blocking cross-check recorded in its item: the table has to explain a
  0.0376 deficit, so "the two flat axes are flat" is true but not sufficient.
- [w-3f9c02](w-3f9c02.md) — `open`, deliberately **unclaimed**. It is the same
  scoring territory `2e5b93` is measuring, and two fronts moving scoring at
  once would contend on the same lines and re-baseline the output lock for no
  measurable gain. The claim order for the next pass is written into that item:
  read the table, then either claim it for the fix or close it as a
  refutation.
- No new front was opened, and none should be. Both remaining questions are
  owned, and the measurement that would justify a third `src/`-touching front
  does not exist yet.
- [w-7b2d40](w-7b2d40.md) is `done` as a refutation, with its numbers, its
  scratch harness, and the routing to `w-3f9c02` all durable.

### Hazard for a host with `gh`: pull request #3 tracks the accumulation branch into `main`

`refs/pull/3/head` on the remote is **`a8bfc27`** — the current head of
`post-milestone-acceptance` — and `refs/pull/3/merge` exists, so the pull
request is open and GitHub considers it mergeable. Its base is expected to be
`main`. Per [../skills/itinerary-madgab.md](../skills/itinerary-madgab.md),
scheduled work must never be merged or pushed into `main`, so **this pull
request must be closed, not merged.** This host has no `gh` binary, so it
cannot be closed from here. A later pass on a host with `gh` should run
`gh pr close 3` (and confirm the base ref first with
`gh pr view 3 --json baseRefName,state`) and record the closure here. Until
then this is the single largest standing risk to the branch policy in this
itinerary, and it is the one thing a human may need to do.

### Next action for a later fresh pass, in priority order

1. **`2e5b93`.** Read its per-axis spread table against the cross-check in
   [w-2e5b93](w-2e5b93.md) and the deficit breakdown in
   [w-c1d3a7](w-c1d3a7.md) §8. Then claim [w-3f9c02](w-3f9c02.md) for the fix
   if the table puts the residual on the score side, and assign an agent to it
   in its own worktree. One scoring front at a time; the fences on both items
   stay until the table lands.
2. **Do not open a selection front.** [w-3f9c02](w-3f9c02.md) criterion 1 and
   [w-7b2d40](w-7b2d40.md) §"the refutation" close it by measurement, and any
   proposal in that family must now explain how it moves the **fill rank**.
3. **Close pull request #3** if a host with `gh` is available (see the hazard
   above).
4. **Re-measure the first canonical condition** after any scoring change:
   `wreck a nice beach` for `recognize speech` holds at raw rank 27 and is the
   thing most likely to be traded away by a change made to reach the second.
   `approximate_finds_recognize_speech_resegmentation` must stay green.
5. Only after `approximate_finds_classic_madgab_resegmentation` passes on
   `post-milestone-acceptance` on its own merits, with no phrase-specific case
   in `src/`, close this item and [w-a02d28](w-a02d28.md) together and
   re-check every milestone condition in
   [../skills/itinerary-madgab.md](../skills/itinerary-madgab.md), including
   that the work is on `post-milestone-acceptance` and not `main`.

### Objective confirmation on this pass's own head (`908194f`)

Re-measured by this coordinator rather than copied forward, so the milestone
claim in this item never rests on a carried number:

```text
cargo test --release --test corpus_integration approximate_finds
  approximate_finds_recognize_speech_resegmentation  ok
  approximate_finds_classic_madgab_resegmentation    FAILED (still)
  (2.15s; visible list for its target is still the
   "it justice too <X> aim" wordings, unchanged and verbatim)
```

So the first canonical condition is intact on the integrated head, the second
is unmet, and this item stays `working` with one blocker. The suite was not
re-run beyond this filter: `a8bfc27` is docs-only, so `--lib`,
`exact_determinism` and `approx_determinism` cannot have changed.

## Pass 2026-09-26T16:20Z (coordinator `coord-7e21`): the score side is settled and the fix front is assigned

This pass moved the residual from "measured" to "assigned", and re-claims this
item from `coord-7d3f1` to `coord-7e21`. `post-milestone-acceptance` is at
`662b57e` and pushed; `main` is untouched (there is still no `main` ref on
this host); this branch remains the only accumulation branch.

**The measurement that unblocked the queue.** `2e5b93`'s per-axis spread
table, over the top 2000 candidates of ten real targets, confirms the
cross-check this item's previous pass demanded rather than contradicting it:
the axes that discriminate between a sayable clue and a function-word salad
are `SIMILARITY` (weight 0.25), `FAMILIARITY` (0.10) and `CLOSED_CLASS`
(-0.15), while the axes carrying the most weight are flat — `RHYTHM` 0.30 at a
quartile spread of 0.0019, `WORD_NOVELTY` 0.15 at -0.0003, `NOVELTY` 0.15 at
0.0001, `SHAPE` 0.05 at 0.0001. The full table, the boundary-novelty-level
aggregate, and the family decomposition are in
[w-3f9c02](w-3f9c02.md)'s `coord-7e21` section, which records them so the
next pass does not depend on an agent log.

That aggregate is the useful part for this item's blocker, because it
explains the **family-level** 0.0376 rather than the canonical clue's own
0.8199. Candidates one boundary-novelty step down lose 0.011-0.015 of total
score while the mean of the other six axes is 0.017-0.037 *higher* for them,
and the canonical structure's own best member is exactly that case: 0.878128
against a 0.915691888 cutoff, its whole deficit one 0.05 novelty step with
+0.0145 net from everything else. So the axis that charges a clue for not
reproducing a boundary cannot distinguish "divergent" from "longer than the
visible competition".

**Actions taken.**

- [w-3f9c02](w-3f9c02.md) is **claimed** (`coord-7e21`, branch
  `madgab-score-axes`, worktree `/workspace/madgab-score-axes`) and **assigned
  to agent `3f9c02`**, which is now running. Its deliverable is a landed,
  general change to how the boundary-novelty axis charges a clue, argued
  from the population table, plus a regression test at the API or executable
  boundary. All of that item's fences travel with the assignment: no
  phrase-specific case, no relaxed test, no `zz_`/`MADGAB_` instrumentation on
  the branch, no `axes::*` sweep as the fix, no `src/adjacency.rs`, and a
  re-baselined `approximate_output_is_locked` must name the structures that
  moved. A reweighting alone is explicitly not the ask, because the table
  says a family can lose one step of one axis and win on the other six.
- `2e5b93` ([w-2e5b93](w-2e5b93.md)) was **prompted non-blockingly and left
  running** to make its table durable — commit, push `madgab-score-spread`,
  close its own item with the numbers, change no `src/` and land no weight
  move — so the measurement front and the implementation front do not contend.
- A fresh worktree and branch were created for the fix front; no finished work
  was available to integrate, so nothing was merged on this pass.

**Blocker count is unchanged at one**, `approximate_finds_classic_madgab_resegmentation`.
It is now owned by a running agent rather than by a measurement, which is the
first time that has been true since the reach family closed.

### Standing hazard, unchanged: pull request #3

`refs/pull/3/head` tracks the accumulation branch and GitHub considers the
pull request mergeable, so it must be **closed, not merged**. This host still
has no `gh` binary, so a later pass on a host that does must run
`gh pr view 3 --json baseRefName,state` and then `gh pr close 3`, and record
the closure here. Until then it remains the largest standing risk to the
branch policy in this itinerary.

### Next action for a later fresh pass, in priority order

1. **`3f9c02`.** Read its diff as a diff for generality — the mechanism must
   still penalise genuinely divergent clues, no phrase-specific case, no
   `zz_`-prefixed scaffolding, no `src/adjacency.rs`, no relaxed test — then
   run the four suites on the integrated tree and the six-target wall clock
   from [w-3f9c02](w-3f9c02.md) criterion 5, and integrate onto
   `post-milestone-acceptance` only if all of that holds. If it reports a
   refutation with numbers instead, close [w-3f9c02](w-3f9c02.md) the way
   `w-5f1c04` and `w-6b2f04` were closed and let the itinerary's standing
   goal pick the next front.
2. **The first canonical condition is the guard.** Re-measure
   `wreck a nice beach` for `recognize speech` (raw rank 27 today,
   `approximate_finds_recognize_speech_resegmentation` green) after any
   scoring change lands. It is the condition most likely to be traded away by
   a change made to reach the second.
3. **`2e5b93`** should end `succeeded` with `w-2e5b93` `done` and
   `madgab-score-spread` pushed. If it is still `working` with a clean tree
   and a finished job, close the item yourself rather than waiting again.
4. **Close pull request #3** on a host with `gh` (see the hazard above).
5. Only after `approximate_finds_classic_madgab_resegmentation` passes on
   `post-milestone-acceptance` on its own merits, with no phrase-specific case
   in `src/`, close this item and [w-a02d28](w-a02d28.md) together and
   re-check every milestone condition in
   [../skills/itinerary-madgab.md](../skills/itinerary-madgab.md), including
   that the work is on `post-milestone-acceptance` and not `main`.

## Pass 2026-09-26T15:25Z (coordinator coord-5f4c): blocker count down to one
front, and the item is re-claimed

Re-claimed from `coord-7d3f1`, whose pass is the 15:20Z one above. No source
change and nothing integrated on this pass, because the only live source work
is in flight. The blocker count is unchanged at **one**
(`approximate_finds_classic_madgab_resegmentation`), but it is now carried by
**two independent fronts** rather than one, which is the useful change:

* **[w-2e5b93](w-2e5b93.md) is now `done`.** Agent `2e5b93` finished; its
  per-axis spread table is recorded on this branch and closes the item's own
  fork in favour of the score side, with two corrections that matter: the
  `04f83f` "0.0000 range" reading is a top-2000 window artefact (over the whole
  pool `FAMILIARITY` is the *finest*-grained axis), and the 0.0376
  family-level residual is entirely one `NOVELTY` step of 0.0500, with
  `SIMILARITY` and `SHAPE` on the family's side. The `w-c1d3a7` §8 familiarity
  deficit does not survive at family level. So a reweighting is not the fix and
  this item's criterion "no `axes::*` sweep" was right.
* **[w-3f9c02](w-3f9c02.md) is working**, agent `3f9c02`, branch
  `madgab-score-axes`, and owns the *form* of the boundary-novelty axis, which
  is what `2e5b93`'s table says the fix has to be.
* **[w-8a1d47](w-8a1d47.md) is new, claimed and launched this pass**, agent
  `b4e5a7` in `/workspace/madgab-fill-rank` (branch `madgab-fill-rank`). It
  owns the other half: `select_diverse` spends all 50 slots by global rank
  ~101 in a single descending-score walk, so a candidate of a structure the
  list has not seen yet is never examined even when it is far under its share
  cap — which is the measurement behind "the fill rank is the only gate left".
  Fenced away from `axes::*`, `boundary_novelty`, `src/adjacency.rs` and the
  enumeration constants, so it does not contend with `3f9c02`.

`3f9c02` is 7 minutes into its run and was left running untouched: it has the
measurement it needs and a fence it must not cross, and a second prompt this
pass would only add noise. Its worktree is on `scratch/3f9c02-measure` with an
instrumented `src/lib.rs`; the item records `madgab-score-axes` as the
implementation branch, and that is where a landed change must appear — the
`zz_`/env-gated harness must not.

The pull-request #3 hazard in this item's notes is unchanged: this host still
has no `gh` binary, so that stays a human step.

### Next action for a later fresh pass, in priority order

1. **`3f9c02` and `b4e5a7`**, either order, and do not wait for both. Review
   each diff for generality (mechanism still penalises what it is supposed to;
   no phrase-specific case; no `zz_` scaffolding; no relaxed or re-baselined
   test; the other's fence respected), run `cargo test --release --lib`,
   `--test corpus_integration`, `--test exact_determinism` and
   `--test approx_determinism` on the integrated tree plus the six-target wall
   clock from [w-3f9c02](w-3f9c02.md) criterion 5, and integrate each onto
   `post-milestone-acceptance` — never `main`.
2. Re-measure both canonical examples after each integration, not once at the
   end. The first is a guard: `wreck a nice beach` for `recognize speech` is
   green today and is the condition most likely to be traded away.
3. Check each finished agent's HEAD is contained in its named branch before
   treating the work as safe; `04f83f0` once finished on a detached head and
   the commit was unreferenced.
4. Close this item and [w-a02d28](w-a02d28.md) together only once
   `approximate_finds_classic_madgab_resegmentation` passes on
   `post-milestone-acceptance` on its own merits with no phrase-specific case
   in `src/`, then re-check every milestone condition in
   [../skills/itinerary-madgab.md](../skills/itinerary-madgab.md).

## Pass 2026-09-26T15:35Z (coordinator coord-9d3a): no source change; the two
fronts are made to compose, and one criterion conflict is resolved

Re-claimed from `coord-5f4c`. No source change on this pass and nothing
integrated, because there is nothing finished to integrate: both live source
agents are mid-run. The blocker count is unchanged at **one**,
`approximate_finds_classic_madgab_resegmentation`. This item's branch is
`post-milestone-acceptance`, its worktree `/workspace/madgab`, and it is clean
and pushed at `fc5296f`; `origin` matches local exactly. `main` does not exist
on this host, local or remote, so the "never merge scheduled work into `main`"
condition holds trivially and the accumulation branch is unambiguous.

**State confirmed on arrival, not assumed.** A sweep of every local branch for
commits not in the accumulation head found no finished source work: the five
unmerged commits on `madgab-adjacency` are docs-only against a
merge-base-diverged branch, and `src/adjacency.rs` is byte-identical (592
lines) on both trees; `madgab-fuzzy-cost` is an old branch whose `src/` diff is
entirely the *absence* of later work; and
`madgab-score-spread`'s single commit `d63cded` is a 405-line addition to
[w-2e5b93](w-2e5b93.md) that the accumulation head already carries in a later
form. Nothing to review, so nothing was reviewed.

**Objective re-measurement on `fc5296f`**, taken by this coordinator rather than
carried forward from the previous pass, because the milestone claim should
never rest on a copied number:

```text
cargo test --release --test corpus_integration approximate_finds
  approximate_finds_recognize_speech_resegmentation  ok
  approximate_finds_classic_madgab_resegmentation    FAILED
  visible list for its target is still "it justice too <X> aim" wordings
  1 passed; 1 failed; 8 filtered out; 2.24s
```

Both agents are alive and were left running, each steered with the fact the
other one produced, because until now the two fronts had each been told only
that the other existed:

* **`3f9c02`** ([w-3f9c02](w-3f9c02.md), 18 minutes in) — its axis sweep
  produced the table that decides this milestone, and it had not been
  connected to the selection front. Given: a coverage-first admission order can
  only reach the top-`top_n` structures by best member, so the canonical
  structure's **best-member rank** — 3136 under the axis as it stands, 41 under
  the flat form, 38-62 under the other variants — is the quantity that decides
  visibility, and only the flat form also clears the 0.9157 cutoff. Told
  explicitly that the deliverable is a *form* and not a removal, because flat is
  the ceiling of the effect rather than a resting point, and told that the guard
  test's pool rank moves from 42 to 209-3819 across its own variants, so the
  first canonical condition must be measured per variant rather than assumed.
* **`b4e5a7`** ([w-8a1d47](w-8a1d47.md), 9 minutes in) — it stopped on a real
  contradiction: its change necessarily moves the visible list, so
  `approximate_output_is_locked` fails, and its own criterion 2 forbade
  re-baselining. Resolved in this pass and recorded durably in that item: the
  lock **may** be re-baselined with a record of which structures moved, which is
  [w-7b2d40](w-7b2d40.md) criterion 6 and the lock's own documented history of
  two prior re-baselines for intended selection and scoring changes; relaxing
  either acceptance test still may not. Also given the same best-member-rank
  table, with the instruction not to tune selection until the clue appears,
  since a red acceptance test is not a refutation of a change that cannot reach
  rank 3136.

The one hazard this pass did **not** clear, recorded so a later pass does not
assume it away: `/workspace/madgab-score-axes` is on `scratch/3f9c02-measure`
with an instrumented `src/lib.rs`, while
[w-3f9c02](w-3f9c02.md) records `madgab-score-axes` as the implementation
branch. That is the shape of the `04f83f0` detached-head loss. Both agents were
told to land on their named branch and to keep the `MADGAB_*`-gated harness and
any `zz_` scratch test off it. Still no `gh` binary on this host, so the
pull-request #3 hazard in this item's notes remains a human step.

No third front was opened, deliberately. Every candidate the measurement
suggests now belongs to `axes::*` (the closed-class form, which the
[w-2e5b93](w-2e5b93.md) table shows is the largest real discriminator) or to
`select_diverse` (any other admission rule), and a third front on either
territory would contend with a live agent rather than compose with one. The
useful parallelism this pass is two fronts plus the cross-front table that
makes them additive.

Next action for a later fresh pass, in priority order:

1. Review whichever of `b4e5a7` and `3f9c02` lands first, as a diff for
   generality, on the terms recorded in [w-8a1d47](w-8a1d47.md) and
   [w-3f9c02](w-3f9c02.md). Run `cargo test --release --lib`,
   `--test corpus_integration`, `--test exact_determinism`,
   `--test approx_determinism` and the six-target wall clock, then integrate
   onto `post-milestone-acceptance` — never `main`. Do not wait for the second
   front.
2. Re-measure both canonical examples after **each** integration.
3. Check each finished agent's HEAD is contained in its named branch before
   treating its work as safe.
4. Close this item and [w-a02d28](w-a02d28.md) together, and re-check every
   milestone condition in [../skills/itinerary-madgab.md](../skills/itinerary-madgab.md),
   only once the acceptance test passes on its own merits with no
   phrase-specific case in `src/`.

## Pass 2026-09-26T23:59Z (coordinator coord-4c17): the score front is closed
as a refutation; the last unmeasured candidate is the *composition*

No source change on this pass and nothing integrated, because there was nothing
finished to integrate. This item is re-claimed from `coord-9d3a` to
`coord-4c17`. Blocker count is unchanged at **one**,
`approximate_finds_classic_madgab_resegmentation`. `post-milestone-acceptance`
was clean, pushed at `577a17f`, and identical to `origin` on arrival;
`main` does not exist on this host, local or remote.

**State confirmed on arrival, not assumed.** A sweep of every local branch for
commits not in the accumulation head found no finished source work. All of the
ahead-commits are docs-only or scratch: `madgab-adjacency`'s five, `tmp`,
`wip/*`, `archive/*`, and the four `scratch/*` measurement branches. The two
live worktrees were checked individually for the detached-head hazard: both are
on a **named branch**, and `/workspace/madgab-score-axes` is on
`madgab-score-axes` at `be589ca` with `src/` clean after the agent restored it
off `scratch/3f9c02-measure`. So no work is at risk on this pass.

**Front 1 is finished and refuted: [w-3f9c02](w-3f9c02.md) is `done`.**
Agent `3f9c02` closed it at `0e33fb5` on `madgab-score-axes` (pushed; `src/`
and `tests/` diff against the accumulation head is empty, so nothing to
review there). Its finding is the useful output of this pass: the canonical
structure's best-member rank does move, from 3136 to **41** under a saturating,
partially length-invariant acoustic charge — a threshold charge instead of the
current per-unit rate — which clears the top-50 fill rank and so satisfies the
[fill-rank measurement](w-8a1d47.md) that [w-7b2d40](w-7b2d40.md) established as
the real gate. But that form also breaks the *first* canonical condition, so no
monotone reweighting lands: under F1 the `recognize speech` wording survives
only at best-member rank 100, and under F2 it is not enumerated at all (pool
19772 against 19868) — a search perturbation, not a ranking one. F1 also
regresses `the cat sat on the mat` 1.98 s -> 2.83 s; F2 holds the baseline.
Baseline re-measured on the clean head by the agent, not copied forward:
`--lib` 50/0, `corpus_integration` 9 passed / 1 failed (this item's target),
`exact_determinism` 1/0, `approx_determinism` 2/0, six-target wall clock
unchanged.

**Front 2 was not left to close as a second independent refutation.** Agent
`b4e5a7` had reached the same "land nothing" conclusion, on a different and
correct ground: on the current head strict coverage-first admission and the
first-canonical guard are *arithmetically* incompatible, with the crossing at
coverage 25 of 50, and no principled derivation of the policy lands at or below
25. Taken together, this pass's two fronts say something the two items do not
say individually: **the score change that makes the second clue reachable
breaks the first clue's guard, and the selection change that keeps the first
clue green cannot itself reach a best member at rank 3136.** Neither front is
wrong. What has never been measured is the pair, and because
[3f9c02](w-3f9c02.md) measured the other front's enabling number to be 41 —
inside the top 50 — the composition is the only remaining candidate that can
satisfy the itinerary, which requires *both* clues visible at once.

So `b4e5a7` was reassigned, and this is the one substantive decision of the
pass:

- its collision boundary in [w-8a1d47](w-8a1d47.md) criterion 3 (no `axes::*`
  edits while [w-3f9c02](w-3f9c02.md) is live) is **re-authorized for
  measurement and for one landed commit**, now that the other front is closed
  and its F1/F2 forms are recorded patch-level rather than held as an
  uncommitted experiment. It is still not a licence to sweep axis weights;
- deliverable, in order: a **2x2 table** on the current head, release build,
  `top_n` 50, for both canonical conditions — {axis as-is, axis+F2} x
  {coverage-first on, off} — reporting per cell and per target the pool size,
  the requested wording's visible rank, its within-structure rank, the
  structure's best-member rank, and green/red. A follow-up steer corrected
  F1 to F2, since F1 carries a 1.4x runtime regression on one target and F2 is
  the form the other front recommends;
- if a cell is green on **both** conditions with no axis weight moved and no
  acceptance test relaxed, land it as one commit on `madgab-fill-rank` and
  argue it from the mechanism, never from either phrase. Re-baselining
  `approximate_output_is_locked` remains permitted with a record of which
  structures moved; relaxing either acceptance test remains forbidden;
- if no cell is green, close [w-8a1d47](w-8a1d47.md) as a refutation carrying
  that table. Three refutations, two of them
  independent and one joint, is a much stronger negative result than either
  alone, and it is a legitimate end state for the standing goal;
- durability first: its coverage-first patch is still **uncommitted** in
  `/workspace/madgab-fill-rank/src/lib.rs` and is the only copy, so it was told
  to commit it to a scratch branch and push it before spending more turns.

Both agents were left running. No third front was opened: every remaining
candidate territory is `axes::*` or `select_diverse`, which is exactly where
the one live agent is now working, and a third front there would contend rather
than compose. The parallelism that is useful this pass is the single agent plus
the cross-front table, which is what makes the two results additive.

[../continuation-approximate-search.md](../continuation-approximate-search.md)
remains `superseded` by this item. Still no `gh` binary on this host, so the
pull-request #3 hazard in this item's notes remains a human step.

Next action for a later fresh pass, in priority order:

1. `b4e5a7`. If it landed a composition, review it as a diff for generality —
   the argument must come from the fill-rank measurement and the threshold-charge
   property, not from either phrase; no acceptance test relaxed; the re-baselined
   lock accompanied by a record of which structures moved — then run `cargo test
   --release --lib`, `--test corpus_integration`, `--test exact_determinism`,
   `--test approx_determinism` and the six-target wall clock, and integrate onto
   `post-milestone-acceptance`, never `main`.
2. If it returned only the 2x2 table, treat the table as the front's result and
   let a later pass file the next item from it; do not re-run the sweep.
3. Check its HEAD is contained in `madgab-fill-rank` before treating the work as
   safe, and confirm its scratch instrumentation stayed off that branch.
4. Close this item and [w-a02d28](w-a02d28.md) together, and re-check every
   milestone condition in [../skills/itinerary-madgab.md](../skills/itinerary-madgab.md),
   only once the acceptance test passes on its own merits with no
   phrase-specific case in `src/`.

## Pass 2026-09-26T16:42Z (coordinator coord-7a10): both fronts are closed as
refutations, and the blocker is enumeration

No source change on this pass. The last two fronts finished, both as
refutations, and between them they relocate the milestone's last blocker out
of scoring and out of selection and into **lexical enumeration**. Blocker
count is unchanged at **one**,
`approximate_finds_classic_madgab_resegmentation`.

State confirmed on arrival, not assumed: `post-milestone-acceptance` was clean
and identical to `origin` at `0928b03`; `main` does not exist on this host,
local or remote; every branch ahead of the accumulation head is docs-only or
scratch; the one live worktree (`madgab-fill-rank`) was on a **named branch**,
so the detached-head hazard is absent; and `scratch/8a1d47-measure` was already
on `origin` at `53bbd17`, so the coverage-first patch and the F1/F2 ports are
durable and merged nowhere.

**The decisive measurement, from `w-8a1d47`'s close-out** (merged here as
**6f1022a**, item file only — `git diff fc5296f -- src tests` on
`madgab-fill-rank` is empty, so there was no source diff to review). On release
builds of `fc5296f`, `--approximate --top 50`, pools dumped from the selector's
own input:

```text
It's just a stupid game, clean head:  pool 18065  structures 333
  hits 56   justice 7099   dupe 0   hid 18   came 157
cells (coverage-first off / on)     pools byte-identical, 18065 / 17973
  canonical structure [3,10,13,15] best-member rank: 94 clean, 63 under F2
```

The requested wording is **not enumerated in any cell of the 2x2**, and the
pool does not change at all when the admission order changes. That is
stronger than either front's own conclusion: it does not merely say the
fronts are individually insufficient, it says the candidate does not exist to
be selected. So:

* `w-3f9c02` (score) stays `done` and refuted — a threshold acoustic charge
  moves the family's best-member rank 3136 -> 41, inside the top-50 fill rank,
  but breaks the first-canonical guard;
* `w-8a1d47` (selection) is now `done` and refuted — coverage-first admission
  does what its property says (5-11 visible structures -> 50 on all six
  targets, fill rank 51-102 -> 490-1812, `approx_determinism` green, no wall
  clock regression) and still cannot show a wording that is not in the pool,
  while it trades the guard away. Its patch stays parked on
  `scratch/8a1d47-measure`; landing it is a policy call no measurement has
  earned, and it is not landed.

Coordinator re-measurement on `0928b03` with the release binary, for the record
rather than copied forward:

```text
recognize speech:  raw phrase="wreck a nice beach" rank=27 score=0.918313383
                   raw_cutoff rank=49 score=0.917045726   (search 1.63s)
It's just a stupid game:  raw phrase="hits justice dupe hid came" missing
                   candidates=18065  raw_cutoff 0.915691888  (search 1.71s)
```

**The one thing this pass adds, and it is a warning rather than a result:**
`w-4b1e07`'s own 11:52Z pass records per-slot ranks of **3 / 2 / 10 / 99 / 11**
over the 160-wide per-span shortlist, which puts `dupe` *inside* the shortlist
at rank 10. That is irreconcilable with 0 emissions, and the same class of
error has already been retracted once in this queue — `w-7b41d2`'s reachability
claim, withdrawn at 14:10Z for measuring a harness whose budget was wider than
the default path. Reachability evidence gathered off the default path, or from
an instrumented harness, is not admissible here. The quantity that counts is
membership of the **default-path deduplicated pool**, read off a release build.
That rule is written into the successor item rather than left in a chat.

**Opened and launched: [w-c4e8d7](w-c4e8d7.md)**, on a worktree
`/workspace/madgab-enum-reach`, branch `madgab-enum-reach` from `6f1022a`, agent
`c4e8d70` **running** and left running. It owns lexical enumeration and nothing
else — `FuzzyLexicon::matches_at` and the per-span shortlist, the per-slot
lists, the best-first product search, `LEXICAL_BRANCH_KEEP`, `SPAN_SHORTLIST`,
`SEGMENTATION_KEEP` and the emission budget — which is uncontested now that both
`axes::*` owners and the selector front are closed. Its first deliverable is
diagnosis by line on the default path, before any code change, and its
sequencing rule is written down: if membership goes non-zero and the wording is
still invisible, that is a *successful* end state for the fix, and the item
reports the new best-member rank and fill rank and stops rather than
re-litigating selection.

No third front was opened on the closed territories, and the parked
coverage-first patch was deliberately not landed. `[../continuation-approximate-search.md](../continuation-approximate-search.md)`
remains `superseded` by this item. Still no `gh` binary on this host, so the
pull-request #3 hazard in this item's notes remains a human step.

Next action for a later fresh pass, in priority order:

1. `c4e8d70`. Its line-level diagnosis is the milestone. If it has landed a
   fix, review it as a diff for generality — the rejection must be gone, no
   phrase-specific case, no `axes::*` or `select_diverse` edit, no
   `zz_`/`MADGAB_*` scaffolding on `madgab-enum-reach` — then run
   `cargo test --release --lib`, `--test corpus_integration`,
   `--test exact_determinism`, `--test approx_determinism` and the six-target
   wall clock, and integrate onto `post-milestone-acceptance`, never `main`.
2. Check its HEAD is contained in `madgab-enum-reach` before treating its work
   as safe; `04f83f0` once finished on a detached head and its commit was
   unreferenced by any branch.
3. Only once `approximate_finds_classic_madgab_resegmentation` passes on
   `post-milestone-acceptance` on its own merits, with no phrase-specific case
   in `src/`, close this item and [w-a02d28](w-a02d28.md) together and
   re-check every milestone condition in
   [../skills/itinerary-madgab.md](../skills/itinerary-madgab.md).

## Pass 2026-09-26T16:58Z (coordinator `coord-3f9a`): the blocker is no longer
membership, and this item's own reachability contradiction is resolved

No source change on this pass and nothing integrated. Re-claimed from
`coord-4c17`. `post-milestone-acceptance` was clean and identical to `origin` at
`95f7a6c`; `main` does not exist on this host, local or remote, so the
"never merge scheduled work into `main`" condition holds trivially and this
branch remains the only accumulation branch. A sweep of every local branch for
commits not in the accumulation head found no finished source work to review:
the ahead-commits are docs-only, scratch probes, or the live agent's
`a9c141a` "wip: uniform coverage sweep", which is mid-work by its own title.

Objective re-measurement on `95f7a6c`, taken by this coordinator with the
release binary rather than copied forward, because this item's milestone claim
should never rest on a previous pass's number:

```text
cargo test --release --test corpus_integration approximate_finds
  approximate_finds_recognize_speech_resegmentation  ok
  approximate_finds_classic_madgab_resegmentation    FAILED (still)
  1 passed; 1 failed; 8 filtered out; 2.43s
recognize speech:        raw phrase="wreck a nice beach" rank=27 score=0.918313383
                         raw_cutoff rank=49 score=0.917045726   (search 1403ms)
It's just a stupid game: raw phrase="hits justice dupe hid came" missing
                         candidates=18065  raw_cutoff 0.915691888  (search 1675ms)
```

Blocker count is unchanged at **one**,
`approximate_finds_classic_madgab_resegmentation`, and the runtime criterion
still has headroom (1.4 s and 1.7 s against this item's 9.5 s / 21.3 s
starting figures).

**What changed is the kind of the blocker, and it is a real change.** The
enumeration front [w-c4e8d7](w-c4e8d7.md) has, in measurement, moved the
quantity this item has been chasing for eight passes: on its instrumented tree
the requested wording's middle word goes from **0 to 14 pool candidates**, so
the wording is now *enumerated*. Two consequences worth recording here because
they bear on this item rather than only on that one:

* This item's own 11:52Z warning — that per-slot ranks of 3/2/10/99/11 are
  irreconcilable with zero emissions, and that the same class of error was
  retracted once already in this queue — is **resolved in favour of
  reachability**: the two numbers were never in conflict, because the traversal
  has changed underneath them. The admissibility rule written into `w-c4e8d7`
  did its job.
* The remaining gap is the **pairing**, not the membership. In the agent's own
  pool dump `dupe` is emitted with `eeg`, `uhh`, `add` and `gave`
  (`it justice dupe eeg aim` at `[2,11,13,16,19]`, `each justice dupe eeg gave`
  at `[3,11,13,16,19]`) but not with `hid` at the canonical cut. So the
  itinerary's completion condition — the requested clue appears in the
  generated approximate proposal set — is now within one slot of being
  reachable, and no scoring change and no selection policy is on the critical
  path any more.

`c4e8d70` was steered non-blockingly with facts it could not cheaply derive —
chiefly that `dupe` **is** inside the shipped corpus (rarity `40210` against the
default `max_rarity` of `50000`, so lexicon admission is not the defect and the
"cheaper explanation" needs no front of its own), the exact portfolio structure
and line numbers of the span shortlist at `src/lib.rs:696-858` and why its fill
pass inherits the cheap-and-familiar order, the named retention constants, and
the six closed-and-recorded fronts it must not re-sweep. Detail and the full
fact list are in [w-c4e8d7](w-c4e8d7.md)'s 16:58Z pass. It is **left running**
with `prompts: 2`; its probes are committed on the named branch
`scratch/c4e8d7-measure` (`c0ac549`) and its WIP on the named branch
`madgab-enum-reach` (`a9c141a`), so nothing is unrecoverable and the
`04f83f0` detached-head hazard is absent.

No second front was opened, deliberately: the only territories left are the one
this agent occupies and ones already refuted, and the itinerary warns against
manufacturing a backlog. The useful parallelism available right now is the one
live agent plus the durable record of everything it should not re-derive.

### Standing hazard, unchanged: pull request #3

`refs/pull/3/head` tracks the accumulation branch and GitHub considers it
mergeable, so it must be **closed, not merged**. This host still has no `gh`
binary; a later pass on a host that does must run
`gh pr view 3 --json baseRefName,state` and then `gh pr close 3`, and record the
closure here.

### Next action for a later fresh pass, in priority order

1. `c4e8d70` on `madgab-enum-reach`. Review its final commit as a diff for
   generality — the rejection must be gone, no phrase-specific case, no
   `axes::*` or `select_diverse` edit, no `zz_`/`MADGAB_*` scaffolding, no
   relaxed acceptance test, and any re-baselined `approximate_output_is_locked`
   accompanied by a record of which structures moved — then run
   `cargo test --release --lib`, `--test corpus_integration`,
   `--test exact_determinism`, `--test approx_determinism` and the six-target
   wall clock from [w-7b2d40](w-7b2d40.md), and integrate onto
   `post-milestone-acceptance`, never `main`.
2. Re-measure both canonical examples after that integration, not once at the
   end. The first is the guard: `wreck a nice beach` is green today at visible
   slot 46 of 50 and is the condition a pool-changing fix is most likely to
   trade away.
3. If the wording is still invisible once it is enumerated, take the reported
   best-member rank and fill rank as the input to a **new** item on selection,
   rather than reopening selection inside `w-c4e8d7`; that item's sequencing
   rule says so explicitly.
4. Only once `approximate_finds_classic_madgab_resegmentation` passes on
   `post-milestone-acceptance` on its own merits, with no phrase-specific case
   in `src/`, close this item and [w-a02d28](w-a02d28.md) together and
   re-check every milestone condition in
   [../skills/itinerary-madgab.md](../skills/itinerary-madgab.md), including
   that the work is on `post-milestone-acceptance` and not on `main`.
