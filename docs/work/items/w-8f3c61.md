---
work_item: true
id: w-8f3c61
state: working
priority: high
owner: agent-8f3c61
updated: 2026-09-26T12:25:00Z
branch: madgab-depth-order
worktree: /workspace/madgab-depth-order
---

# Reach a deep word in one slot by enumeration *order*, not by budget

## Goal

Make the canonical-clue blocker reachable by changing the **order** in which
the per-segmentation lexical enumeration visits slot combinations, so that a
wording which is deep in exactly one slot costs O(that depth) instead of
O(the product of the fan-outs). This is the same general question as
[w-6b2f04](w-6b2f04.md) — how the enumeration spends its budget — but a
different mechanism, and it is opened because the budget mechanism has been
refuted twice and is currently implemented in a shape that, by the arithmetic
below, cannot reach the clue.

## Context: what is already established, do not re-measure it

From [w-3e5c7](w-3e5c7.md) (independent audit, integrated as `24f3cf7`) and
[w-7b41d2](w-7b41d2.md):

- every one of the five canonical words survives the per-span shortlist
  (`SPAN_SHORTLIST = 160`); per-slot ranks are 6-8 / 0 / 10-18 / 99 / 11, and
  the "hid rank 85" and "hid rank 99" figures are the same word in two
  different orderings, not a disagreement;
- the canonical segmentation is retained by the structural DP at rank 151 of
  256; the matcher is innocent;
- forcing the canonical alignment to index 0 of **all five** slots, with that
  segmentation's emission and pop caps lifted, puts it at **emission 23962,
  pop 28930, pool rank 28573** of its own segmentation, against a real
  emission budget of 64 and a real per-segmentation pop limit of 4000;
- the blocker is therefore enumeration **depth** inside a single
  segmentation. Shortlist reordering, walk-rank ordering, per-slot caps,
  budget-derived per-slot caps, segmentation retention and pruning have all
  been implemented and refuted by measurement. A share rule that gives a
  structure's first alignment the whole allowance is *also* predicted to miss,
  because the canonical segmentation is rank 151 of 256.

## The mechanism this front owns

The per-segmentation search is a best-first product search over prefixes.
Its frontier has to grow by roughly the per-slot fan-out at every level, so
a combination that is deep in one slot can sit behind a large number of
better-bound prefixes at each of the levels above it. The cost of depth is
multiplicative in the levels.

The general fix is to change the **visit order** so depth in one slot is
additive rather than multiplicative. Concretely, the space of index tuples
`(i_0, ..., i_{n-1})` over the per-slot candidate lists should be visited in
an order that enumerates by *sum of slot indices* (a diagonal, or any
max-index-first stratification) rather than by best-first prefix bound, with
the emission and pop budget spent on that order. Under such an order the
canonical tuple (3/2/10/99/11, i.e. max index 99) is reached after roughly
`sum of indices` rounded work rather than after 28930 pops, and the same
property holds for any other target: a clue that needs one unusual word
becomes affordable.

Variants are all in scope and all equally valid: diagonal/stratified order
over index tuples, per-slot index round-robin, or keeping best-first for the
cheap majority and switching to stratified order once the frontier exceeds a
budget. Pick one, justify it by measurement, and record why the others were
rejected.

**Fence, to compose with [w-6b2f04](w-6b2f04.md) rather than duplicate it.**
That front owns the *budget*: `LEXICAL_GLOBAL_EMISSION_BUDGET`,
`LEXICAL_GLOBAL_POP_BUDGET`, `spent_emissions` / `spent_pops`,
`structure_wording_allowance`, `structure_depth_ceiling` and the
breadth-first per-structure schedule. This front must not edit those; it owns
the ordering of the frontier inside a segmentation and the fan-out that
produces it. If the two changes turn out to need the same lines, say so in the
item rather than silently merging them — the coordinator sequences them.
**Read [w-6b2f04](w-6b2f04.md) and its agent's log before writing code, and do
not re-run its sweep.**

## Completion criteria

- The canonical clue for the itinerary's second example is **enumerated** and
  visible from the real default path, measured from the release binary with
  `MADGAB_TRACE_PHRASES` and `--approximate` on the real target: report the
  rank, the candidate-pool size and the wall clock.
- Justified on a **spread of real targets**, not only the canonical pair:
  report search wall clock and pool size before and after for at least three
  targets besides the canonical two, including
  `congratulations on your promotion`.
- It still bounds something. Name what the previous cap bounded and what
  bounds it after the change, with the arithmetic. A fix that simply removes
  the bound is not acceptable, and neither is one that replaces a bound with
  an unmeasured magic number.
- **No phrase-specific hard-coding**: nothing in `src/` may match
  `wreck|beach|recognize|justice|stupid|dupe|came|hid` outside unit tests,
  comments and the pre-existing `src/main.rs` doc examples. Prove it with
  `git grep -i -E` on your own branch.
- No `axes::*` weight moves and no changes to the scoring functions; the
  blocker is enumeration shape, not the objective. Re-baselining
  `approximate_output_is_locked` is *not* expected; if it changes, that is a
  finding to report loudly.
- Temporary instrumentation, sweep scripts and survey tests (the earlier
  fronts' `zz_dump_canon` / `zz_scratch_analysis.rs` are the precedent) must
  **not** reach `post-milestone-acceptance`.
- Regression tests express useful external behaviour on a spread of targets:
  that approximate mode enumerates structurally diverse, sayable clues within
  its time budget, and that the enumeration's budget is bounded.
- Validation reported from the merged-equivalent tree, not copied forward:
  `cargo test --release --lib`, `--test corpus_integration`,
  `--test exact_determinism`, `--test approx_determinism`. Expect
  `approximate_finds_classic_madgab_resegmentation` in `corpus_integration` to
  pass; that is the point of this item. `cargo fmt`, `cargo clippy` and
  doctests do not exist on this host — see
  [../../environment-notes.md](../../environment-notes.md); do not report them
  as satisfied.
- Commit on `madgab-depth-order`, **push it**, rebase onto the current
  `post-milestone-acceptance` head before final validation (never merge or
  push to `main`), and leave the working tree clean with nothing valuable left
  uncommitted.

## Handoff / notes

Filed by coordinator pass `coord-5b7e` on 2026-09-26T12:25Z as a second,
independent front beside [w-6b2f04](w-6b2f04.md)'s agent `6b2f04`, which is
mid-implementation of the budget mechanism. Rationale: the milestone has now
consumed many passes resting on a single agent, and coordinator arithmetic on
that agent's own constants suggests its shipped shape cannot reach the clue —
it clamps each segmentation at 64 emissions and leaves the per-segmentation
pop limit at 4000, so no single segmentation can approach the 28930 pops the
audit measured, and the global budgets only redistribute the same totals.
That arithmetic is a hypothesis this front is free to confirm or refute by
measurement.

Ownership: branch `madgab-depth-order`, worktree `/workspace/madgab-depth-order`,
created from `25adfbe`. The worktree-wiped-clean hazard is real in this
repository: commit early, commit often, and check `git branch --contains
<your head>` before you finish — a finished agent's work on a detached HEAD is
unrecoverable from a later pass's point of view.
