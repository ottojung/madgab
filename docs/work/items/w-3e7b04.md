---
work_item: true
id: w-3e7b04
state: working
priority: normal
owner: coord-6a11
updated: 2026-09-26T19:25:00Z
branch: madgab-review-pool-3e7b04
worktree: /workspace/madgab-review-pool
agents: 3e7b04 (review only, report-only, opened by coord-6a11)
---

# Independent post-merge review of the pool-determinism fix `3d520c0`

## Goal

An independent, refutation-first review of the one code change merged into
`post-milestone-acceptance` since the search-side fronts were closed: the
candidate-pool run-to-run reproducibility fix `3d520c0` (from `db7fd5f` /
`d3b7c2`, report `e3823d3`, item [w-6b91d3](w-6b91d3.md), which is now `done`).
It is a **report**: `REPORT-3e7b04.md` pushed on `madgab-review-pool-3e7b04`.

## Why this is worth a pass now

`3d520c0` landed on the accumulation branch and was reviewed only by the agent
that wrote it and by one coordinator pass that recorded suite output. It is
nevertheless a change to tie-breaking and collection order inside
`coverage_tuples`, i.e. **it decides which of several equal-scoring candidates
is emitted**, and it moved the deduplicated pool by 1-2 candidates on three of
four measured targets. Every reachability claim in this repository is
denominated in pool membership, so the correctness and generality of that
change is load-bearing for all later measurement, not just for itself.

## The questions the review must answer

1. **Fence scan, re-run, not inherited.** `git diff 3d520c0^..3d520c0` — list
   every file, every added line. Report any added line containing an acceptance
   word or phrase (`wreck a nice beach`, `recognize speech`, `hits justice dupe
   hid came`, `it's just a stupid game`), or any `ZZ_` / `zz_` / `MADGAB_` /
   `env::var` scaffolding used to steer production behaviour. Also confirm no
   test was relaxed, re-baselined, skipped or `#[ignore]`d by this change, and
   that `tests/no_phrase_hard_coding` and both
   `approximate_finds_*` tests are untouched in the diff.
2. **Generality argument.** Argue from the mechanism why breaking a tie with a
   total order over existing candidate properties is a property of *all* inputs
   and not of the ones measured. Name the properties, and state plainly what
   class of input could be *hurt* by a deterministic tie-break (a systematically
   favoured first-seen order, for instance).
3. **Is the determinism claim true on the merged result?** Independent evidence
   that the default path's deduplicated pool is now run-to-run reproducible:
   at least 10 consecutive release runs of `madgab --approximate` on at least
   three targets including the canonical one, comparing the pool count, not just
   the visible top-50. Report the counts as a table with min/max, not a boolean.
4. **Re-take the noise floor on `3d520c0`**, and state it as a number the next
   front can use: the smallest pool delta and the smallest percentage-point delta
   that is decidable at your replicate count. `b47d02`'s older figure (~±25
   accepted tuples / ±0.06 pp) was measured on a non-deterministic pool and should
   be re-derived, not inherited.
5. **Regression check against the parent.** Pool membership and `--top 50` visible
   output, parent `3d520c0^` vs `3d520c0`, release, on at least six targets
   including `recognize speech` and the canonical target. Report any
   (target, word) pair present on the parent and absent at the tip — the failure
   mode that disqualified `3b14482` — and state explicitly whether
   `recognize speech` -> `wreck a nice beach` is still produced.
6. **Suites.** `cargo test --release --lib`, `--test no_phrase_hard_coding`,
   `--test corpus_integration`, `--test approx_determinism`, `--test exact_determinism`,
   on the branch, with a `git archive 3d520c0^` base arm where a failure needs
   separating into pre-existing red and new red.

## Verdict

`MERGE RECOMMENDATION:` one of `ALREADY-MERGED-SOUND`,
`ALREADY-MERGED-WITH-NOTES` (naming the notes), or `REGRESSION - REVERT
CANDIDATE` (naming the line and the mechanism). This is a review of something
already on the accumulation branch, so a sound verdict is the expected outcome;
say so plainly if the change is sound and do not manufacture a defect.

## Forbidden

No `src/` or `tests/` change on the branch: this front pushes **exactly one**
commit, `REPORT-3e7b04.md`. Probes and scratch arms live in untracked files or in
a `git archive` base arm elsewhere; if you need a probe, keep it untracked and
say so. No editing, relaxing, re-baselining, skipping or `#[ignore]`ing
`approximate_finds_classic_madgab_resegmentation`,
`approximate_finds_recognize_speech_resegmentation` or
`approximate_output_is_locked`. No phrase-specific, word-specific,
substring-specific or dictionary-lookup case. Do not merge anything, do not push
to or touch `main`, and do not touch the accumulation branch.

## Collision boundary

`558697` is live in `/workspace/madgab-axis-558697` on `madgab-axis-558697`
under [w-558697](w-558697.md) and is measuring on `3d520c0` or later. It lands no
code, so the two fronts cannot contend on a file; you may both read the same
commits. Its branch is being rebased onto `3d520c0`, so do not treat
`madgab-axis-558697`'s tip as a base.

## Completion criteria

1. `REPORT-3e7b04.md` **committed and pushed** on `madgab-review-pool-3e7b04`,
   reachable at `git show madgab-review-pool-3e7b04:REPORT-3e7b04.md`. Push is
   the deliverable.
2. All six questions answered with numbers, plus the verdict line.
3. Explicit statement of the commit measured, the targets, suite results, what
   you did **not** measure, and wall clock.
4. `git diff --name-only 6faf20d..HEAD` on your branch is `REPORT-3e7b04.md`
   and nothing else.

## Handoff / notes

Opened by coordinator `coord-6a11` at 2026-09-26T19:25Z from
`post-milestone-acceptance` at `6faf20d`, worktree `/workspace/madgab-review-pool`,
branch `madgab-review-pool-3e7b04` pushed at `92a5b23` before the agent started.

Do not re-derive the numbers this review is about; read them:

```sh
git show origin/madgab-pooldet-d3b7c2:REPORT-d3b7c2.md   # the fix's own evidence
git show 3d520c0 --stat                                  # the merged change
git show madgab-depth4-b47d02:REPORT-b47d02.md           # the old, now-stale noise floor
git show madgab-depthcap-c81e55:REPORT-c81e55.md         # guard margin, production rank
```

Host notes: `git -c commit.gpgsign=false commit`; `/tmp` is `noexec`, so build
under `/workspace` or `/tmp/opencode`; build a second arm with
`git archive <sha> | tar -x -C <dir>`; verify durability with `git ls-remote
origin`, not a local remote-tracking ref; `grep`, `sed` and `python3` are not on
this host's `PATH` — the usable binaries are under
`/gnu/store/2daxpqdg74pan6bp78cp6jzknb435w9d-coreutils-9.1/bin` and
`/gnu/store/05vy146ycavwjm8va1d8qym188s14vzv-grep-3.11/bin`, and `node` works for
scanning.

Next action for a later fresh pass: `antonina agent status --id 3e7b04`, then its
log, then the durability gate — `git ls-remote origin madgab-review-pool-3e7b04`
and `git show madgab-review-pool-3e7b04:REPORT-3e7b04.md`. A progress line or a
green suite is not a completion signal. If the verdict is `REGRESSION - REVERT
CANDIDATE`, the follow-up is a new work item, not a revert by a later pass.
