---
work_item: true
id: w-8a1d47
state: done
priority: high
owner: agent-8a1d47
updated: 2026-09-26T16:10:00Z
branch: madgab-fill-rank
worktree: /workspace/madgab-fill-rank
---

# The selector's leftover slots are spent on depth, not on unrepresented
# boundary structures: close the fill-rank gate

## Goal

Make `select_diverse` admit the best candidate of an **unrepresented**
boundary structure *before* it spends a slot on a second or third member of a
structure that already has one, so the 50 slots it hands out are decided by
structural coverage rather than by the top of the score order. This is the
general property behind the last remaining blocker on the acceptance target
and it is stated without reference to any phrase.

## Why this is the gate, by measurement already taken

All of this was measured on accumulation head `8cc71c4` by agent `7b2d40` and
is recorded in [w-7b2d40](w-7b2d40.md) and [w-3f9c02](w-3f9c02.md) §"Why
selection cannot reach it either":

* for `It's just a stupid game` the pool is 18,065 candidates over 333
  distinct structures, but the visible list shows only **6** structures and
  the 50 slots are exhausted at global rank **101**;
* below-cutoff-but-visible candidates do exist (27 of them, at global ranks
  54-101), so being 0.0958 below the visible cutoff is **not** what hides a
  candidate;
* when the canonical alignment is injected at a score that gives it
  within-structure rank **2** under a cap of 17 — comfortably under the cap —
  it is **still invisible** at global rank 2,968;
* on all six of that item's targets there are **0** under-cap candidates
  invisible at or before the fill rank, and **4,609-4,869** invisible beyond
  it.

The last two lines are the finding. A candidate under its structure's cap that
sits deep in the pool is invisible, and the quantity that decides it is the
**fill rank**, not the cap, not the cutoff and not the score.

## The mechanism, in the code as it stands

`select_diverse` (`src/lib.rs:3021`) has three steps:

1. one representative per structure inside the score cutoff (fills only 5-6
   slots, because a saturated cutoff holds only 5-6 structures);
2. `for &i in &order { if picked.len() == top_n { break } ... }` — a single
   walk in **descending score order** over the whole pool, admitting anything
   under its structure's `share_cap`;
3. a cap-dropping fallback.

Step 2's own comment states the intent: *"the slots a structure gives up are
taken by the best candidate of an under-filled structure, which is exactly the
trade the policy is meant to make."* The implementation cannot make that trade
once the list is full. The walk is greedy in score, so the first six
structures to appear take every slot they are entitled to, `picked.len()`
reaches `top_n` at rank ~101, the loop breaks, and the ~4,600 under-cap
candidates that follow — including every candidate of a structure the list has
not seen yet — are never examined. The policy's structural half is therefore
decided by the top of the score order, which is what it exists to prevent.

The general property to fix: **coverage of structures must be bought before
depth within a structure.** Concretely, admit one representative per
structure over the whole pool (bounded by `top_n`, and by whatever walk budget
is needed to stay inside the existing wall clock) and only then spend
remaining slots on further members of structures that already have one. This
is a change of admission *order* inside one function, not a change of weights
and not a change of caps.

## Completion criteria

1. A landed, general change to the admission order in `select_diverse` (or the
   `admit`/cap helpers it uses) that buys structural coverage before depth.
   Argue it from the fill-rank measurement above and from the function's own
   stated policy, never from the acceptance example.
2. **No phrase-specific case.** No special case for any word, substring or
   word sequence of `hits justice dupe hid came`, `wreck a nice beach`, or
   either acceptance phrase, in `src/` or `tests/`. No test relaxed or
   re-baselined to make the change pass. `zz_`-prefixed scratch
   instrumentation and `MADGAB_*`-gated hooks stay on a scratch branch and must
   not reach `post-milestone-acceptance`.
3. **Collision boundary, because [w-3f9c02](w-3f9c02.md) is live in the same
   file.** Do not touch `axes::*` weights, the `boundary_novelty` axis or its
   form, `src/adjacency.rs` or the `ADJACENCY_*` constants, and do not touch
   lexical enumeration, `LEXICAL_BRANCH_KEEP`, `SPAN_SHORTLIST` or
   `SEGMENTATION_KEEP`. This item owns selection order only. If a change
   cannot be made inside that boundary, say so instead of widening it.
4. Report, and justify against, the fill-rank measurement: for each of the six
   targets in [w-7b2d40](w-7b2d40.md), the number of distinct structures in
   the visible list, the fill rank, and the count of under-cap candidates
   still invisible beyond it — the last number should fall.
5. Validation reported: `cargo test --release --lib`,
   `--test corpus_integration`, `--test exact_determinism`,
   `--test approx_determinism`. `approx_determinism` must stay green, which
   matters more here than on a scoring change: approximate mode must remain
   run-to-run deterministic and **raising `top_n` must never retract a shown
   proposal**. If a coverage-first order cannot preserve that property, that is
   the finding to report, not a test to relax.
6. Wall clock not regressed on `--approximate --top 50` across the six targets
   of [w-7b2d40](w-7b2d40.md). Baseline on `8cc71c4`, end-to-end process
   time: 1.76 / 1.92 / 1.80 / 1.94 / 1.99 / 2.28 s. A deeper pool walk is
   the obvious cost; if it needs a bound, derive the bound rather than
   truncating coverage to the old fill rank.
7. `approximate_finds_recognize_speech_resegmentation` must stay green. It is
   the first canonical condition and is a **guard, not a target**; if this
   change trades it away, the change is wrong.
8. `cargo fmt`, `cargo clippy` and doctests do not exist on this host; see
   [../../environment-notes.md](../../environment-notes.md). Do not report them
   as satisfied.

## Handoff / notes

Filed by coordinator `coord-5f4c` on 2026-09-26, as the parallel half of
[w-3f9c02](w-3f9c02.md). That item's own criterion 1 says the selection
hypotheses are closed by measurement *as far as the share cap and the cutoff
go*, and that any future selection proposal must explain how it moves the
**fill rank**, "because that is the only gate left". This item is that
proposal. The two fronts are deliberately independent and both are on the
critical path: this one moves the selector's admission order, that one moves
the boundary-novelty axis. If either lands, the milestone moves; if both land
and the acceptance test still fails, the two measurements together will say
what is left.

Nothing here re-derives anything an earlier item closed:
[w-5f1c04](w-5f1c04.md) and [w-6b2f04](w-6b2f04.md) closed the enumeration
shape, [w-7b2d40](w-7b2d40.md) closed the share cap, and
[w-3c8e10](w-3c8e10.md) already landed the current diversity policy this item
amends. The remaining gate is the one all three of those measurements leave
open.

Next action for a later fresh pass: read the diff for generality, run the four
suites of criterion 5, and integrate onto `post-milestone-acceptance` — never
`main` — if the change is general, stays inside criterion 3's boundary, holds
the determinism property and keeps the first canonical case green.

## Resolution, 2026-09-26 (agent 8a1d47): the change works and does not land;
## no cell of the composition is green

**Outcome: the coverage-first admission order was written, measured and
refuted as landable on this head. `git diff fc5296f -- src tests` on
`madgab-fill-rank` is empty.** The composition `axis+F2` x `coverage-first`
was measured as the steer of `coord-4c17` asked, in all four cells, and no
cell is green on both canonical conditions. The item closes carrying the 2x2,
the best partial combination, and the gate that is left.

Everything needed to reproduce this is on `scratch/8a1d47-measure`
(`53bbd17`), including `MEASUREMENT.md`, the two patches and the model. That
branch carries the `ZZ_POOL_OUT` hook and the F1/F2 ports and is merged
nowhere.

### 1. The change, and it does what the property says

Step 1 of `select_diverse` walks the **whole pool** in descending score order
and admits the best member of every boundary structure until `top_n`
structures are represented, instead of looking only inside `order[..top_n]`.
Step 2 (score, under the share cap) and step 3 (cap-dropping fill) are
untouched, as are `STRUCTURE_FLOOR`, `share_cap` and every `axes::*` constant.
The bound is derived, not truncated: the coverage walk stops at `top_n`
representatives or at the end of the pool, so it is one pass over a list step 2
was already walking. Measured walk depth: it reaches global rank 490-1812 of a
15,712-18,065 pool, i.e. 3-10% of the pool, and spends **50 of 50** slots on
coverage and 0 on depth on every one of the six targets, because every pool
holds 306-352 structures.

### 2. Criterion 4's table, as the change leaves it

Release build of `fc5296f` + this patch, `--approximate --top 50`,
`beam_width 64`, pools dumped from the selector's own input. "under-cap
invisible beyond fill" counts candidates of within-structure rank <= `cap`
that are past the fill rank, which is the definition [w-7b2d40](w-7b2d40.md)
used (its figures and these pools agree exactly: 16,027 / 18,065 / 16,547 /
16,508 / 16,163 / 15,712).

| target | pool | structures | cap | visible structures | fill rank | under-cap invisible beyond fill | worst visible score |
|---|---|---|---|---|---|---|---|
| recognize speech | 16,027 | 313 | 17 | 5 -> **50** | 53 -> **1,812** | 4,618 -> **3,963** | 0.917007 -> 0.879436 |
| It's just a stupid game | 18,065 | 333 | 17 | 6 -> **50** | 102 -> **1,252** | 5,005 -> **4,492** | 0.911264 -> 0.889908 |
| a whole lot of trouble | 16,547 | 326 | 17 | 6 -> **50** | 50 -> **1,277** | 4,963 -> **4,421** | 0.895696 -> 0.880970 |
| the cat sat on the mat | 16,508 | 330 | 17 | 11 -> **50** | 51 -> **490** | 4,995 -> **4,690** | 0.893648 -> 0.886997 |
| put it back on the shelf | 16,163 | 315 | 17 | 7 -> **50** | 52 -> **821** | 4,865 -> **4,431** | 0.894875 -> 0.885214 |
| when the rain finally stopped | 15,712 | 352 | 17 | 11 -> **50** | 62 -> **558** | 5,126 -> **4,792** | 0.914079 -> 0.903801 |

All three numbers move the way the item predicted: coverage 5-11 -> 50, fill
rank 51-102 -> 490-1812, and the under-cap-but-invisible count falls on all
six, by 272-655 each. The fall is bounded and worth stating plainly: the
under-cap set is fixed by the pool and the cap at 4,668-5,176 candidates, of
which any 50-slot list shows 50, so **the residual can never go below
|under-cap set| - 50**. The order can only move candidates from "beyond the
fill rank" to "examined and declined"; it cannot shrink the set. The real
number this change moves is the middle column: 5-11 structures shown instead
of 50.

### 3. The 2x2, and why no cell lands

Both canonical conditions, `--approximate --top 50`, release builds of
`fc5296f` + the stated combination. `F2` is
[w-3f9c02](w-3f9c02.md)'s threshold charge on the retained share,
`max(0, 2*kept/total - 1)`, ported at `b6ddcb0`/`c15b51a` exactly as that item
records it and with **no `axes::*` constant touched**. F1 was measured first
and dropped in favour of F2 on `coord-4c17`'s correction.

**Classic condition — `It's just a stupid game` -> `hits justice dupe hid came`:**

| cell | pool | structures | wording in pool | global rank | within-struct rank | family `[3,10,13,15]` best-member rank | visible structures | green |
|---|---|---|---|---|---|---|---|---|
| axis, coverage off | 18,065 | 333 | **no** | — | — | **94** | 6 | no |
| axis, coverage **on** | 18,065 | 333 | **no** | — | — | **94** | 50 | no |
| axis+F2, coverage off | 17,973 | 313 | **no** | — | — | **63** | 7 | no |
| axis+F2, coverage **on** | 17,973 | 313 | **no** | — | — | **63** | 50 | no |

The wording is **not enumerated in any cell**: the word `dupe` occurs in
**0** of 18,065 candidates on the clean head, and in 0 of 17,973 under F2
(`hits` 56, `justice` 7,099, `hid` 18, `came` 157). No admission order and no
axis form can select a candidate the traversal never emits, and the pool is
byte-identical between the coverage-off and coverage-on cells, so selection is
provably not what hides it. This agrees with [w-3f9c02](w-3f9c02.md) and
[w-7b2d40](w-7b2d40.md) on the clean head and contradicts the older
[w-c1d3a7](w-c1d3a7.md) claim that the alignment is in a retained pool.

**First-canonical guard — `recognize speech` -> `wreck a nice beach`:**

| cell | pool | wording in pool | global rank | within-struct rank | structure best-member rank | visible structures | green |
|---|---|---|---|---|---|---|---|
| axis, coverage off | 16,027 | yes | 28 | 13 of 75 | **3** | 5 | **yes** |
| axis, coverage **on** | 16,027 | yes | 28 | 13 of 75 | 3 | 50 | no |
| axis+F2, coverage off | 16,125 | yes | 1,279 | 13 of 75 | 30 | 5 | no |
| axis+F2, coverage **on** | 16,125 | yes | 1,279 | 13 of 75 | 30 | 50 | no |

Two independent reasons the guard is red outside cell A, and they are the
arithmetic conflict `coord-4c17` accepted:

* **coverage-first vs depth.** The guard is a *global-rank-28 within-cutoff
  repeat*: the 13th-best wording of 75 in the 3rd-best structure. Coverage-first
  shows one wording per structure, so it shows that structure's best member
  and never the 13th. Sweeping the coverage budget on this pool, the guard
  survives at coverage <= 25 and dies at coverage >= 26 (depth 25 vs 24). No
  principled derivation lands at 25: `top_n` (50) and the only derivation
  inside the fence, `top_n - cap` (33), both displace it. The budget would
  have to be chosen by looking at the guard, which is the fitting criterion 2
  exists to prevent.
* **F2 vs the guard.** F2 moves the guard's own score from 0.918313383 to
  0.768313383 and its global rank from 28 to 1,279. Its structure's best member
  falls from rank 3 to rank 30, so coverage-first now *reaches the structure*
  (30 <= 50) and still does not show the wording, which is 13th of 75. This
  reproduces [w-3f9c02](w-3f9c02.md)'s finding on its own head, where F2 removed
  the wording from the pool outright; on `fc5296f` F2 keeps it and buries it.

**No cell is green on both conditions. Nothing is landed in `src/`.**

### 4. The best partial, and its exact remaining gap

The best partial is **coverage-first alone (cell B)**, and the reason is that
it is the half that becomes *sufficient* rather than the half that is
sufficient now. Coverage-first is the only admission order that can reach a
structure by best-member rank 41-63, because it spends its slots on
best members; today's order cannot reach such a structure at all, since it
spends all 50 on within-cutoff repeats of the top few.
[w-3f9c02](w-3f9c02.md) measured the family's best-member rank at **41** under
F1 on head `be589ca`; on `fc5296f` it is **94** clean and **63** under F2. So
the gap has exactly two parts, and both are outside this item's fence:

1. **the family's best-member rank must fall below 50** — a score-front
   quantity, owned by [w-3f9c02](w-3f9c02.md)'s successors. On `fc5296f` no
   axis form tried here gets there (94 clean, 63 under F2);
2. **the wording must be enumerated at all** — `dupe` fills 0 of ~18,000
   candidates today. That is lexical reach for one span, owned by the
   enumeration front ([w-5f1c04](w-5f1c04.md), [w-6b2f04](w-6b2f04.md)), and
   fenced out of both this item and the score front.

Neither part is a selection question, which is the point of the item: the
fill-rank gate is real, it is the only gate a selection order can move, and
moving it does not reach either clue on this head.

### 5. Validation, as run

On `madgab-fill-rank` at `fc5296f` with **no `src/` change** (what is pushed):

```text
cargo test --release --lib                    50 passed; 0 failed    (10.35s)
cargo test --release --test corpus_integration  9 passed; 1 FAILED    (10.21s)
    approximate_finds_classic_madgab_resegmentation  FAILED  (the pre-existing blocker)
    approximate_finds_recognize_speech_resegmentation  ok
cargo test --release --test exact_determinism  1 passed; 0 failed    (4.51s)
cargo test --release --test approx_determinism  2 passed; 0 failed    (27.24s)
```

With the coverage-first patch applied, on the same host, for the record:

```text
cargo test --release --lib                    50 passed; 0 failed    (9.63s)
cargo test --release --test corpus_integration  7 passed; 3 FAILED    (10.11s)
    approximate_output_is_locked                        FAILED (re-baselinable)
    approximate_finds_recognize_speech_resegmentation   FAILED (the guard: criterion 7)
    approximate_finds_classic_madgab_resegmentation     FAILED (pre-existing)
cargo test --release --test exact_determinism  1 passed; 0 failed    (4.63s)
cargo test --release --test approx_determinism  2 passed; 0 failed    (27.32s)
```

Two things worth keeping from that:

* **Every unit test in `--lib` passes unchanged**, including
  `near_equal_alternative_in_a_shown_structure_is_selected` and
  `one_structure_cannot_take_the_whole_list`. Their fixtures hold exactly
  `STRUCTURE_FLOOR` structures, so coverage cannot exceed the cap and both
  still assert what they were written to assert. No test was relaxed,
  re-baselined or deleted on this item, and no assertion was weakened.
* **`approx_determinism` is green under the new order, which was the property
  most likely to break.** Beyond the suite, the CLI was checked for retraction
  across `top_n` 3 -> 5 -> 8 -> 16 -> 32 -> 50 on three targets, spanning the
  regime change where the coverage phase stops being able to fill the list on
  its own: **0 retracted at every step, on all three.** The order is
  prefix-stable in `top_n`: the representative phase admits best members in one
  global score order, the depth phase admits in the same order under a cap that
  only loosens as `top_n` grows, and pruning keeps the best `K`, so a larger
  request can only add.

### 6. Wall clock, criterion 6

End-to-end process time, `--approximate --top 50`, three runs each, same host,
same session, clean head first:

```text
                    clean fc5296f              coverage-first
recognize speech      1.77 1.80 1.76            1.81 1.77 1.72
It's just a stupid game 1.88 1.85 1.90           1.92 1.88 2.01
a whole lot of trouble  1.80 1.77 1.85            1.87 1.82 1.89
the cat sat on the mat  2.00 1.98 2.02            1.98 1.99 2.08
put it back on the shelf 2.21 2.23 2.12           1.91 2.00 1.96
when the rain finally stopped 2.55 2.42 2.57       2.30 2.41 2.35
```

No regression: the clean column reproduces criterion 6's baseline
(1.76 / 1.92 / 1.80 / 1.94 / 1.99 / 2.28 s) and the coverage-first column is
within noise of it, better on two targets. The deeper walk is free here
because it replaces a walk step 2 was already doing over the same in-memory
pool, and because `select_diverse` runs after the search, not inside it.

`cargo fmt`, `cargo clippy` and doctests do not exist on this host
([../../environment-notes.md](../../environment-notes.md)) and are **not**
claimed.

### 7. Handoff

Branch `madgab-fill-rank`: this file only, no `src/`, no `tests/`.
Measurement branch `scratch/8a1d47-measure` (`53bbd17`) is pushed and merged
nowhere; it carries `ZZ_POOL_OUT`, the coverage-first patch, the F1/F2 ports
and the model, and `MEASUREMENT.md` says how to rebuild each of the four cells.

Nothing in this item widened criterion 3's boundary: no `axes::*` constant, no
`boundary_novelty` form, no `src/adjacency.rs`, no `ADJACENCY_*`, no lexical
enumeration, no `LEXICAL_BRANCH_KEEP` / `SPAN_SHORTLIST` / `SEGMENTATION_KEEP`,
no `STRUCTURE_FLOOR` and no `share_cap`. The F2 port exists only on the scratch
branch, under `coord-4c17`'s explicit authorisation, and the boundary it needed
to cross is recorded above rather than taken silently.

**Next action for the coordinator:** two items, in this order, and neither is
selection. (1) The enumeration front: why does `dupe` fill 0 of ~18,000
candidates for a target that has a 3-phoneme slot it could fill? Until the
wording is emitted, no selection or score change can show it, which is the
whole of the classic condition's gap. (2) The score front: the family's
best-member rank is 94 clean and 63 under F2 on this head and 41 under F1 on
`be589ca`; whichever form is carried, the number that matters is that rank
against `top_n`, and coverage-first is already built, measured and waiting to
spend slots on it. If the coordinator decides the guard's within-cutoff repeat
is worth less than 50 structures, the coverage-first patch is one `git apply`
from landing on this branch and its wall clock and determinism cost are both
measured above; that is a policy call about the list, not a measurement, and it
belongs to the coordinator.
