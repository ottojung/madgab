---
work_item: true
id: w-2f7a10
state: working
priority: high
owner: coord-1a4c
updated: 2026-09-26T18:58:00Z
branch: madgab-sweep-4e8a52 (fix 79309a1 integrated at 27f9776 via madgab-int-79309a1; no branch of its own remains open)
worktree: fronts /workspace/madgab-depth4-b47d02, /workspace/madgab-depthcap-c81e55 (report-only, removable)
agents: e1a3f7, a3f19c, b47d02, c81e55 — all terminal (succeeded); reports durable on origin/madgab-costref 74e3d8b, origin/madgab-int-79309a1 53ba542, origin/madgab-depth4-b47d02 b8aa6d8, origin/madgab-depthcap-c81e55 b8391f6
---

# The canonical wording is enumerated nowhere in its own boundary structure:
# make the traversal reach a *pairing*, not just a single deep word

## Goal

Make the approximate mode generate the requested resegmentation for
`It's just a stupid game` as a result of general search, segmentation or
ranking behaviour — never a phrase-specific hard-code. The quantity that must
move is **whether the five-word pairing is enumerated at all**, and then whether
the selector admits it.

## Why this is a new item and not a continuation

[w-c4e8d7](w-c4e8d7.md) closed with its blocker list empty and, by its own
sequencing rule, stopped: membership went from 0 to non-zero and the wording is
still invisible, which that item defines as its successful end state. It also
forbids re-litigating selection, admission order, share cap and axis weights,
and [w-8a1d47](w-8a1d47.md) and [w-3f9c02](w-3f9c02.md) closed those two
territories. This item is what is left, and it is a **different question** from
either of them: not "which candidates are admitted" and not "how are they
scored", but "the traversal never emits this combination of coordinates at
all".

## The measurement this is based on

From `c4e8d70`'s end-state note on `da9c14c`, on the integrated tree
(`58a45b0` and after), release build, `--approximate --top 50`, offsets read
from `madgab::Clue::cuts`:

```text
It's just a stupid game, pool 17,907, 333 distinct structures,
visible cutoff 0.915691888 at rank 49

canonical structure [3, 10, 13, 15, 19]
  members in the pool                47
  best member                        "each justice too add gain"
  best-member global rank            3108
  best member score                  0.87812846738654216
  gap to the visible cutoff          -0.037563
  fill rank                          104   (the 50th visible slot is pool rank 104)
  the requested wording              not among the 47
```

The structure exists, is populated, and is reachable in the pool; the requested
wording is simply not one of the 47 wordings the traversal emits for it. So
**no scoring, admission, share-cap or cutoff change can surface it**, and the
best member's score is byte-identical to the parent's, which confirms the
reserve changed *which wordings are enumerated* and not how any of them scores.
That is the seam this item owns.

The single-word half of the problem is already solved and must stay solved: the
middle word of the requested alignment fills 14 of 17,907 candidates, and
`tests/corpus_integration.rs::approximate_pool_reaches_alternatives_past_the_opening_slot_width`
is the guard for it, as is
`approximate_finds_recognize_speech_resegmentation` for the first canonical
example.

## Hypotheses worth testing first, in order

1. **The per-slot depth profile is the constraint.** The reserve now walks slot
   subsets breadth-before-depth, so a word *is* reachable in each slot
   individually, but the emitted tuples may never place two deep coordinates in
   the *same* structure. A pairing needs more than one slot off the traversal's
   best index at once. `coverage_tuples` already has an `EMIT_PROFILE_MAX_DEEP`
   of 3 and subsets up to size 3; the question is whether the budget
   (`EMIT_PROFILE_RESERVE`) or the ordering is what keeps the requested shape
   out, and which.
2. **The cost bound rejects the tuple, not the ordering.** `build` compares each
   tuple's total substitution cost against `total_budget` additively. If the
   requested tuple's cost exceeds the budget while each of its words
   individually does not, then the per-slot affordability slack
   `sum_{j != k} m_j + cost_{k,i} <= total_budget` — the arithmetic
   [w-4b1e07](w-4b1e07.md)'s 11:52Z pass derived — is the real gate, and the
   fix is to make the *pair's* affordability visible to the traversal rather
   than each word's.
3. **The structure's own retention is the gate.** 47 members is small; check
   whether `SEGMENTATION_KEEP` or the pool's per-structure cap is what holds it
   at 47, and whether a structure that is *under*-represented is being
   crowded out of the pool by structures with more members.

## Scope and collision boundary

This front owns the traversal's **tuple space**: `coverage_tuples`,
`sweep_index`, `EMIT_PROFILE_MAX_DEEP`, `EMIT_PROFILE_RESERVE`,
`LEXICAL_COMBINATIONS_PER_SEGMENTATION`, the per-slot candidate lists, the
emission budget accounting in `build`, and the per-structure retention in the
pool. It may read `axes::*` and `select_diverse` to *diagnose* but must not
change them.

Forbidden: sweeping or re-tuning `axes::*` weights or any `boundary_novelty`
form (closed by [w-3f9c02](w-3f9c02.md)), `src/adjacency.rs` and
`ADJACENCY_*`, changing `select_diverse`'s admission order or the share cap /
`STRUCTURE_FLOOR` (closed by [w-8a1d47](w-8a1d47.md)), landing the parked
coverage-first patch, relaxing or re-baselining **either** acceptance test, and
any phrase-specific case. `approximate_output_is_locked` may be re-baselined
only with a record of which structures moved, and only as a consequence of this
item's own change.

## Completion criteria

1. **Diagnosis by line, before any fix.** The exact place in the default path
   where the requested tuple is not emitted: the function, the line, and the
   value of the quantity that rejects it. If the answer is "the traversal never
   offers that coordinate combination", show the count of tuples it does offer
   for that structure and the count it would need to.
2. **A landed, general fix** for the class, argued from the mechanism and never
   from either phrase: no special case for any word, substring or word sequence
   of either acceptance example, in `src/` or `tests/`.
3. **Measured membership, on the default path, release build:** the count of
   pool candidates containing the requested wording goes from **0** to
   non-zero, and the same count is reported for six real targets so the change
   is shown not to be specific to one input.
4. **A regression test** expressing the general property at an appropriate
   boundary — that a *pairing* of deep alternatives in different slots is
   reachable in the approximate pool, not merely each alternative alone — and
   naming neither acceptance phrase.
5. `approximate_finds_recognize_speech_resegmentation` and
   `approximate_pool_reaches_alternatives_past_the_opening_slot_width` both stay
   green. They are the guards, not targets.
6. `cargo test --release --lib`, `--test corpus_integration`,
   `--test exact_determinism`, `--test approx_determinism` and
   `--test no_phrase_hard_coding` reported, with `approx_determinism` and the
   fence green. `cargo fmt`, `cargo clippy` and doctests do not exist on this
   host; see [../../environment-notes.md](../../environment-notes.md). Do not
   claim them.
7. Wall clock on `--approximate --top 50` not regressed. The 1.77-2.51 s column
   in [w-7b2d40](w-7b2d40.md) is **host noise** — `c4e8d70` measured the parent
   tree at 1.82-3.21 s in the same session — so measure interleaved against the
   parent in one session and say so, rather than quoting the old column. If
   deeper enumeration needs budget, **derive** it from the bound the search
   already respects (`total_budget`, compared additively at emission time)
   rather than raising a constant, and say what the constant was bounding.

## Handoff / notes

Filed by coordinator `coord-9f2c` on the numbers `c4e8d70` reported when
[w-c4e8d7](w-c4e8d7.md) closed. The push of this file is the submission event;
it is `open` and unowned, so the next coordinator should claim it before
launching an agent.

Usable as-is, pushed: `scratch/c4e8d7-measure` (`ZZ_TRACE` / `ZZ_STAGE` /
`ZZ_POOL_OUT` probes on the integrated line) and `scratch/c4e8d7-basepool`
(the same dump hook on the parent tree, the admissible baseline instrument).
**`scratch/c4e8d7-basepool` must never be merged.** Keep new instrumentation on
a scratch branch of this item and off the implementation branch, and end the
implementation branch on a clean non-`wip:` commit.

**Admissibility rule, inherited from `w-c4e8d7` and not to be relaxed.**
Reachability evidence counts only if it is (a) taken on the **default**
approximate path, (b) with a release build, and (c) expressed as membership of
the deduplicated pool the release binary actually produces, or as a line-level
trace inside that path. Shortlist ranks from an instrumented or re-budgeted
harness are not admissible. This queue has retracted one reachability claim
already ([w-7b41d2](w-7b41d2.md) at 14:10Z) for exactly that reason. Say which
of your numbers are which.

This is the item that closes [w-4b1e07](w-4b1e07.md) and, with it,
[w-a02d28](w-a02d28.md) and the itinerary's primary milestone. Do not start
[w-2e5b93](w-2e5b93.md) before this one: its per-axis spread table must be
re-taken on the pool this item settles, and the pool has changed shape twice in
the last two passes.

### Pass 2026-09-26T18:05Z (coordinator coord-4e2b): claimed, split into two independent fronts, both agents running

State `open` -> `working`, owner `coord-4e2b`, claimed at `fe4ad78` on
`post-milestone-acceptance`. `main` is untouched. The previous pass left this
item open and unowned with no agent on it; nothing was in flight for it, so this
pass started it rather than reconciling a running front.

Two fronts are running in parallel, deliberately in separate worktrees on
separate branches so they do not contend on files:

| agent | branch | worktree | front |
| --- | --- | --- | --- |
| `2f7a11` | `madgab-pairing-2f7a10` | `/workspace/madgab-pairing` | diagnosis of criterion 1 (function, line, rejecting quantity) and a landed general fix, with the validation and the before/after membership counts |
| `2f7a12` | `scratch/2f7a10-base` | `/workspace/madgab-pairbase` | the admissible baseline only: pool size, distinct structures, the `[3, 10, 13, 15, 19]` member count and best member, a six-target membership table, and interleaved wall clock, on the parent head |

`2f7a12` is measurement-only and its branch **must never be merged**; it exists
so that criteria 3 and 7 can be judged against a baseline taken on the default
release path rather than re-derived by the implementer, and so a reviewer has an
independent number to check the fix against. `2f7a11` was told not to merge it.

Next action for a later fresh pass, in order:

1. `antonina agent status --id 2f7a11` / `--id 2f7a12`, then read their logs.
   Neither is a completion signal on its own.
2. Review `madgab-pairing-2f7a10` before it is integrated: no phrase-specific
   case, no change to `axes::*` / `select_diverse` / `adjacency.rs` / the share
   cap, both guards green, the no-hard-coding fence untouched, and a wall-clock
   number taken interleaved against `fe4ad78` in the same session.
3. Integrate into `post-milestone-acceptance` only after that review, and only
   if `approximate_finds_classic_madgab_resegmentation` passes on its own
   merits; that is also the unblock condition recorded on [w-a02d28](w-a02d28.md).
4. Only then take [w-2e5b93](w-2e5b93.md)'s per-axis spread on the settled pool.

Host note for whoever picks this up: `git` here is configured to sign commits
and `gpg` is absent, so a plain `git commit` fails. Use
`git -c commit.gpgsign=false commit`.

### Pass 2026-09-26T18:20Z (coordinator `coord-7c31`): reconciliation only; both fronts alive, the recorded branch is now on `origin`, and the depth-profile cap is named as the hypothesis to measure

Fresh pass, nothing integrated: both fronts are minutes old with
uncommitted work, so there is nothing finished to review. Re-claimed from
`coord-4e2b`. `post-milestone-acceptance` is clean and identical to
`origin` at `3191e52`; `main` is untouched (still `c0ecd7c` on
`origin`, and this host's fetch refspec tracks only the accumulation
branch, so the "never push scheduled work to `main`" condition is checked
with `git ls-remote` and not with a local ref).

State found, from agent and worktree state rather than timestamps:

- `2f7a11` and `2f7a12` are both `running` and alive in
  `/workspace/madgab-pairing` and `/workspace/madgab-pairbase`. Neither is
  finished, and neither a progress message nor a green suite is a
  completion signal.
- Both worktrees are on **named branches**, so the `04f83f0`
  detached-head hazard is absent.
- **Durability gap found and closed.** No branch this item names existed
  on `origin` at all: `madgab-pairing-2f7a10`,
  `scratch/2f7a10-measure` and `scratch/2f7a10-base` were all
  single-copy in agent worktrees. `madgab-pairing-2f7a10` is now pushed
  at `fe4ad78`, and the agents have been told to push their own work from
  now on.

**A discrepancy worth recording, because it would have made a landed fix
unreviewable.** The item records `2f7a11`'s branch as
`madgab-pairing-2f7a10`, but `/workspace/madgab-pairing` is checked out
on `scratch/2f7a10-measure`. A scratch branch must never be merged, so a
fix left there is neither reviewable nor integrable. The agent has been
told to land on `madgab-pairing-2f7a10`, and to keep its three untracked
`examples/zz_*.rs` probes, and any `ZZ_*`/`MADGAB_*` diagnostic not
already in the base tree, off that branch.

**The one thing this pass contributes that neither agent could cheaply
derive.** `2f7a11`'s own `zz_pool` output gives the canonical structure
`[3,10,13,15,19]` per-slot candidate-list widths
`[160, 7, 160, 160, 93]`, and per-slot ranks of **7 / 0 / 13 / 99 / 11**
for `hits justice dupe hid came`. So **four** of the five coordinates are
deep at once and only slot 1 is not, while the coverage reserve is
documented as sweeping slot subsets breadth-before-depth with
`EMIT_PROFILE_MAX_DEEP = 3` and subsets up to size 3. If that cap really
is the limit on how many slots may be deep in a single reserved tuple,
then a 4-deep coordinate set is outside the class of shapes the reserve
can express — which is hypothesis 1 in this item, now with the numbers
that discriminate it. It went to `2f7a11` as a **hypothesis to measure,
with a refutation declared an acceptable result**, not as a fix to apply,
together with the instruction to say what the constant was bounding and
to derive any extra spend from the global emission and pop budgets rather
than raising an unrelated constant. This also re-anchors the queue's
long-standing "hid rank 85" versus "hid rank 99" confusion: 99 is the
same walk-order figure `w-3e5c7` already recorded, not new evidence.

`2f7a12` was steered to finish and report the baseline it already has
rather than extend it, to state which of its numbers come from the
deduplicated default-path pool and which from its probe, to push its
scratch branch so the instrumentation is durable, and to keep that branch
unmerged and the probe out of `post-milestone-acceptance`. Its own check
that visible output is byte-identical with and without `ZZ_POOL_OUT` is
the admissibility check this queue has retracted one reachability claim
over, and its report should say so explicitly.

No second front was opened. The remaining territory is either the two
live agents' or one of the eight closed-and-recorded refutations, and the
itinerary warns against manufacturing a backlog. [w-2e5b93](w-2e5b93.md)
stays gated: pool membership is still the whole requirement, so a weight
move would re-baseline `approximate_output_is_locked` for no gain, and
the pool has just changed shape.

Next action for a later fresh pass, in order:

1. `2f7a11` and `2f7a12`. Review whichever lands first as a diff for
   generality — a cap must still bound something with named arithmetic,
   no phrase-specific case in `src/` or `tests/`, no
   `axes::*`/`select_diverse`/`adjacency.rs` edit, no relaxed or
   re-baselined acceptance test, no `zz_*`/`.bench`/`MADGAB_*`
   scaffolding on the branch — then run `cargo test --release --lib`,
   `--test corpus_integration`, `--test exact_determinism`,
   `--test approx_determinism` and the no-hard-coding fence, and
   integrate onto `post-milestone-acceptance`, never `main`. `wreck a
   nice beach` is the guard a pool-changing fix is most likely to trade
   away, so re-measure `recognize speech` after that integration rather
   than only at the end.
2. Verify durability with `git ls-remote origin`, not with a local
   remote-tracking ref, before treating either front's work as safe.
3. Only once `approximate_finds_classic_madgab_resegmentation` passes on
   `post-milestone-acceptance` on its own merits, with no
   phrase-specific case in `src/`, close this item,
   [w-4b1e07](w-4b1e07.md) and [w-a02d28](w-a02d28.md) together, and
   re-check every milestone condition in the itinerary.

Host note repeated for whoever picks this up: `git` here is configured to
sign commits and `gpg` is absent, so a plain `git commit` fails. Use
`git -c commit.gpgsign=false commit`.

### Update, same pass (baseline front `2f7a12` finished: `done`, parent-only baseline recorded)

`2f7a12` exited 0 and is closed. Its branch `scratch/2f7a10-base` is
`22507d5`, local and on `origin`, one commit, and it must never be merged
anywhere. `git grep ZZ_POOL_OUT post-milestone-acceptance -- src tests
examples` returns nothing, so the probe reaches nowhere else, and
`tests/` has an empty diff on it. Its recipe is `docs/measure-2f7a10-base.md`
on that scratch branch. It recorded suites `--lib` 50/0,
`corpus_integration` 10 passed / 1 failed (the acceptance test this item
exists to fix, untouched), `approx_determinism` 2, `exact_determinism` 1,
and correctly did not claim `cargo fmt`, clippy or doctests.

Its probe was verified non-intrusive first: visible output is
byte-identical with and without `ZZ_POOL_OUT`, which is the admissibility
check this queue retracted one reachability claim over. The numbers below
are therefore membership of the deduplicated **default-path** pool read
off a release build, which is what counts.

Canonical structure `[3, 10, 13, 15, 19]`, parent head `fe4ad78`, all of
[c4e8d7](w-c4e8d7.md)'s figures reproducing except one off-by-one:

```text
pool                                  17906   (w-c4e8d7's note said 17907;
                                                src/ is byte-identical
                                                between 58a45b0 and fe4ad78,
                                                so the note is off by one)
members of the canonical structure       47   (unchanged)
best member                    each justice too add gain
best-member global rank                 3108
gap to the visible cutoff          -0.037563
the requested wording              not present

per-word membership inside that structure, and where it can occur:
  hits    56     justice 7099     came 157
  dupe    14     all non-leading      (slot 2: 6, 3: 7, 4: 1)
  hid      5     FOUR OF FIVE AT LEADING SLOT 0
```

**The pairing gap, now quantified rather than asserted.** Every word is
reachable alone. Every pair that matters is **0**: `hits+dupe`, `hid+dupe`,
`hits+hid`, `dupe+came` all 0; only `too+dupe` is present, at
`[3,10,11,13,15,19]`. So the reservation does admit multi-word
combinations in that structure, just not this one, and the asymmetry is
informative: `dupe` is confined to non-leading slots and `hid` to the
leading slot, so the two cannot co-occur anywhere in it.

**The generalisable form of the same fact, on six real targets that
contain neither acceptance example.** Deep alternative = a non-target word
at position >= 1; each is reachable in a non-leading slot on all six, and
`taught` on `put it back on the shelf` is well-populated (28 members) yet
**leading-slot only** — the same confinement as the canonical `hid`. That
is the property a fix has to generalise, and it is the honest form of
criterion 3's six-target table:

```text
target                          pool  structs  cutoff(49)  fill  witness  members  best rank  slot
a whole lot of trouble           16151     326   0.895695875    49  delve         1     16051    7
the cat sat on the mat           16262     330   0.893683280    50  tickets       3      1672    1
put it back on the shelf         15633     314   0.894965791    51  louis         8     15011    6
when the rain finally stopped    15610     352   0.914965805    60  aar          13     15572    3
he was a big fat man             19164     313   0.895134791    49  honour        1     18860    6
what are you going to do         14544     309   0.905780912    54  perdue        2     14438    7
```

`misfit` is 0 on every target, recorded so a later pass reports its own
zeros rather than assuming reachability.

**Limitation, stated rather than papered over.** This is a **parent-only**
arm. An interleaved comparison was impossible because `2f7a11` had no
commit, only uncommitted work. The column is therefore not comparable to
[w-7b2d40](w-7b2d40.md)'s 1.77-2.51 s:

```text
a whole lot of trouble            1.82  1.84  1.76  1.75
the cat sat on the mat            1.99  1.92  1.96  1.94
put it back on the shelf          1.95  1.96  1.94  1.93
when the rain finally stopped     2.82  2.46  2.46  2.26
he was a big fat man              1.79  1.72  1.74  1.75
what are you going to do         1.82  1.83  2.95  2.04
It's just a stupid game (ref)     2.68  3.44  2.98  2.82
```

A later pass must interleave its own build against this arm in its own
session to satisfy criterion 7.

`2f7a12` also independently reported the branch hazard this pass had
already found and steered: `/workspace/madgab-pairing` is on
`scratch/2f7a10-measure`, so the implementer's uncommitted work and three
untracked probes are single-copy on a `scratch/*` branch. Two agents
reaching the same conclusion independently is the reason to act on it
rather than file it as a note.

### Update, same pass (discriminating evidence delivered to `2f7a11`; implement front is now writing code)

The baseline's `leading-slot confinement` result was steered to
`2f7a11` as measurement input, with the instruction that it is not a fix to
apply. Accepted within the pass: it moved from probing to implementing
`coverage_tuples`, and is reading the existing unit tests that assert on
that function's behaviour before changing it.

The property it must now generalise, in the words the baseline measured it,
and the form a regression test for criterion 4 should take:

> a word that is well-populated in a boundary structure can still be
> confined to a single slot class within that structure.

True for the canonical `hid` (5 members, 4 of them leading slot 0) and
independently for `taught` on `put it back on the shelf` (28 members,
leading-slot only) — neither of which is an acceptance example. The
mechanism it is testing is that the coverage reserve sweeps slot subsets
breadth-before-depth with `EMIT_PROFILE_MAX_DEEP = 3`, while the shape the
canonical pairing needs has four deep coordinates at once.

Next action for a later fresh pass is unchanged and is step 1 of the 18:20Z
note: review whichever lands first, starting with
`/workspace/madgab-pairing`, and check first that the work is on
`madgab-pairing-2f7a10` rather than `scratch/2f7a10-measure`.

### Pass 2026-09-26T19:05Z (coordinator `coord-9d42`): the branch hazard is closed, the implementer-s diff has been reviewed as it stands, and the in-flight change is steered to land

Fresh pass, nothing integrated: `2f7a11` is still working and has no commit, so
there is no finished work to review onto the accumulation branch.
`post-milestone-acceptance` is at `8ef1a73` and identical to
`origin/post-milestone-acceptance` after an explicit fetch; `main` is
`c0ecd7c` on `origin`, untouched, and checked with `git ls-remote` because
this host-s fetch refspec tracks only the accumulation branch.

State found, from agent and worktree state rather than timestamps:

- `2f7a11` is `running` and alive in `/workspace/madgab-pairing`; the steer
  issued this pass restarted its turn (`started` moved), so it is working,
  not hung.
- `/workspace/madgab-pairing` is now checked out on
  **`madgab-pairing-2f7a10`**, not `scratch/2f7a10-measure`. The hazard this
  item recorded twice is resolved, so a fix left in that worktree is
  reviewable and integrable. Only `src/lib.rs` is modified; the
  `examples/zz_*.rs` probes are gone.
- The branch base is `fe4ad78`; the accumulation head is `8ef1a73`. The five
  commits between them are docs-only, so `src/` is byte-identical and the
  rebase an integration will need is trivial. Recorded so a later pass does
  not read the branch as stale.
- `2f7a12` is closed and its arm is `scratch/2f7a10-base` (`22507d5`, on
  `origin`, must never be merged). Still the only admissible baseline.

**The review this pass could actually do, because there is uncommitted work to
read: the change in `src/lib.rs` is general.** It is a traversal-shape change
in the coverage reserve, not a scoring or admission change, and it argues its
own mechanism:

- `coverage_tuples` now walks class subsets in an order that **interleaves
  depths** (first class of every depth, then the second of every depth) instead
  of breadth-before-depth, so a deep shape is funded from the reserve-s first
  tuple rather than after every one-deep shape has been paid for;
- `profile_tuple` gives every slot of a class **its own** coordinate from its
  own list, at its own width and its own rotation of the phase
  (`COVERAGE_SLOT_ROTATION = 37`), so a class emits *points* of its index
  rectangle rather than *diagonals* of it. That is the difference between a
  wording whose slots are off the traversal-s best index at unrelated ranks
  being late in the order and being absent from it;
- `EMIT_PROFILE_MAX_DEEP` 3 -> 4, on the stated ground that the class count
  no longer bounds depth once the order interleaves;
- a class is dropped rather than partially emitted when a member slot is no
  wider than the opening width, and `build` still compares each tuple-s total
  substitution cost against `total_budget` additively, so the reserve is
  bounded by the same bound as every other emission.

Nothing in it touches `axes::*`, `select_diverse`, `src/adjacency.rs`, the
share cap, or either acceptance test, and there is no special case for any
word, substring or word sequence of either acceptance example. That is the
generality bar criterion 2 sets, and the diff meets it as it stands.

**What the steer asks for, and why each part is load-bearing:**

1. **Land it.** The whole change is a single uncommitted ~144-line working-tree
   diff, one reboot from lost. It is to be committed as one clean non-`wip:`
   commit on `madgab-pairing-2f7a10` and pushed, with **both** diagnostic
   blocks removed from the landed file: the `ZZ_TUPLE_OUT` `eprintln!` inside
   the segmentation loop and the `ZZ_POOL_OUT` writer before `select_diverse`.
   Both are present in the current diff and neither may reach the accumulation
   branch.
2. **Two arguments, not assertions.** What `EMIT_PROFILE_MAX_DEEP = 4` bounds
   now that the class order interleaves depths, with the arithmetic; and the
   emitted-tuple count and pool delta per target, so a reviewer can see the
   unchanged `EMIT_PROFILE_RESERVE` is not silently buying fewer shapes than
   the breadth-first walk did. A refutation is an acceptable result, but as a
   measurement.
3. **Criterion 1 is not yet met and the agent-s own dump is not admissible for
   it.** The `ZZ_TUPLE_OUT` probe printed exactly **one** line — phase 151,
   14 tuples, every one of them at most 3-deep, reserve 16 — which is one
   segmentation rather than a run, and an instrumented print rather than
   membership of the deduplicated default-path pool. Criterion 1 needs the
   place in the default path where the requested coordinate combination is never
   offered, and criterion 3 needs membership 0 -> non-zero on a release default
   build. The baseline arm-s numbers were re-sent verbatim so the comparison is
   against an admissible arm, not a re-derivation.
4. **Criterion 4 is not optional and is not yet written.** The property, in the
   baseline front-s words: a word that is well-populated in a boundary
   structure can still be confined to a single slot class within that structure.
   The test must show a *pairing* of deep alternatives in different slots is
   reachable in the pool, not each alternative alone, naming neither acceptance
   phrase; `taught` on `put it back on the shelf` is an already-measured
   non-canonical witness.
5. **Re-measure `recognize speech` after the change, not only at the end.** A
   pool-changing fix is most likely to trade away `wreck a nice beach`, and
   both acceptance tests are guards rather than targets.
6. **Wall clock interleaved against `fe4ad78` in its own session.** The
   1.77-2.51 s column is host noise and the parent-only arm is not comparable.

**No second front was opened this pass, and the reason is worth recording.**
The obvious parallel front is an independent measurement of the post-change
six-target membership table and the interleaved wall clock (criteria 3 and 7),
which is exactly the work that cannot be done yet: it needs a pushed commit to
compare, and `2f7a11` has none. Starting it now would produce an agent waiting
on another agent-s commit, which is the serialisation the guide warns against
rather than the parallelism it asks for. It is named below as the first front to
open the moment `madgab-pairing-2f7a10` is pushed.

Nothing was integrated on this pass and `main` is untouched.
[w-2e5b93](w-2e5b93.md) stays gated for the reason already recorded: the pool
has just changed shape, and a weight move now would re-baseline
`approximate_output_is_locked` for no measurable gain.

Next action for a later fresh pass, in priority order:

1. `antonina agent status --id 2f7a11`, then read its log. If
   `madgab-pairing-2f7a10` has a pushed commit, review it as a diff for
   generality before anything else: no phrase-specific case in `src/` or
   `tests/`, no `axes::*`/`select_diverse`/`adjacency.rs`/share-cap edit,
   no `ZZ_*`/`zz_*`/`MADGAB_*`/phase-timing scaffolding on the branch, no
   relaxed or re-baselined acceptance test, and the two named guards plus
   `approximate_finds_classic_madgab_resegmentation` green. Then run `--lib`,
   `--test corpus_integration`, `--test exact_determinism`, `--test
   approx_determinism` and `--test no_phrase_hard_coding`, and integrate onto
   `post-milestone-acceptance`, never `main`.
2. Open the independent verification front in a separate worktree and branch as
   soon as that push exists: the six-target membership table and the
   interleaved wall clock against `scratch/2f7a10-base`-s parent arm, so
   criteria 3 and 7 have a number that the implementer did not take. It must
   measure the deduplicated default-path pool on a release build and must not
   merge `scratch/2f7a10-base`.
3. Verify durability with `git ls-remote origin`, not with a local
   remote-tracking ref, before treating either front-s work as safe.
4. Only once `approximate_finds_classic_madgab_resegmentation` passes on
   `post-milestone-acceptance` on its own merits, with no phrase-specific case
   in `src/`, close this item, [w-4b1e07](w-4b1e07.md) and
   [w-a02d28](w-a02d28.md) together, and re-check every milestone condition in
   the itinerary.

Host note repeated for whoever picks this up: `git` here is configured to sign
commits and `gpg` is absent, so a plain `git commit` fails. Use
`git -c commit.gpgsign=false commit`. Also note `antonina agent prompt` on a
running agent needs `--steer`, and the steering call itself can block for
minutes; budget for that rather than treating it as instant.

### Pass 2026-09-26T17:06Z (coordinator `coord-3a9c`): the fix has landed and is pushed, it has been reviewed for generality here, and the independent verification front is now open

Fresh pass. `post-milestone-acceptance` is `bbfb94a`, clean and identical to
`origin` after an explicit fetch. `main` is `c0ecd7c` on `origin`, untouched,
checked with `git ls-remote` because this host's fetch refspec tracks only the
accumulation branch. Nothing is integrated on this pass and `main` is not
touched.

**Clock note so a later pass is not misled.** The headings on the three passes
above (18:05Z, 18:20Z, 19:05Z) are roughly two hours ahead of the commit times
for the same notes (`16:51`, `16:54`, `17:00`). This note uses host time from
`date -u`, which is what the commits carry.

**The change is landed, pushed and reviewable — the single uncommitted-diff
hazard this queue has carried for three passes is closed.**

```text
a58a49b  w-2f7a10: sample the traversal's index space as points, not diagonals
         on origin/madgab-pairing-2f7a10, on top of fe4ad78
         src/lib.rs only, +179 -43
```

**The review criterion 2 asks for, done here on the pushed commit rather than
on a working tree** (`git diff fe4ad78..a58a49b`, full text in this pass's
notes; re-derivable with the same command):

- only `src/lib.rs` changed. No `tests/` change, so neither acceptance test was
  relaxed or re-baselined, and no integration test was added by this commit.
- no added line contains any acceptance word or phrase. The phrase occurrences
  in the file are all pre-existing `#[cfg(test)]` unit tests.
- no `ZZ_*` / `zz_*` / `MADGAB_*` line is added. Both diagnostic blocks the
  19:05Z note named — the `ZZ_TUPLE_OUT` `eprintln!` in the segmentation loop
  and the `ZZ_POOL_OUT` writer before `select_diverse` — are absent, and the
  two `examples/zz_*.rs` probes are untracked and off the branch.
- nothing in `axes::*`, `select_diverse`, `src/adjacency.rs`, the share cap or
  `STRUCTURE_FLOOR` is touched, consistent with this item's scope boundary.
- the change is a traversal-shape change, not a scoring or admission change:
  `coverage_tuples` now walks class subsets interleaved by depth instead of
  breadth-before-depth; the new `profile_tuple` gives every slot of a class its
  own coordinate, from its own list, at its own width and its own rotation of
  the phase (`COVERAGE_SLOT_ROTATION = 37`), so a class emits *points* of its
  index rectangle rather than diagonals of it; and `EMIT_PROFILE_MAX_DEEP`
  3 -> 4 on the stated ground that the class count no longer bounds depth once
  the order interleaves. `build` still bounds every tuple's total substitution
  cost against `total_budget` additively, so the reserve is not unpriced.
- criterion 4's regression test is present and is written as a **shape**
  property, not a phrase: `depth_profile_reserve_emits_pairings_not_only_diagonals`
  asserts, over 64 phases and a five-slot width vector, that the reserve emits
  a tuple deep in three or more slots whose coordinates are not all equal, and
  that it reaches its own maximum depth. It names no word, target or phrase.

That is the generality bar met. It is **not** integration, and criteria 3 and
7 are still unmeasured on a default-path release build by anyone other than the
implementer.

**The verification front is now unblocked, and is open: agent `3a9c21`.**

| agent | branch | worktree | front |
| --- | --- | --- | --- |
| `2f7a11` | `madgab-pairing-2f7a10` | `/workspace/madgab-pairing` | implementing; `a58a49b` landed, now rebasing onto the accumulation head `bbfb94a` with `src/lib.rs` staged |
| `3a9c21` | `scratch/2f7a10-verify` | `/workspace/madgab-verify-3a9c21` | independent measurement: criterion 3 membership on the default release path for the canonical target and the six-target table, criterion 7 interleaved wall clock against `fe4ad78`, both guards re-measured, and the five suites |

`3a9c21` was created at `a58a49b` and is measurement-and-review only: it may
not modify `src/` or `tests/`, may not merge anything, and may never merge
`scratch/2f7a10-base`. It was given the baseline arm's numbers verbatim so its
comparison is against an admissible arm rather than a re-derivation, and was
told explicitly that the parent-only baseline is **not** a valid wall-clock
comparison. Its early progress line reports the default-path pool for the
stupid-game target as 17637 lines where the baseline arm had 17906, i.e. the
reserve change shrank the pool by about 270 candidates; that is one number from
a running agent and is **not** yet a result, but it is the first thing a
reviewer should look at, because a smaller pool is a live risk to both
acceptance guards.

**Still outstanding against this item's criteria, after this pass:**

- criterion 1, the line-level diagnosis in the default path. `3a9c21`'s
  membership numbers decide whether the fix even reaches the requested
  coordinate combination.
- criteria 3 and 7, now assigned to `3a9c21` and not to be re-derived by
  `2f7a11`.
- the two arguments the 19:05Z note asked for: what `EMIT_PROFILE_MAX_DEEP = 4`
  now bounds, with arithmetic; and the emitted-tuple count and pool delta per
  target, so a reviewer can see the unchanged `EMIT_PROFILE_RESERVE` is not
  silently buying fewer shapes than the breadth-first walk did. Still owed by
  `2f7a11`, and the pool-delta number is the same one the pool shrink above
  makes urgent.

No second front beyond `3a9c21` was opened: the remaining territory is the two
live agents' or one of the eight closed-and-recorded refutations, and
[w-2e5b93](w-2e5b93.md) stays gated for the reason already recorded — the pool
has just changed shape again, so a weight move would re-baseline
`approximate_output_is_locked` for no measured gain.

Next action for a later fresh pass, in priority order:

1. `antonina agent status --id 3a9c21` and `--id 2f7a11`, then their logs.
   Neither a progress message nor a green suite is a completion signal. If
   `2f7a11`'s rebase onto `bbfb94a` lands a new commit on
   `madgab-pairing-2f7a10`, re-run the fence scan on it
   (`git diff a58a49b..new --name-only` should still be `src/lib.rs` only, and
   the phrase and `ZZ_*` scans should still be empty) before treating it as
   integrable.
2. Integrate onto `post-milestone-acceptance` — never `main` — only once
   `3a9c21` has reported criterion 3 membership 0 -> non-zero on the default
   release path **and** both guards green, and the five suites are green on the
   branch being integrated. Re-measure the guards after integration rather than
   only before it.
3. Treat the ~270-candidate pool shrink as a first-class review question, not a
   footnote: report which structures and which wordings left the pool, and
   whether any of the six baseline witnesses regressed to zero membership.
4. Close this item, [w-4b1e07](w-4b1e07.md) and [w-a02d28](w-a02d28.md)
   together only once `approximate_finds_classic_madgab_resegmentation` passes
   on `post-milestone-acceptance` on its own merits with no phrase-specific
   case in `src/`, and then re-check every milestone condition in the
   itinerary.

Host note repeated: `git` is configured to sign commits and `gpg` is absent, so
use `git -c commit.gpgsign=false commit`. `antonina agent prompt` on a running
agent needs `--steer` and can block for minutes. Verify durability with
`git ls-remote origin`, not with a local remote-tracking ref.

### Pass 2026-09-26T17:20Z (coordinator `coord-6d14`): the reserve shape is **not** the gate, a second front now owns slot feasibility, and integration is on hold

Fresh pass. `post-milestone-acceptance` is `65e5531`, clean and identical to
`origin/post-milestone-acceptance` after an explicit fetch. `main` is `c0ecd7c`
on `origin`, untouched, checked with `git ls-remote` because this host's fetch
refspec tracks only the accumulation branch. Re-claimed from `coord-3a9c`.
Nothing was integrated on this pass and `main` was not touched.

**The headline is a negative result, and it is the useful thing this pass
found.** The reserve-shape fix is general, it does what it was designed to do,
and it still does not reach the requested wording. From `2f7a11`'s own
default-path release measurement, with the continuity clause it added on top
of `a58a49b`:

```text
                        parent fe4ad78   branch (with continuity clause)
pool                            17907                    17648
distinct words                   2014                     1970
canonical structure members         47                       47
max off-modal slots in a member      3                       4
members at >=3 off-modal              4                       5
pairs of the requested wording's
  own words (six pairs)              0                       0
```

So the general property this item's criterion 4 asks a test to express — a
*pairing* of deep alternatives in different slots is reachable in the pool,
not merely each alternative alone — is now measurably true, where before the
reserve could not express the shape at all. **Criterion 3 is still not met**
and criterion 1 is still not met. The reserve's tuple space is no longer the
binding constraint, so this front must not keep widening it.

**What the same measurement says the gate actually is.** Per-word, per-position
membership in that pool (`occurrences / number of distinct slot positions the
word ever occupies`), which is the form the discriminating hypothesis needs:

```text
              parent                      branch
hits            46 / 1                     48 / 1
dupe            14 / 3                     10 / 2
hid              5 / 2                      3 / 1
came           165 / 5                    167 / 5
taught          11 / 3                      7 / 2
justice        8139 / 3                   8029 / 3
```

`hits` occupies exactly one slot position and `hid` exactly one, while the
requested wording needs `hid` at slot 3. Combined with the baseline front's
independent non-canonical witness — `taught` on `put it back on the shelf`, 28
members, **leading-slot only** within its structure — the general class is the
one the baseline named, now unmasked as the *binding* one:

> a word that is well-populated in a boundary structure, and affordable in a
> four-deep shape, can still be confined to a single slot class within that
> structure.

The leading slot has the largest affordability slack by construction, because
`build` compares each tuple's total substitution cost against `total_budget`
additively and the slack for slot *k* is `sum_{j != k} m_j + cost_{k,i}`. A word
that is deep in the ranked order is therefore affordable early and unaffordable
late for reasons that have nothing to do with the word's quality. The three
candidate gates that distinguish this — list construction (per-slot width,
rotation, phase offset, opening-width cut), sweep stride (`sweep_index` gives a
class's slots one index derived from the class's widths), and the additive cost
bound — are the next question, and the branch's own cost accounting points at
the third: 945 of 6938 offered profile tuples cost-rejected (24.4%) against 354
of 3404 (18.3%) on the parent, and the rejected ones are the deepest.

**The second front is open, and it is deliberately fenced off from this one.**

| agent | branch | worktree | front |
| --- | --- | --- | --- |
| `2f7a11` | `madgab-pairing-2f7a10` | `/workspace/madgab-pairing` | finish, validate and report the reserve-shape change honestly; land and push it; no further widening |
| `3a9c21` | `scratch/2f7a10-verify` | `/workspace/madgab-verify-3a9c21` | independent default-path measurement: six-target membership before/after, criterion 7 interleaved wall clock, reviewer verdict |
| `5b7c04` | `scratch/2f7a10-slots` | `/workspace/madgab-slots` | **new**: per-slot candidate-list / sweep-index / additive-cost diagnosis, the three-gate discrimination, the multi-target confinement table, and the property a fix must satisfy |

`5b7c04` was created at `65e5531` and is measurement-only: it may not modify
`src/` or `tests/`, may not merge anything, and may never merge
`scratch/2f7a10-base`. It is the direct successor to the retired `2f7a12` arm's
role, and it is fenced against `2f7a11`'s territory precisely so the two can
run concurrently instead of contending on `coverage_tuples`. It was given the
parent and branch numbers verbatim so its comparison is against measured arms
rather than a re-derivation, and was told that "the word is offered and swept
in every slot and the gate is elsewhere" is an acceptable and useful result.
Splitting here is what the guide asks for: the previous note named this front
as unavailable only because it needed a pushed commit to compare, and
`a58a49b` is pushed.

**The review criterion 2 asks for, re-run here on the rebased tip rather than
inherited from the 17:06Z note.** `7c70784` is a rebase of `a58a49b` onto
`bbfb94a`, so `a58a49b..7c70784` is **docs-only** and `src/lib.rs` there is
byte-identical to `a58a49b`'s. The fence scan was re-run against the change
itself, `fe4ad78..a58a49b`: 179 added lines, **0** of which contain any
acceptance word or phrase or any `ZZ_` / `zz_` / `MADGAB_` token, and the only
file touched is `src/lib.rs` — so no `tests/` change, neither acceptance test
relaxed or re-baselined, nothing in `axes::*`, `select_diverse`,
`src/adjacency.rs` or the share cap. The eight `MADGAB_TRACE` occurrences in
`src/lib.rs` are pre-existing (the same eight are on `65e5531`) and none is
added by this change. Generality criterion 2 is met; that was never the
question.

**Durability, and one hazard still open.** `7c70784` was single-copy on the
agent's worktree, so it is now pushed to
`madgab-pairing-2f7a10-rebase-backup` on `origin` as a copy only — nothing may
merge or rebase onto it. `origin/madgab-pairing-2f7a10` is still `a58a49b`, so
the agent's own push will need `--force-with-lease` because of the rebase; that
was requested explicitly rather than left to be discovered. The four
`examples/zz_*.rs` probes are untracked in `/workspace/madgab-pairing` and are
still off the branch.

**Integration is on hold, and the condition for lifting the hold is now
written down** so the next pass does not argue it again from scratch:

1. the six-target before/after membership table from `3a9c21`, with every
   witness that regressed to zero named — `tickets` on
   `the cat sat on the mat` (3 -> 0) is already measured, with `capture` and
   `copper` also at zero;
2. an account of the −259 pool and −44 distinct words from the reserve's own
   bound (the additive `total_budget` comparison in `build`, and the
   `funded` / `emit_allowance` accounting that turns 256 segmentations into
   512), or an explicit statement that it cannot be recovered within that bound
   and what the constant would then have to be;
3. the criterion-4 test shown **red on `fe4ad78`**, so it is a guard and not a
   restatement of the change;
4. `recognize speech` -> `wreck a nice beach` re-measured on the final code,
   because a pool-changing fix is most likely to trade that guard away.

A change that costs a real target a reachable wording is not a free win, so the
bar for integrating it as a general improvement in its own right is that the
breadth cost is explained rather than waved at. Even if it is integrated on
those terms, this item stays `working`: the milestone is not reached, and the
route to it now runs through `5b7c04`'s slot-feasibility diagnosis.

`[w-2e5b93](w-2e5b93.md)` stays gated for the reason already recorded, and with
one more: the pool has now changed shape three times in four passes, so its
per-axis spread table would have to be re-taken again on a pool that is still
moving.

Next action for a later fresh pass, in priority order:

1. `antonina agent status --id 2f7a11`, `--id 3a9c21`, `--id 5b7c04`, then
   their logs. None of them is a completion signal on its own. All three are
   running now and are meant to be left running.
2. When `5b7c04` reports which of the three gates it is, with the arithmetic,
   that answer is the next *decision*, not the next measurement: it chooses
   between a change to how the per-slot candidate lists are derived, a change
   to the sweep stride, and a change to the additive budget accounting. Each of
   those is a different front and only one of them should be opened.
3. Review the reserve-shape branch against the four integration conditions
   above before integrating it, and re-measure both guards after integration
   rather than only before it.
4. Do not open a third front until one of the three current ones reports. The
   remaining territory is theirs, and manufacturing a backlog is what the
   itinerary warns against.
5. Only once `approximate_finds_classic_madgab_resegmentation` passes on
   `post-milestone-acceptance` on its own merits, with no phrase-specific case
   in `src/`, close this item, [w-4b1e07](w-4b1e07.md) and
   [w-a02d28](w-a02d28.md) together, and re-check every milestone condition in
   the itinerary.

Host notes for whoever picks this up. `git` is configured to sign commits and
`gpg` is absent, so use `git -c commit.gpgsign=false commit`.
`antonina agent prompt` on a running agent needs `--steer`, and the steering
call itself can block for minutes — issue it backgrounded and get on with the
pass. **`/tmp` is `noexec` on this host**: a `cargo build` in a `/tmp/...`
target directory fails with `Permission denied (os error 13)` on
`build-script-build`, which cost the verification front a cycle. `/tmp/opencode`
*is* exec-capable, so build under `/tmp/opencode` or under `/workspace`; for a
second arm, `git archive <sha> | tar -x -C <dir>` gives a pristine tree without
creating a worktree or touching the repository. Verify durability with
`git ls-remote origin`, not with a local remote-tracking ref.

### Addendum, same pass (both fronts answered the steer; two branch facts to record)

Recorded within minutes of the note above, so a later pass does not read the
state as it was when this pass ended.

- `2f7a11` took the land-and-push instruction: `origin/madgab-pairing-2f7a10`
  is now `3b14482`, the continuity clause on top of the reserve change, pushed
  with the rebase's force. `madgab-pairing-2f7a10-rebase-backup` (`7c70784`) is
  the superseded durability copy and nothing may merge or rebase onto it.
- **New branch hazard, same shape as the two this item has already caught:**
  `/workspace/madgab-pairing` is now checked out on a new local branch
  `zzparent` at `fe4ad78`, which is the parent arm the implementer is building
  for the interleaved wall clock. It is a measurement arm, not a line of work:
  **it must never be merged**, and no fix or test belongs on it. This queue has
  now had three passes in a row in which a worker's actual output sat on a
  branch that was not the recorded implementation branch; check
  `git worktree list` and the branch each worker is *on*, not just that its
  branch exists, before treating anything as integrable.
- `3a9c21` has commits on `scratch/2f7a10-verify` (`781fb5f`) and is running its
  second turn; it is still measurement-only and still may not merge
  `scratch/2f7a10-base`.
- `5b7c04` is running in `/workspace/madgab-slots` on `scratch/2f7a10-slots`
  from `65e5531`; it is the front whose answer decides the next move.

No integration, no merge, and `main` untouched at `c0ecd7c` on this pass.

### Pass `coord-2c48`, 17:32Z–17:52Z: the gate is named, the reserve verdict is contested, two fronts are open

Fresh pass. All three fronts from the previous pass have now reported, and the
pair between them does not agree, so this pass resolved what it could by
measurement, opened the one front the previous pass said was waiting on a
decision, and opened one independent front to settle the integration question.

**1. The blocker is now identified, and it is the sweep.** `5b7c04` answered the
question the previous pass said was the next *decision*, and the answer is gate
**(b)**, with the other two refuted by arithmetic rather than by opinion:

- **(a) per-slot candidate list — refuted.** All four requested coordinates are
  present in their slot's ranked list, at ranks 7 / 13 / 99 / 11, all below the
  slot width.
- **(c) additive `total_budget` — refuted.** Total substitution cost of the
  requested tuple is 1.119520 against `total_budget` 1.5, slack 0.380480;
  0 of 14 (parent) and 0 of 15 (after) offered tuples were cost-rejected, and
  the deepest after-arm tuple at 1.4672 was admitted.
- **(b) `sweep_index` — the gate.** In `coverage_tuples`,
  `sweep_index(w, per, nth, phase) = 10 + (nth*stride + phase) % (w - 10)` with
  `stride = ceil(span/per)`: an arithmetic progression over each slot's list,
  **floored at the opening width 10**, with a stride that grows as the slot
  widens, and one draw per shape class. So rank 7 in a width-160 slot is below
  the floor and is never swept in any phase; rank 99 in a width-160 slot is in
  neither the mod-10 head set nor the non-head set; rank 11 in a width-93 slot
  is in neither; and a mid-list rank is reachable only as a class's *shared
  head*, which writes the same index into every member. Single-position
  confinement is therefore the normal case, and a set of deep coordinates at
  mutually unrelated ranks is not expressible at any depth cap.

  The general property, which is what a fix has to serve and what a test should
  name: *a word that is offered in a slot's list and affordable under the
  additive cost bound can still be unreachable in that slot inside any emitted
  tuple, because the reserve's coverage is an arithmetic sweep of the list
  rather than the list itself.* Multi-target witnesses, none of them an
  acceptance phrase: `taught` is confined to the leading slot in all 25
  structures it appears in on `put it back on the shelf`; `aar` to slot 3;
  `tickets` to slot 1 with a single occurrence; `delve` to slot 7; `honour` to
  {1,6}. A separate lever noted but not the gate: a slot whose width is
  `<= LEXICAL_BRANCH_STAGE_0` makes `sweep_index` return `None`, which drops
  every shape class containing it.

  `5b7c04`'s own refutation is the load-bearing sentence for integration:
  **on the reserve-shape arm a 4-deep tuple *is* emitted and the wording is
  *still* absent, so shape was never the binding constraint.**

**2. The reserve-shape integration is contested, so the hold stays.** The two
fronts measured different commits and disagree on the pool guard:

| front | commit | pool guard | pool breadth | verdict |
| --- | --- | --- | --- | --- |
| `3a9c21` | `a58a49b` (**not** the tip) | **FAILS** (green on parent) | −11.5%, −259 members, `tickets` 3→0 | "not defensible" |
| `2f7a11` | `3b14482` (**the tip**) | green | not measured | land and push |

The commit that separates them is `3b14482`, the continuity clause that keeps
each class's first coordinate on the parent's sweep, and `3a9c21` tested
`a58a49b`, the earlier cut that rotated every coordinate. The coordinator ran
the disputed test directly on the tip this pass, in `/workspace/madgab-pairing`
at `3b14482`, release build:

```text
test approximate_pool_reaches_matches_deep_in_a_span ... ok
test approximate_pool_reaches_alternatives_past_the_opening_slot_width ... ok
test result: ok. 2 passed; 0 failed
```

So `2f7a11`'s claim about the tip is true and `3a9c21`'s refutation does not
transfer to it. That settles the guard but **not** the breadth cost: whether
`3b14482` restores the parent's pool baseline or merely shrinks an 11.5% loss is
exactly what is unmeasured, and per the previous pass's own condition 1 an
unexplained breadth cost is not a free win. `2f7a11` has also withdrawn a
number it reported earlier (the "256 → 512 segmentations" figure) and supplied
the real accounting: segmentation counts are identical to the parent on all six
targets, the reserve offers about 2% more tuples, and the loss is refused by
`build`'s additive bound (10.4% → 25.1% on the canonical target), which it
concludes is not recoverable without moving `total_budget` by +35–65%.

**3. Two fronts are open, on separate bases, contending on nothing.**

| agent | branch | worktree | base | front |
| --- | --- | --- | --- | --- |
| `4e8a52` | `madgab-sweep-4e8a52` | `/workspace/madgab-sweep-4e8a52` | `aa662a4` | re-derive the three-gate discrimination on the accumulation base, design and implement the general sweep fix, land it with a red-on-parent regression test at the `coverage_tuples`/`sweep_index` boundary, and measure the seven-target before/after |
| `9d3b17` | `scratch/2f7a10-tip-verify` | `/workspace/madgab-verify-9d3b17` | `3b14482` | settle the contested integration question: six-target membership and breadth at the **tip**, the 11.5% claim attributed to the right commit, guard status, requested-wording presence, interleaved wall clock, full suite |

`4e8a52` is built on `post-milestone-acceptance` rather than on the reserve
branch on purpose: the reserve change is not integrated, and the sweep fix must
be integrable on its own. `9d3b17` is measurement-only, so the two do not
contend on `src/`. Neither may merge anything, and `4e8a52` was told explicitly
not to touch the other fronts' trees or to merge `scratch/2f7a10-base`,
`zzparent`, or `7c70784`. Exactly one implementation front is open, as the
previous pass required.

**4. Branch hazards, fourth occurrence of the same shape.** `/workspace/madgab-slots`
is checked out on `scratch/2f7a10-slots-after`, **not** on the recorded
`scratch/2f7a10-slots`, and that branch carries `7c70784` + `3b14482`; the
recorded branch is at `65e5531` with no worktree. `5b7c04`'s `src/lib.rs`
modification is staged-plus-unstaged and is a read-only, env-gated per-slot trace
(`ZZ_SLOT_OUT`, `zz_slots`), consistent with its measurement-only fence — but it
is uncommitted and unpushed, as are `scratch/2f7a10-slots-after` and `zzparent`.
This queue has now had four passes in a row in which a worker's output sat on a
branch other than the recorded one. Check `git worktree list` *and* the branch
each worktree is actually on before treating anything as integrable. The
unpushed scratch branches were left alone deliberately: they are a finished
front's measurement scaffolding, `9d3b17` re-takes the numbers that matter, and
pushing another front's scratch work is not this pass's call.

**5. State of the milestone.** Unchanged and not met. The requested wording is
still 0 pool members on the canonical target, `approximate_finds_classic_madgab_resegmentation`
is still red (also red on the parent), and `w-a02d28` stays `blocked` on the same
fact. `recognize speech` → `wreck a nice beach` is present and green on every arm
measured this pass, which is the one milestone condition that is already met and
must not be traded away by a pool-changing fix — both new fronts were told to
measure it.

Next action for a later fresh pass, in priority order:

1. `antonina agent status --id 4e8a52` and `--id 9d3b17`, then their logs. Both
   are running and are meant to be left running. Neither a progress message nor
   a green test is a completion signal.
2. `4e8a52`'s step 1 is a gate on its own step 2: if the three-gate
   discrimination does not reproduce on `aa662a4`, the fix is aimed at the wrong
   thing and that is the finding, not a failure to report.
3. Integrate the reserve-shape change only if `9d3b17` shows the breadth cost at
   `3b14482` is either restored or explained. The bar written in the previous
   pass still stands: a change that costs a real target a reachable wording is
   not a free win. If it is explained but not recovered, integrating it is a
   judgement call and belongs on the record, not in silence.
4. Do not open another front. The remaining territory is `4e8a52`'s, and
   manufacturing a backlog is what the itinerary warns against.
5. Only once `approximate_finds_classic_madgab_resegmentation` passes on
   `post-milestone-acceptance` on its own merits, with no phrase-specific case in
   `src/`, close this item, [w-4b1e07](w-4b1e07.md) and [w-a02d28](w-a02d28.md)
   together and re-check every milestone condition in the itinerary.

No integration, no merge, and `main` untouched on this pass. Host notes from the
previous pass still apply: `git -c commit.gpgsign=false commit`; `/tmp` is
`noexec`, so build under `/workspace` or `/tmp/opencode`; `git archive <sha> |
tar -x -C <dir>` for a second arm; verify durability with `git ls-remote origin`;
`antonina agent prompt` on a running agent needs `--steer` and blocks, so issue
it backgrounded.

### Pass `coord-3b6e`, 17:42Z–17:47Z: 2f7a11's report recovered from an untracked file, both open fronts steered, nothing integrated

Fresh pass, five minutes of host clock. `post-milestone-acceptance` is `1507024`, clean and
identical to `origin` after an explicit fetch; `main` is `c0ecd7c` on `origin`, untouched,
checked with `git ls-remote` because this host's fetch refspec tracks only the accumulation
branch. Re-claimed from `coord-2c48`. No integration, no merge, no new front.

**The one durable thing this pass fixed: a finished front's evidence was one reboot from
loss.** `2f7a11` has been closed for two passes and its branch is pushed at `3b14482`, but
its report existed only as an **untracked** file in `/workspace/madgab-pairing`. It is now
committed and pushed on the branch it describes:

```text
82894d0  w-2f7a10: keep the implementer front's report on its own branch
         on origin/madgab-pairing-2f7a10, on top of 3b14482
         REPORT-2f7a11.md only; no src/ or tests/ change
         read it with: git show 82894d0:REPORT-2f7a11.md
```

It is worth more than a report, because it closes three things this item's notes had
recorded as open, and a reviewer should not have to re-derive any of them:

- **criterion 1, at the line level.** The per-slot candidate-list assignment (parent line
  403, now `profile_tuple`) wrote **one index per class**, so every tuple the reserve
  emitted was a *diagonal* of its class's index rectangle and no tuple could hold two
  different non-zero coordinates; and the class walk (parent line 381) with
  `EMIT_PROFILE_MAX_DEEP = 3` meant the 4-deep class `{0,2,3,4}` was the 16th class tried
  against `EMIT_PROFILE_RESERVE = 16` and funded **zero** times. Tuples offered for the
  canonical structure: **15**, all at most 3-deep; tuples needed: **1**, at a shape the
  reserve could not express.
- **the refutation that killed `EMIT_PROFILE_MAX_DEEP` as the gate by arithmetic.** Raising
  it 3 → 5 and changing nothing else gives pool 17,906 → 17,918 and canonical members
  **47 → 47, unchanged**, because the reserve is exhausted inside depth 3. The *order* was
  the gate, not the cap. That is the same conclusion `5b7c04` reached from the other side,
  now with numbers from the implementer.
- **criterion 4, shown red on the parent.**
  `depth_profile_reserve_emits_pairings_not_only_diagonals` is red at phase 0 on the
  parent's `coverage_tuples`, with all 14 emitted tuples printed and every deep coordinate
  equal in every one of them. So it is a guard, not a restatement of the change. The report
  also records why a `tests/`-level test cannot discriminate this: the visible top 50 is
  byte-identical on the parent and the public API does not expose the pre-selection
  enumeration that moves.

It withdraws its own "256 → 512 segmentations" figure and gives the real accounting:
segmentation counts identical to the parent on all six targets, about 2% more tuples
offered, cost-rejected 10.4% → 25.1% on the canonical target, and `spent_emissions` 1.5–3.8%
*below* the parent. Its measured answer is that the breadth is **not** recoverable within the
same constants: the freed allowance is not budget-limited, and the tuples `build` refuses are
the deep 3- and 4-slot ones, so restoring them means moving `total_budget` by +35–65%, which
is a change to what the search considers a good clue rather than to the traversal's tuple
space. It also names its own *first* cut as the one that broke `tickets` and attributes the
restore to the continuity clause, since the first coordinate of every class there is
byte-identical to the parent's.

**Branch hazard, cleared.** `/workspace/madgab-pairing` is back on
`madgab-pairing-2f7a10`, not on `zzparent` as the previous pass found it, and the report plus
`examples/zz_multi.rs` were the only untracked things in it; the probe stays off the branch.
That is the fifth occurrence of this shape in this item — check `git worktree list` *and* the
branch each worktree is actually on before treating anything as integrable.

**Both open fronts were steered, and both are meant to stay running.**

| agent | branch / worktree | state found | steer |
| --- | --- | --- | --- |
| `4e8a52` | `madgab-sweep-4e8a52` / `/workspace/madgab-sweep-4e8a52` | running, minutes old, `src/lib.rs` modified and **uncommitted**, and the branch has **no ref on `origin` at all** | durability first — commit and push, then keep pushing; the fix must stand alone because the reserve change is not integrated; do not add a second test asserting `2f7a11`'s property, and its own test must be red on `aa662a4`; no `ZZ_*`/`zz_*`/`MADGAB_*` scaffolding on the branch |
| `9d3b17` | `scratch/2f7a10-tip-verify` / `/workspace/madgab-verify-9d3b17` | running, `src/lib.rs` instrumented plus two untracked `zz_*` probes, building a timing harness in `/workspace/arms` | task unchanged; the report at `82894d0` is a claim to confirm or refute with its own numbers, not an input; push `scratch/2f7a10-tip-verify` for durability; leave `/workspace/arms` clean, since earlier arms share it |

`4e8a52`'s in-flight diff already discriminates the gate the way `5b7c04` did — it is
rewriting an existing unit test's assertion from `deep.windows(2).all(|w| w[0] == w[1])` to
pairwise-distinct deep coordinates — so it is aimed at the sweep, and its step 1 remains a
gate on its step 2 rather than a formality.

**State of the milestone: unchanged and not met.** The requested wording is still 0 pool
members on the canonical target, `approximate_finds_classic_madgab_resegmentation` is still
red, [w-a02d28](w-a02d28.md) stays `blocked` on that same fact, and
`recognize speech` → `wreck a nice beach` is green on every arm measured — which both fronts
were told to keep measuring, because a pool-changing fix is most likely to trade it away.

Next action for a later fresh pass, in priority order:

1. `antonina agent status --id 4e8a52` and `--id 9d3b17`, then their logs. Both are
   running and are meant to be left running. Neither a progress line nor a green suite is a
   completion signal.
2. Review whatever lands first for generality before integrating anything: no phrase-specific
   case in `src/` or `tests/`, nothing in `axes::*`, `select_diverse`, `src/adjacency.rs`, the
   share cap or `STRUCTURE_FLOOR`, no relaxed or re-baselined acceptance test, no
   `ZZ_*`/`zz_*`/`MADGAB_*`/phase-timing scaffolding on the branch, and the criterion-4 test
   shown **red on the base it claims to fix**.
3. Integrate the reserve-shape change only on `9d3b17`'s tip numbers, with the breadth cost
   either restored or explained. `2f7a11`'s accounting says explained-not-restored, and that
   is a judgement call that belongs on the record rather than in silence.
4. Do not open another front. The remaining territory is `4e8a52`'s, and manufacturing a
   backlog is what the itinerary warns against.
5. Only once `approximate_finds_classic_madgab_resegmentation` passes on
   `post-milestone-acceptance` on its own merits, with no phrase-specific case in `src/`,
   close this item, [w-4b1e07](w-4b1e07.md) and [w-a02d28](w-a02d28.md) together and
   re-check every milestone condition in the itinerary.

Host notes unchanged: `git -c commit.gpgsign=false commit`; `/tmp` is `noexec`, so build under
`/workspace` or `/tmp/opencode`; `git archive <sha> | tar -x -C <dir>` for a second arm;
verify durability with `git ls-remote origin`; `antonina agent prompt` on a running agent
needs `--steer` and blocks for minutes, so issue it backgrounded. On this host `setsid` is
absent, so a steering call is backgrounded as
`( nohup antonina agent prompt --id <id> --steer "$(cat file)" > log 2>&1 < /dev/null & )`.

### Pass `coord-5e1a`, 17:50Z-17:57Z: the sweep fix is landed, pushed, reviewed for generality here, and held; its report is durable; the cost-accounting front is open

Fresh pass, seven minutes of host clock. `post-milestone-acceptance` is `a747b1b`,
clean and identical to `origin` after an explicit fetch. `main` is `c0ecd7c` on
`origin`, untouched, checked with `git ls-remote` because this host's fetch
refspec tracks only the accumulation branch. Re-claimed from `coord-3b6e`. No
integration, no merge, no push to `main`.

**1. The implementation front finished and its work is durable.** `4e8a52` exited
0 at 17:52Z. Its change is landed and pushed, verified with `git ls-remote`:

```text
79309a1  w-4e8a52: give each member of a shape class its own sweep rate in the
         coverage reserve          on origin/madgab-sweep-4e8a52, base aa662a4
         src/lib.rs only, +189 -15
acbd6dd  w-2f7a10: keep the sweep front's report on its own branch
         REPORT-4e8a52.md only; read it with: git show acbd6dd:REPORT-4e8a52.md
```

`REPORT-4e8a52.md` was **untracked and one reboot from loss**, the same shape of
hazard the previous pass recovered for `2f7a11`. It is now committed and pushed on
the branch it describes, and the branch's tree is clean apart from the probes,
which stay off it. Sixth occurrence of this hazard shape in this item.

**2. The review criterion 2 asks for, run here on the pushed commit**
(`git diff aa662a4..79309a1`, re-derivable with the same command):

- one file, `src/lib.rs`. No `tests/` change, so neither acceptance test was
  edited, relaxed or re-baselined.
- 189 added lines, **0** of which contain `recognize`, `speech`, `wreck`, `nice`,
  `beach`, `stupid`, `justice`, `dupe`, `hits`, `hid`, `came`, `mad gab`,
  `phrase_signature`, `zz_` or `MADGAB_TRACE`. No added `ZZ_*` / `zz_*` /
  `MADGAB_*` / phase-timing scaffolding; both probes the front used were removed
  from the tree before the commit.
- nothing in `axes::*`, `select_diverse`, its admission order, the share cap,
  `STRUCTURE_FLOOR` or `src/adjacency.rs` is touched, so the scope boundary holds.
- the mechanism is a traversal-shape change, not a scoring or admission change:
  `sweep_index` gains a `member` argument and each member of a shape class draws
  its own index at its own rate, `rate(j) = the next integer > rate(j-1) coprime
  to the span`, taken modulo the span of the class's narrowest slot so every
  member's index is legal everywhere. `member 0` keeps the parent's index
  exactly, so the reserve is a coordinate-wise superset of the old coverage.
  Emissions are unchanged, and `build` still bounds every tuple's total
  substitution cost against `total_budget` additively, so the reserve is not
  unpriced.
- the regression tests are three unit tests at the `coverage_tuples` /
  `sweep_index` boundary over synthetic all-wide width vectors, naming no word,
  phrase or target, and the discriminating clause is shown **red on `aa662a4`**
  (3 failed / 49 passed) and green on the branch (`--lib` 53 passed).

**3. The result is negative for the milestone, and that is the useful part.**

```text
"It's just a stupid game"   pool 17906 -> 17906    aggregate breadth 1047.38 -> 1048.13
recognize speech            pool 15906 -> 15909    aggregate breadth    5.25 ->    5.25
put it back on the shelf    pool 15633 -> 15642    aggregate breadth    7.00 ->    7.00
the cat sat on the mat      pool 16262 -> 16259    aggregate breadth    2.60 ->    2.40
when the rain finally stopped 15610 -> 15605     aggregate breadth    7.00 ->    7.17
a whole lot of trouble      pool 16149 -> 16159    aggregate breadth    2.29 ->    2.33
he was a big fat man        pool 19171 -> 19165    aggregate breadth    1.00 ->    1.00

"wreck a nice beach"                  pool TRUE -> TRUE    top 50 TRUE
"Hits Justice Dupe Hid Came"          pool FALSE -> FALSE top 50 FALSE
```

So: a pairing of deep alternatives **is** expressible in the traversal now, and
the requested wording is still not produced. Three single-occurrence witnesses
are lost (`aar` on the mat, `taught` and `honour` on a whole lot of trouble) and
three words gain a slot position. The front's own verdict: "This is a wash, and
I am not claiming it as an improvement." Wall clock is interleaved against the
parent in one session with the arm order alternated: 2330 -> 2292 ms and
2319 -> 2330 ms, both inside run-to-run noise. Suites green on the tip except
`approximate_finds_classic_madgab_resegmentation`, which is **red on the base
too** and is this item's target rather than a guard.

**4. The decision this pass therefore did not take, and why, written so the next
pass does not argue it again.** `79309a1` is held rather than integrated, and
the hold condition is `e1a3f7`'s number:

> a more expressive schedule is only worth integrating if the expressiveness is
> not paid for in refusals. If the per-member rates buy tuples that `build`'s
> additive cost comparison then throws away, the breadth wash above is
> illusory and the traversal change bought nothing.

That is not a reason to distrust the change — it is general, cheap, safe and
correct on its own terms — it is a reason not to bank it before it is priced.
The front names the same thing as its next measurement: how many offered tuples
are `build`-refused, split by depth, with the per-member-rate sweep in place,
and how many of the refusals have their deep coordinates at *adjacent* rather
than unrelated ranks. The number decides whether the next front is a
**cost-accounting** change or another **coverage** change, and it is a number
only an independent arm can take.

**5. The redundancy between the two candidate branches is now explicit, and it
is a decision the next pass has to make rather than a detail.** `79309a1` and the
reserve-shape arm `3b14482` are **competing fixes for the same defect**, not
additive ones: both exist to stop a shape class writing one index into every
member of itself. `4e8a52` states the relationship itself — its change subsumes
the "not only diagonals" half of `2f7a11`'s criterion-4 test and generalises it
to all-wide vectors, but does **not** subsume the other half, which needs
`EMIT_PROFILE_MAX_DEEP` 3 -> 4 and a class order that funds `{0,2,3,4}`. So
integrating both without deciding would be two edits to `coverage_tuples`
claiming the same property. On today's evidence the sweep fix is the better
candidate to integrate: one file, no cap raised, no class-order change, member-0
continuity, a wash breadth, and a red-on-base test per claim. If the cost front
prices it and it survives, integrate `79309a1` and treat `3b14482` as superseded
except for its depth-cap and class-order parts, which are then a separate,
narrow question.

**6. `9d3b17` is still running and its interim numbers sharpen the same point.**
It has attributed the contested breadth cost to the right commits, on the
default-path pool: at the tip `3b14482` the canonical target loses 297 wordings
and gains 254 (183 lost at both cuts, 114 of `a58a49b`'s losses recovered by the
continuity clause); across its seven targets 1724 lost and 1382 gained. So the
11.5 % shrink the earlier pass recorded belongs to the earlier cut, and at the
tip the reserve-shape change is also approximately a wash on breadth while
likewise not producing the wording. These are numbers from a **running** agent
and are not yet a result; it is writing its report.

**One front is open, and it is deliberately not on the implementation branch.**

| agent | branch / worktree | front |
| --- | --- | --- |
| `9d3b17` | `scratch/2f7a10-tip-verify` / `/workspace/madgab-verify-9d3b17` | finishing the contested integration measurement at `3b14482`; measurement-only |
| `e1a3f7` | `scratch/2f7a10-cost` / `/workspace/madgab-costref` | **new**: per-depth offered / accepted / `build`-refused table for `aa662a4` against `79309a1` on the default release path, the adjacent-versus-unrelated split of the refusals, and the cost-versus-coverage decision |

`e1a3f7` is measurement-only: it may not modify `src/` or `tests/` in any
mergeable sense, may not merge anything, may never merge `scratch/2f7a10-base`,
`zzparent`, `7c70784` or `madgab-pairing-2f7a10`, and must push
`scratch/2f7a10-cost` so its evidence is durable. It was given `4e8a52`'s numbers
so its comparison is against a measured arm rather than a re-derivation, and it
was told that a refutation — expressiveness bought and then refused — is a
first-class success. The two fronts are independent and contend on nothing.

**Durability, one gap closed.** `scratch/2f7a10-tip-verify` (`112c0c5`) had **no
ref on `origin` at all**; it is now pushed. `9d3b17` has uncommitted
instrumentation in `src/lib.rs` plus untracked `zz_*` probes in its worktree,
which is expected for a running measurement front and is not this pass's call to
commit.

**State of the milestone: unchanged and not met.** The requested wording is still
0 pool members on the canonical target,
`approximate_finds_classic_madgab_resegmentation` is still red (and red on the
base), [w-a02d28](w-a02d28.md) stays `blocked` on that same fact, and
`recognize speech` -> `wreck a nice beach` is green on every arm measured — which
`e1a3f7` was told to keep measuring.

Next action for a later fresh pass, in priority order:

1. `antonina agent status --id e1a3f7` and `--id 9d3b17`, then their logs. Both
   are running and are meant to be left running. Neither a progress line nor a
   green suite is a completion signal.
2. On `e1a3f7`'s per-depth refusal table, take the integration decision on
   `79309a1` in section 4 above and record it either way: integrated with the
   guards re-measured afterwards, or held with the reason. Re-run the fence scan
   (`git diff aa662a4..79309a1 --name-only` is `src/lib.rs`, and the phrase and
   `ZZ_*` scans are empty) before treating it as integrable, and never push or
   merge it to `main`.
3. Take the redundancy decision in section 5 explicitly, so `3b14482` and
   `79309a1` are never both integrated as if they were additive.
4. Do not open an implementation front until `e1a3f7` names cost accounting or
   coverage as the next territory, and open **one** of them. The remaining
   territory is not the coordinator's to manufacture.
5. Only once `approximate_finds_classic_madgab_resegmentation` passes on
   `post-milestone-acceptance` on its own merits, with no phrase-specific case in
   `src/`, close this item, [w-4b1e07](w-4b1e07.md) and [w-a02d28](w-a02d28.md)
   together and re-check every milestone condition in the itinerary.

Host notes unchanged: `git -c commit.gpgsign=false commit`; `/tmp` is `noexec`,
so build under `/workspace` or `/tmp/opencode`; `git archive <sha> | tar -x
-C <dir>` for a second arm; verify durability with `git ls-remote origin`, not a
local remote-tracking ref; `antonina agent prompt` on a running agent needs
`--steer` and blocks for minutes, so background it as
`( nohup antonina agent prompt --id <id> --steer "$(cat file)" > log 2>&1 < /dev/null & )`.

### Addendum, same pass, two minutes later: `9d3b17` has finished, its report is durable, and it settles the reserve-shape arm

Correcting section 6 above, which recorded its numbers from a still-running agent: `9d3b17`
exited 0 at 17:54Z. Its probe commit is pushed (`9f95a9c`) and its report was **untracked**,
so it is now committed and pushed on its own branch:

```text
011dc82  w-2f7a10: keep the reserve-tip front's report on its own branch
         on origin/scratch/2f7a10-tip-verify; REPORT-9d3b17.md only
         read it with: git show 011dc82:REPORT-9d3b17.md
```

Seventh occurrence of this hazard shape in this item. **Check `git worktree list` and the
branch each worktree is on, and the status of each front's report file, before treating any
front's evidence as durable.**

Its verdict on the contested question, measured on the deduplicated default-path pool of a
release build, seven targets, with the probe proven non-intrusive (`cmp` of visible stdout
with and without it) and arms built by `git archive`, never by merging:

- **it confirms `2f7a11`'s counter table to the digit** — canonical produced 3404 -> 3469,
  built 3049 -> 2598, cost-rejected 355 -> **871**, spent 15 204 -> 14 977, segmentations
  **256 -> 256**. So the reserve-shape change did not lose breadth to fewer segmentations
  or a smaller allowance (`allowance_stop` is 0 on all seven targets on all three arms): it
  bought about 2 % more tuples and had **10.4 % -> 25.1 %** of them refused by `build`'s
  additive `total_budget`. The mechanism, not the size of the search, is what changed.
- **it attributes the contested regressions to the right commit.** The 11.5 %-style
  breadth loss and the `tickets` 3 -> 0 regression are properties of `a58a49b`; at
  `3b14482` the pool guard is green, `tickets` is restored, and the visible top 50 is
  byte-identical to the parent on all seven targets.
- **and it contradicts the implementer's own "no other witness regressed" claim.** At the
  tip, **1 724 (target, word) pairs are present on the parent and absent at the tip**, 1 071
  of them lost on both cuts: `capture` 1 -> 0, `copper` 1 -> 0, `tons` 10 -> 0, `zip` 9 -> 0,
  `did` 8 -> 0, `fan` 7 -> 0, `sea` 7 -> 0, `tie` 6 -> 0, `sit` 6 -> 0, `one` 5 -> 0,
  `sack` 5 -> 0, `chess` 5 -> 0, `cal` 5 -> 0, `golf` 5 -> 0. That is a real breadth cost
  and it is measured, not argued.
- `hid` and `hits` are back to **leading-slot-only** at the tip, i.e. the property this item
  exists to remove is not advanced on the canonical target by `3b14482`, and the requested
  wording is still absent.

**So section 5's redundancy decision can now be made, and this pass makes it: the
reserve-shape arm `3b14482` is not integrable.** It costs a thousand-plus reachable
(target, word) pairs, it does not move the property the item is about, and it does not
produce the wording. It is superseded for integration purposes by the sweep fix's approach;
if anything of it is wanted later, the only part that is not a second copy of the sweep fix
is the depth-cap and class-order change (`EMIT_PROFILE_MAX_DEEP` 3 -> 4 and an order that
funds `{0,2,3,4}`), and that is a separate, narrow question to open only on evidence.
`madgab-pairing-2f7a10` stays pushed for the record and nothing merges it. Marking that arm
superseded is left to the next pass so it is one deliberate commit rather than a note.

`e1a3f7` is now the **only** running front, and the next-action list above stands with the
`9d3b17` entries deleted. The integration decision on `79309a1` still waits on its per-depth
refusal table, and that is the single most useful thing a later pass can move.

### Pass `coord-6f04`, 2026-09-26T18:0xZ: the held fix gets a second, independent front

Fresh pass. Nothing in this item's own state moved: the accumulation head is still `1d90c82`,
and `git diff --name-only aa662a4..1d90c82` is `docs/work/items/w-2f7a10.md` and
`docs/work/items/w-4b1e07.md` only, so **`src/` is byte-identical to the base the held fix
was written against** and `79309a1` still applies to the current head with no drift. The
milestone is still unmet: the requested wording is 0 pool members, the acceptance test is
still red, and `recognize speech` -> `wreck a nice beach` is still the one green canonical
guard.

`9d3b17`'s work is finished, reported and durable at `011dc82`
(`git show 011dc82:REPORT-9d3b17.md`); `e1a3f7` is **running** (it is mid-measurement,
building a parent arm out of `git archive` and proving its probe non-intrusive with an
`IDENTICAL` stdout comparison). Both were left running; neither was steered, because
`agent prompt --steer` blocks and nothing in the front's task needed changing.

**The deliberate commit the last pass deferred.** The reserve-shape arm is recorded here as
closed for integration purposes, so a later pass does not reopen it: `3b14482` on
`madgab-pairing-2f7a10` is **not integrable and not to be merged or cherry-picked**. It costs
1 724 (target, word) pairs present on the parent and absent at the tip, it does not advance
the leading-slot property this item exists to remove, and it does not produce the wording. It
is not marked `superseded` in its own metadata because the *item* it belongs to
([w-6b2f04](w-6b2f04.md)) is already `done` and its finding is recorded there; the branch
stays pushed for the record. The only part of it that is not a second copy of the sweep fix
is the depth-cap/class-order change (`EMIT_PROFILE_MAX_DEEP` 3 -> 4, an order funding
`{0,2,3,4}`), and that stays a separate narrow question to open only on evidence.

**New front `7d1c04`**, worktree `/workspace/madgab-verify-7d1c04`, branch
`madgab-sweep-verify-7d1c04` at `79309a1`. Chosen because the integration decision on
`79309a1` has two independent halves and only one of them was being produced. `e1a3f7` owns
the *price* (per-depth refusal accounting). `7d1c04` owns the *guards*: a mechanical fence
scan of the added lines for canonical phrase/tokens and `ZZ_`/`MADGAB_` scaffolding, a
line-level generality argument that the new sweep rate is a property of shape classes rather
than of one input, `cargo test --release --lib` plus the approximate-mode integration tests
with the base arm built by `git archive` to separate pre-existing red from new red, whether
the visible top-50 output on >= 4 non-canonical targets is byte-identical to the base arm, and
a refutation-first verdict of INTEGRABLE-NOW / INTEGRABLE-WITH-GUARDS / NOT-INTEGRABLE. It is
told to report a blocker as a blocker and not to fix it, and to push `REPORT-7d1c04.md` on its
own branch. The two fronts contend on no file: `7d1c04` writes only its report, `e1a3f7` is
measurement-only on `scratch/2f7a10-cost`.

Next action for a later fresh pass, in priority order:

1. `antonina agent status --id e1a3f7` and `--id 7d1c04`, then their logs. Both are meant to
   be left running; a progress line and a green suite are not completion signals. Check
   `git worktree list` and each worktree's branch, and whether each report file is tracked,
   before treating either front's evidence as durable — that hazard has now recurred seven
   times in this item.
2. When `e1a3f7`'s per-depth refusal table lands, take the integration decision on `79309a1`
   and record it either way, combining it with `7d1c04`'s guard verdict. Re-run the fence scan
   before treating the fix as integrable, and never push or merge it to `main`.
3. Keep `3b14482` closed for integration, as recorded above.
4. Do not open an implementation front until `e1a3f7` or `7d1c04` names cost accounting or
   coverage as the next territory, and open **one** of them. The remaining territory is not
   the coordinator's to manufacture.
5. Only once `approximate_finds_classic_madgab_resegmentation` passes on
   `post-milestone-acceptance` on its own merits, with no phrase-specific case in `src/`,
   close this item, [w-4b1e07](w-4b1e07.md) and [w-a02d28](w-a02d28.md) together and
   re-check every milestone condition in the itinerary.

### Pass `coord-4c1e`, 2026-09-26T18:0xZ: both fronts alive, the guard half has already produced its headline, and durability is re-issued

Fresh pass, 18:01Z start. Durable state inspected: the accumulation head is `a7e8d7a` and is
**identical to `origin/post-milestone-acceptance`** with a clean worktree, and
`git diff --name-only aa662a4..a7e8d7a` is `docs/skills/scheduled.md`, this item and
[w-4b1e07](w-4b1e07.md) only. So **`src/` is still byte-identical to the base the held sweep
fix was written against** and `79309a1` still applies to the current head with no drift.
`main` was not touched by this pass.

Both open fronts are **running and alive**, and both were left running. Neither was stopped,
and no new implementation front was opened, per rule 4 below.

**The guard half has already produced its decisive negative, and it is worth recording now
rather than waiting for the report.** From `7d1c04`'s log, on its own two arms (fix `79309a1`
and a `git archive` base): `approximate_finds_classic_madgab_resegmentation` is **red on both**,
and `cargo test --release --lib` is **50 passed / 0 failed on both**. So:

- the one red acceptance test is **pre-existing red, not new red introduced by the sweep fix** —
  the fix's "three unit tests red on base, green on branch" claim is about unit tests and does
  not extend to the canonical acceptance test;
- therefore **`79309a1` is not the gate for the canonical resegmentation.** At best it is a
  general breadth/quality improvement, and the integration decision on it is a general-quality
  call, not a milestone call. That decision still needs `e1a3f7`'s per-depth refusal table
  and `7d1c04`'s fence/generality/non-regression verdict, but the shape of the decision is
  now known and should not be re-litigated as if `79309a1` could close the milestone.
- combined with `3b14482` being closed above, **both candidate fixes for the sweep/reserve
  defect are now non-milestone fixes.** The milestone gate is unchanged: the leading-slot /
  slot-feasibility property plus the refusals charged by `build`'s additive `total_budget`.

From `e1a3f7`'s log, mid-measurement, on the canonical target with interleaved runs of both
arms: branch 1603/1578/1527/1991/1811/1366 ms versus parent 1598/1597/1338/1405/1463/1430 ms
— medians about 1590 ms versus about 1448 ms, ranges overlapping, so the sweep fix is inside
host noise on this axis rather than a demonstrated cost. This is provisional; the priced
per-depth refusal table is the deliverable, not the wall clock.

**Durability hazard, eighth occurrence, now actively mitigated.** `7d1c04`'s worktree is
currently clean (no report file yet), and `e1a3f7`'s worktree holds an **uncommitted** probe
(`M src/lib.rs` plus untracked `examples/costpool.rs`). That is exactly the shape that has
lost evidence seven times in this item. Both fronts have been steered, in the background and
non-blocking, with the same two instructions: (a) commit the probe or test patches **and** the
report on their own branch and push with `git -c commit.gpgsign=false commit`, so the next
coordinator can read `git show <branch>:REPORT-<id>.md`; (b) the exact shape of the deliverable
they owe — `7d1c04` the refutation-first INTEGRABLE-NOW / INTEGRABLE-WITH-GUARDS /
NOT-INTEGRABLE verdict with the named guards, the fence scan, and visible top-50
non-regression on >= 4 non-canonical targets; `e1a3f7` the per-depth, per-shape-class refusal
table on the canonical target plus two non-canonical ones, and whether the reserve arm's
10.4 % -> 25.1 % refusal rate is improved, unchanged, or worsened by `79309a1`. Both were told
to report a blocker as a blocker, to implement no fix, to merge nothing to `main`, and to leave
the accumulation branch alone.

Next action for a later fresh pass, in priority order:

1. `antonina agent status --id e1a3f7` and `--id 7d1c04`, then their logs. When a front exits,
   confirm its report is **tracked and pushed** on its own branch before treating the evidence
   as durable: `git show madgab-sweep-verify-7d1c04:REPORT-7d1c04.md` and
   `git show scratch/2f7a10-cost:REPORT-e1a3f7.md`. A progress line and a green suite are
   not completion signals, and the acceptance test's redness is pre-existing on both arms, so a
   green `--lib` run must not be read as milestone progress.
2. Take the integration decision on `79309a1` and record it either way, combining the two front
   verdicts, on the framing established above: it is a general-quality candidate, not the
   milestone gate. Re-run the fence scan before treating it as integrable. Never push or merge
   it to `main`.
3. Keep `3b14482` closed for integration, as recorded above.
4. Open **one** implementation front, on territory named by the landed evidence rather than
   manufactured: the milestone gate is the leading-slot / slot-feasibility property and the
   `build` additive `total_budget` refusal rate, and the best territory name will come from
   `e1a3f7`'s table showing which depths and shape classes are refused. Do not open it before
   that table is durable, and do not let it contend with either front's files.
5. Only once `approximate_finds_classic_madgab_resegmentation` passes on
   `post-milestone-acceptance` on its own merits, with no phrase-specific case in `src/`,
   close this item, [w-4b1e07](w-4b1e07.md) and [w-a02d28](w-a02d28.md) together and
   re-check every milestone condition in the itinerary.

### Pass `coord-8f0a`, 2026-09-26T18:06Z-18:11Z: the guard front is recovered and closed, the hold on `79309a1` is decided in writing, and the price front is the only thing still in flight

Fresh pass, about five minutes of host clock. `post-milestone-acceptance` is `39b2a11`, clean
and identical to `origin` after an explicit fetch; `main` was not touched. `git diff
--name-only aa662a4..post-milestone-acceptance` is `docs/skills/scheduled.md`, this item and
[w-4b1e07](w-4b1e07.md) only, so **`src/` is still byte-identical to the base the held sweep
fix was written against** and `79309a1` still applies to the current head with no drift.

**1. Front `7d1c04` is finished, its report is durable, and its slot is closed.** It exited 0
at about 17:58Z. Its report was recovered onto its own branch and pushed, so the ninth
occurrence of this item's repeated hazard shape is closed too:

```text
6101202  w-2f7a10: validation report 7d1c04 - headline, runtime medians, fence scan, guards
         on origin/madgab-sweep-verify-7d1c04; REPORT-7d1c04.md only
         read it with: git show madgab-sweep-verify-7d1c04:REPORT-7d1c04.md
```

Its verdict is **INTEGRABLE-NOW** for `79309a1`, on: an empty fence scan (189 added lines,
`HITS=0`; `--test no_phrase_hard_coding` 6/6; the only `eq`-like line is an integer bound
check on a slot index against a slot width); `--lib` 53/0 against 50/0 on a
`git archive aa662a4` base arm; `exact_determinism` 1/0, `approx_determinism` 2/0;
`corpus_integration` 10 pass / 1 fail with that 1 fail **reproduced red on the base arm**;
visible top 50 **byte-identical on all 7 targets**, 0 changed; and runtime medians +1.3 % to
+7.7 % tip over base, all inside a 25-40 % run-to-run spread.

**The fence scan was re-run by this coordinator on the pushed commit rather than inherited**,
scanning `git diff aa662a4..79309a1` with `node` (this host has no `rg`/`grep` on `PATH` for
the report author either):

```text
git diff aa662a4..79309a1 --name-only   ->  src/lib.rs        (one file; no tests/ change)
added=189  HITS=0                        (no acceptance word, no ZZ_/MADGAB_/zz_/env read)
```

So no acceptance test was edited, relaxed or re-baselined, and nothing in `axes::*`,
`select_diverse`, `src/adjacency.rs` or the share cap is touched.

**2. The integration decision on `79309a1`, taken here and recorded: HELD, one more time, and
now for a stated reason rather than a pending one.** The previous pass's hold condition was
`e1a3f7`'s per-depth refusal table, and that table is still not durable, so the condition is
unmet. The report itself supplies the reason the hold is the right call rather than a
formality, in its own second caveat:

> continuity is coordinate-level, not tuple-level: `member 0` reproduces the parent index
> exactly, but the branch's per-member legality check **can refuse a diagonal the parent
> emitted**, so the reserve's tuple count is not a superset of the parent's.

That is exactly the regression class that disqualified the competing arm `3b14482` on the
previous pass - 1 724 (target, word) pairs present on the parent and absent at the tip - and
`7d1c04` measured **no pool or cost side at all**. So the single unmeasured risk in this change
is the same risk that killed the other candidate, and the only measurement that resolves it
is the one already running. `79309a1` remains a **general breadth/quality candidate, not the
milestone gate**: the acceptance test is red on the fix and red on the base, and the visible
top 50 is unchanged on 7 of 7 targets.

**3. `e1a3f7` is the only front in flight and is left running.** It is `running` in
`/workspace/madgab-costref` on `scratch/2f7a10-cost` at `79309a1`, with its probe still
**uncommitted** (`M src/lib.rs`, `?? examples/costpool.rs`, `?? measurements/`) and its
branch with **no ref on `origin` at all**. That is the tenth occurrence of the same hazard
shape, so it is named here as the single most valuable thing a later pass can do: when it
exits, confirm `REPORT-e1a3f7.md` is tracked and pushed so
`git show scratch/2f7a10-cost:REPORT-e1a3f7.md` works. It was **not** steered this pass -
`agent prompt --steer` blocks for minutes and the previous pass's steer already states the
exact deliverable and the durability instruction.

**4. No new front was opened**, per rule 4: the next implementation territory must be named by
`e1a3f7`'s durable per-depth refusal split (which depths and shape classes `build` refuses),
and manufacturing a backlog is what the itinerary warns against. `7d1c04`'s slot is now free
but the territory it vacated - the guard half - is delivered, not repeated.

**State of the milestone: unchanged and not met.** The requested wording is still 0 pool
members on the canonical target, `approximate_finds_classic_madgab_resegmentation` is still
red on both the base and the fix, [w-a02d28](w-a02d28.md) stays `blocked` on that same fact,
and `recognize speech` -> `wreck a nice beach` is green on every arm measured, which is the
one milestone condition already met and must not be traded away by a pool-changing fix.

Next action for a later fresh pass, in priority order:

1. `antonina agent status --id e1a3f7`, then its log. Check
   `git show scratch/2f7a10-cost:REPORT-e1a3f7.md` and `git ls-remote origin
   scratch/2f7a10-cost` **before** treating its numbers as durable - that branch currently has
   no remote ref at all. A progress line and a green suite are not completion signals.
2. Take the integration decision on `79309a1` on the two verdicts together, and **record it
   either way**: integrated with both guards re-measured afterwards, or held with the reason.
   Re-run the fence scan first. Never push or merge it to `main`.
3. Keep `3b14482` closed for integration, as recorded above.
4. Open **one** implementation front, on the territory `e1a3f7`'s table names - cost
   accounting if the refusals are the binding constraint, coverage if they are not - and not
   before that table is durable.
5. Only once `approximate_finds_classic_madgab_resegmentation` passes on
   `post-milestone-acceptance` on its own merits, with no phrase-specific case in `src/`,
   close this item, [w-4b1e07](w-4b1e07.md) and [w-a02d28](w-a02d28.md) together and re-check
   every milestone condition in the itinerary.

Host notes unchanged: `git -c commit.gpgsign=false commit`; `/tmp` is `noexec`, so build
under `/workspace` or `/tmp/opencode`; `git archive <sha> | tar -x -C <dir>` for a second
arm; verify durability with `git ls-remote origin`, not a local remote-tracking ref;
`antonina agent prompt` on a running agent needs `--steer` and blocks for minutes, so
background it as `( nohup antonina agent prompt --id <id> --steer "$(cat file)" > log 2>&1 <
/dev/null & )`. `grep`, `sed` and `python3` are **not** on this host's `PATH`; the usable
ones are `/gnu/store/2daxpqdg74pan6bp78cp6jzknb435w9d-coreutils-9.1/bin` and
`/gnu/store/05vy146ycavwjm8va1d8qym188s14vzv-grep-3.11/bin`, and `node` works for scanning.

### Pass `coord-9b4e`, 2026-09-26T18:21Z-18:25Z: the hold on `79309a1` is released on the measurement that was asked for, the item's real gate is named, and three independent fronts are in flight

Fresh reconciliation pass, about four minutes of host clock.
`post-milestone-acceptance` is `2bacbcb`, clean and identical to `origin` after an explicit
`git fetch --all --prune`; `main` was not touched. No Antonina agent was running at the
start of this pass (`antonina agent list` shows 176 `succeeded` and 2 `failed`, none
`running`), so nothing was left unsupervised or duplicated.

**1. `e1a3f7` is finished, its report is durable, and it is the single most valuable
measurement the repository has produced.** It exited 0 at 18:09Z; the previous pass's named
hazard (report committed but unpushed, branch with no remote ref) did **not** recur:

```text
74e3d8b  w-2f7a10 e1a3f7: measure the coverage reserve's cost refusals per depth, parent vs 79309a1
git ls-remote origin scratch/2f7a10-cost  ->  74e3d8b
git show scratch/2f7a10-cost:REPORT-e1a3f7.md
```

Four results, and they decide this item's next territory:

- **The hold condition on `79309a1` is now met, and the hold is released.** `7d1c04` gave
  the fix an INTEGRABLE-NOW verdict on every guard but measured **no pool or cost side at
  all**, and warned that the branch's per-member legality check "can refuse a diagonal the
  parent emitted" - the same pool-regression class that disqualified the competing arm
  `3b14482` (1 724 parent-present pairs absent at the tip). `e1a3f7` measured exactly that,
  as default-path pool membership on 12 targets, release, parent `aa662a4` vs `79309a1`:
  **pool sizes are equal or larger on every target** - canonical 19 601 -> 19 601,
  `recognize speech` 17 078 -> 17 083, the other ten likewise, with `he was a big fat man`
  and `there is no way to know` capped at 20 000 on both arms. The risk is measured absent.
- **The fix is not more refusable than what it replaced.** Reserve offers are identical
  (45 387 both arms); built 36 524 -> 36 653 (+0.35 %), depth >= 2 21 447 -> 21 572 (+0.58 %);
  deep refusal rate 26.12 % -> 25.69 %, with 86.9 % of deep refusals now at unrelated ranks
  versus 0 on the parent. The canonical target's 10.40 % -> 10.28 % is **unchanged** in
  substance, which answers the previous pass's open question 7 outright.
- **`recognize speech` -> `wreck a nice beach` survives on both arms.** The one milestone
  condition already met is not traded away.
- **Cost accounting is refuted as the next front, and the real gate is named.** The reserve
  offers **0 depth-4 tuples out of 45 387 on both arms**, because `EMIT_PROFILE_MAX_DEEP = 3`
  (`src/lib.rs:212`) is reached before the traversal and before `build` is called. The
  requested clue is a **four-deep** shape (slots 0, 3, 4, 5); its cost was measured by
  `4e8a52` at **0.919520 <= `total_budget` 1.5**, and 4 692 accepted tuples are already
  priced at <= 0.9, so it *would* be accepted by `build` if it were ever offered. Per-depth
  refusal rates are 7.8 % / 23.4 % / 47.3 % at depth 1 / 2 / 3, mean accepted cost
  1.078 / 1.217 / 1.297 against a 1.5 budget, because the *baseline* tuple already costs
  ~1.0 - so the headroom that a deep coordinate spends is ~0.4, not 0.5, and depth 3 is
  already refused three times in five.

`79309a1` is therefore recorded as **necessary and not sufficient**: with the sweep fixed the
requested pairing is expressible in principle only at depth <= 3, and the requested shape is
depth 4. Its own yield through the cost bound is +0.35 % of the reserve's output and the
visible top 50 is unchanged on 7 of 7 targets, so it is a general breadth/quality candidate
and not the milestone gate.

**2. Three independent fronts are open, launched in parallel, and left running.** They are
split by file and by base, so none contends with another:

| front | branch | base | worktree | owns |
|---|---|---|---|---|
| `a3f19c` | `madgab-int-79309a1` | `post-milestone-acceptance` `2bacbcb` | `/workspace/madgab-int-a3f19c` | integrate `79309a1` and **re-measure its guards independently**; re-run the fence scan; pool/`--top 50`/runtime tables; generality review. Explicitly scoped **not** to touch the depth cap, cost accounting, scoring or ranking |
| `b47d02` | `madgab-depth4-b47d02` | `74e3d8b` (the probe) | `/workspace/madgab-depth4-b47d02` | the one measurement `e1a3f7` section 11 named: `EMIT_PROFILE_MAX_DEEP = 4`, nothing else, read the depth-4 row; pool delta; whether depth-4 offers reach the requested shape at all; runtime. **Measurement only, never merged** |
| `c81e55` | `madgab-depthcap-c81e55` | `post-milestone-acceptance` `2bacbcb` | `/workspace/madgab-depthcap-c81e55` | the design question the numbers raise but do not answer: what **general** mechanism makes a four-deep resegmentation reachable and correctly ranked, rather than raising a constant. Reproduces current behaviour, measures a mechanism shortlist, recommends the next implementation front with guards and a regression test. **No implementation** |

`a3f19c` merging, `b47d02` measuring and `c81e55` designing are the three useful things that
can happen at once here, and none of them blocks another. All three were told to report a
blocker as a blocker, to implement no fix beyond their scope, to merge nothing to `main` or
to `post-milestone-acceptance`, to keep the red canonical test red, and to **push their
report to their own branch** - the tenth-recurring hazard in this item is a report that is
committed but unpushed.

**3. State of the milestone: unchanged and not met.** The requested wording is still 0 pool
members on the canonical target, `approximate_finds_classic_madgab_resegmentation` is still
red on the base and on the fix, [w-a02d28](w-a02d28.md) stays `blocked` on that same fact,
and `recognize speech` -> `wreck a nice beach` remains green. A green `--lib` run must not be
read as milestone progress.

Next action for a later fresh pass, in priority order:

1. `antonina agent status --id a3f19c`, `--id b47d02`, `--id c81e55`, then their logs. For
   each, confirm durability **before** treating anything in it as evidence:
   `git ls-remote origin madgab-int-79309a1` with
   `git show madgab-int-79309a1:REPORT-a3f19c.md`;
   `git ls-remote origin madgab-depth4-b47d02` with
   `git show madgab-depth4-b47d02:REPORT-b47d02.md`;
   `git ls-remote origin madgab-depthcap-c81e55` with
   `git show madgab-depthcap-c81e55:REPORT-c81e55.md`.
   A progress line and a green suite are not completion signals.
2. If `a3f19c` reports the integration clean with its guards re-measured, **merge
   `madgab-int-79309a1` into `post-milestone-acceptance` and push that branch**, keeping
   `REPORT-a3f19c.md` out of the accumulation branch if you prefer a docs-only footprint.
   Re-run the fence scan on the merged result. Never push or merge it to `main`.
3. `3b14482` stays closed for integration, as recorded above.
4. Open the implementation front **only** from `b47d02`'s depth-4 row and `c81e55`'s
   recommendation, and only once both are durable. Do not manufacture the territory in
   advance: the depth-4 offered/accepted/refused triple and the pool delta decide between
   "lift the cap and pay for it" and "lift the cap *and* change how a deep tuple is priced",
   and that is not yet answered.
5. Only once `approximate_finds_classic_madgab_resegmentation` passes on
   `post-milestone-acceptance` on its own merits, with no phrase-specific case in `src/`,
   close this item, [w-4b1e07](w-4b1e07.md) and [w-a02d28](w-a02d28.md) together and
   re-check every milestone condition in the itinerary.

Host notes unchanged: `git -c commit.gpgsign=false commit`; `/tmp` is `noexec`, so build
under `/workspace` or `/tmp/opencode`; `git archive <sha> | tar -x -C <dir>` for a second
arm; verify durability with `git ls-remote origin`, not a local remote-tracking ref;
`antonina agent prompt` on a running agent needs `--steer` and blocks for minutes, so
background it as `( nohup antonina agent prompt --id <id> --steer "$(cat file)" > log 2>&1
< /dev/null & )`. `grep`, `sed` and `python3` are **not** on this host's `PATH`; the usable
ones are `/gnu/store/2daxpqdg74pan6bp78cp6jzknb435w9d-coreutils-9.1/bin` and
`/gnu/store/05vy146ycavwjm8va1d8qym188s14vzv-grep-3.11/bin`, and `node` works for scanning.

### Pass `coord-7f2a`, 2026-09-26T18:27Z-18:33Z: all three fronts alive and correctly branched, and the pool-membership currency is not reproducible

Fresh reconciliation pass, about six minutes of host clock. Re-claimed from
`coord-9b4e`. `post-milestone-acceptance` is `34e971a`, clean and identical to
`origin` after an explicit `git fetch --all --prune`; `main` is `c0ecd7c` on
`origin`, untouched, checked with `git ls-remote` because this host's fetch
refspec tracks only the accumulation branch.
`git diff --name-only aa662a4..HEAD` is `docs/skills/scheduled.md`, this item
and [w-4b1e07](w-4b1e07.md) only, so **`src/` is still byte-identical to the
base the held sweep fix was written against** and `79309a1` still applies to
the current head with no drift. Nothing was integrated, merged or pushed to
`main` on this pass.

**1. The three fronts the previous pass opened are all alive, minutes old, and
all three worktrees are on the branch their front is recorded against.** That
is worth stating positively, because this item has now recorded the opposite
nine times:

| front | worktree | branch checked out | worktree state found |
|---|---|---|---|
| `a3f19c` | `/workspace/madgab-int-a3f19c` | `madgab-int-79309a1` at `2bacbcb` | clean; building a base arm under `/workspace/mg-base-a3f19c` |
| `b47d02` | `/workspace/madgab-depth4-b47d02` | `madgab-depth4-b47d02` at `74e3d8b` | `M src/lib.rs` (its depth probe) plus untracked `an1.js`, `an2.js`, `measure.sh`, `measurements/b47d02/` |
| `c81e55` | `/workspace/madgab-depthcap-c81e55` | `madgab-depthcap-c81e55` at `2bacbcb` | `M src/lib.rs` plus untracked `src/probe.rs` |

All three are `running` and alive, one prompt each, and all three are meant to
be left running. `a3f19c` is mid-measurement on twelve targets; `b47d02` is
running cap-3-versus-cap-4 replicates; `c81e55` is writing its probe. No front
reported, so there was nothing finished to review, and per the previous pass's
rule 4 no fourth front was opened.

**Durability: the one gap that is cheap to close now, and it is not closed.**
`git ls-remote origin` finds **no ref at all** for `madgab-int-79309a1`,
`madgab-depth4-b47d02` or `madgab-depthcap-c81e55` - all three branch names are
local-only - while `madgab-sweep-4e8a52` (`acbd6dd`) and `scratch/2f7a10-cost`
(`74e3d8b`) are correctly on `origin`. Pushing the three tips now would only
publish commits that are already ancestors of `origin/post-milestone-acceptance`
and would add no durability, so it was deliberately not done: the hazard is
`b47d02`'s and `c81e55`'s **uncommitted** `src/lib.rs` modifications, which
only their own commits can make durable, and both were already told to push.
Confirm with `git show <branch>:REPORT-<id>.md` **and** `git ls-remote origin
<branch>` before treating any front's evidence as durable.

**2. A defect in this queue's own measurement currency, found by this
coordinator in twenty seconds and independently by two of the three running
fronts: the deduplicated pool the release binary produces is not run-to-run
reproducible.** On the accumulation head's own release binary - built 16:35Z,
and the last commit to touch `src/` is `1786530` at 16:22:49Z, so the binary is
exactly the current `src/` - three consecutive `--approximate --top 20` runs on
one input give:

```text
candidates=14555
candidates=14554
candidates=14554
```

with a byte-identical visible top-20 every time. That is why
`tests/approx_determinism.rs` is green and the defect is invisible to it: the
test asserts on visible proposals, not on the pool behind them. `a3f19c` hit it
on twelve targets (`recognize speech` 17 078 then 17 076, `a whole lot of
trouble` 17 882 then 17 888) and `b47d02` hit it as a non-byte-deterministic
probe trace and widened its own comparison to three replicates per arm. Neither
was looking for it.

This bears directly on criterion 3 of this item and on the admissibility rule
inherited from [w-c4e8d7](w-c4e8d7.md), which denominates every reachability
claim in pool membership: a 0 -> 1 or 3 -> 0 transition at a one-candidate
margin is not decidable, and a per-witness `true`/`false` column is not a
measurement. It is filed as [w-6b91d3](w-6b91d3.md) - `open`, unowned, with
the reproduction, the two witnesses, three discriminating hypotheses and a
collision boundary that keeps it out of `coverage_tuples` while the fronts in
flight are there. **No front was opened on it**: the remaining territory is
`a3f19c`'s, `b47d02`'s and `c81e55`'s, and manufacturing a backlog is what the
itinerary warns against. What it does buy is that the new item exists, so the
next pass does not have to rediscover the defect from an agent log.

**3. State of the milestone: unchanged and not met.** The requested wording is
still 0 pool members on the canonical target,
`approximate_finds_classic_madgab_resegmentation` is still red on the base and
on the fix, [w-a02d28](w-a02d28.md) stays `blocked` on that same fact, and
`recognize speech` -> `wreck a nice beach` remains green. A green `--lib` run,
or an arm reporting "wording present = true" for a per-word check, must not be
read as milestone progress.

Next action for a later fresh pass, in priority order:

1. `antonina agent status --id a3f19c`, `--id b47d02`, `--id c81e55`, then
   their logs. For each, confirm durability **before** treating anything in it
   as evidence: `git ls-remote origin <branch>` with
   `git show <branch>:REPORT-<id>.md`. A progress line and a green suite are
   not completion signals.
2. When `a3f19c` reports the integration clean with its guards re-measured,
   **merge `madgab-int-79309a1` into `post-milestone-acceptance` and push that
   branch**, keeping `REPORT-a3f19c.md` out of the accumulation branch if a
   docs-only footprint is preferred. Re-run the fence scan on the merged
   result. Never push or merge it to `main`. Ask for the guard tables as
   replicate ranges rather than single counts, per [w-6b91d3](w-6b91d3.md).
3. `3b14482` stays closed for integration, as recorded above.
4. Open the implementation front **only** from `b47d02`'s depth-4 row and
   `c81e55`'s recommendation, and only once both are durable. Do not manufacture
   the territory in advance.
5. [w-6b91d3](w-6b91d3.md) may be claimed once those fronts have reported,
   since its criterion 6 compares against them.
6. Only once `approximate_finds_classic_madgab_resegmentation` passes on
   `post-milestone-acceptance` on its own merits, with no phrase-specific case
   in `src/`, close this item, [w-4b1e07](w-4b1e07.md) and
   [w-a02d28](w-a02d28.md) together and re-check every milestone condition in
   the itinerary.

Host notes unchanged: `git -c commit.gpgsign=false commit`; `/tmp` is `noexec`,
so build under `/workspace` or `/tmp/opencode`; `git archive <sha> | tar -x
-C <dir>` for a second arm; verify durability with `git ls-remote origin`, not a
local remote-tracking ref; `antonina agent prompt` on a running agent needs
`--steer` and blocks for minutes, so background it as
`( nohup antonina agent prompt --id <id> --steer "$(cat file)" > log 2>&1 < /dev/null & )`.
`grep`, `sed` and `python3` are **not** on this host's `PATH`; the usable ones
are `/gnu/store/2daxpqdg74pan6bp78cp6jzknb435w9d-coreutils-9.1/bin` and
`/gnu/store/05vy146ycavwjm8va1d8qym188s14vzv-grep-3.11/bin`, and `node` works
for scanning.

### Pass `coord-c4d1`, 2026-09-26T18:31Z-18:35Z: the integration front has landed its commit and passes the fence scan here, a durability gap is re-issued, and the pool-reproducibility defect gets its own front

Fresh reconciliation pass. Re-claimed from `coord-7f2a`. `post-milestone-acceptance`
is `8d4cab1`, clean and identical to `origin` after an explicit
`git fetch --all --prune`; `main` is `c0ecd7c` on `origin`, untouched, checked with
`git ls-remote` because this host's fetch refspec tracks only the accumulation
branch. `git diff --name-only aa662a4..HEAD` is docs only, so `src/` is still
byte-identical to the base the held sweep fix was written against.

**1. The three fronts are alive, correctly branched, and one of them has produced
finished work for the first time.** All three worktrees are on the branch their
front is recorded against, which is worth stating positively again because this
item has recorded the opposite nine times:

| front | worktree | branch checked out | state found |
|---|---|---|---|
| `a3f19c` | `/workspace/madgab-int-a3f19c` | `madgab-int-79309a1` at `e5b1c2e` | **clean, integration commit landed**; `--lib` 53 passed / 0 failed; suites in progress |
| `b47d02` | `/workspace/madgab-depth4-b47d02` | `madgab-depth4-b47d02` at `74e3d8b` | `M src/lib.rs` (depth probe) plus untracked probes and `measurements/b47d02/` |
| `c81e55` | `/workspace/madgab-depthcap-c81e55` | `madgab-depthcap-c81e55` at `2bacbcb` | `M src/lib.rs` plus untracked `src/probe.rs`, `measurements/` |

**2. The integration front's commit is reviewable now, and the review this pass
can do is a real one**, because `e5b1c2e` exists rather than being an uncommitted
working-tree diff:

```text
e5b1c2e  w-2f7a10: give each member of a shape class its own sweep rate in the
         coverage reserve        src/lib.rs only, +189 -15
```

Fence scan, re-run here on the pushed-into-the-worktree commit rather than
inherited from the agent's own note: `git diff 2bacbcb..e5b1c2e --name-only` is
`src/lib.rs` only, so no `tests/` change, neither acceptance test relaxed or
re-baselined, no integration test added by this commit; **0** added lines contain
any acceptance word or phrase or any `ZZ_`/`zz_`/`MADGAB_` token; and
`git diff aa662a4..2bacbcb -- src` is **empty**, so `79309a1` applied with no
`src/` drift. `git log` in that worktree also shows the gpg hazard being hit and
recovered from in place, so the recorded host note is still load-bearing. Its
`--lib` result of 53 passed / 0 failed is confirmed here as the only result of
its four that exists.

**This is not integration.** No report, no pushed branch, and the pool and guard
tables it owes are not in yet.

**3. Durability: the gap is still open and is re-issued as a steer, not as a
note.** `git ls-remote origin` finds no ref for `madgab-int-79309a1`,
`madgab-depth4-b47d02` or `madgab-depthcap-c81e55`; all three are local-only, so
`e5b1c2e` is single-copy. Pushing the tip from here was still deliberately *not*
done: for `b47d02` and `c81e55` the tip is an ancestor of
`origin/post-milestone-acceptance` and carries nothing of theirs, so publishing
it would add no durability, and the only thing that can make their work durable
is their own commits. For `a3f19c` the situation is different and better - the
work is already committed - so the push is its own to make, and it has been
asked for directly, together with `REPORT-a3f19c.md` on the same branch.
Confirm with `git ls-remote origin <branch>` **and** `git show <branch>:REPORT-<id>.md`
before treating any front's evidence as durable.

**4. The pool-reproducibility defect is claimed and worked, in parallel, without
contending on the live fronts' file.** [w-6b91d3](w-6b91d3.md) is now `working`
under `coord-c4d1`, and a new front is open:

| front | branch | base | worktree | front |
|---|---|---|---|---|
| `d3b7c2` | `madgab-pooldet-d3b7c2` (pushed at `8d4cab1`) | `post-milestone-acceptance` | `/workspace/madgab-pooldet` | line-level localisation of the non-determinism, then a landed general fix, a 20-run reproducibility proof, a tightened `approx_determinism`, and both guards |

The previous pass deferred this claim until the three fronts reported, because
criterion 6 compares against them. Criteria 1 and 2 do not: a line-level
localisation inside the current head's own `src/` needs no comparison arm, and
until it exists every pool delta of one or two candidates in the three live
arms is undecidable. The front is in hashing, tie-breaks and collection order,
in its own worktree, while the three live fronts are in
`coverage_tuples`/`sweep_index`, so the two meet only at merge time. All three
live fronts were told to quote **replicate ranges rather than single counts**.

**5. State of the milestone: unchanged and not met.** The requested wording is
still 0 pool members on the canonical target,
`approximate_finds_classic_madgab_resegmentation` is still red on the base and on
the fix, [w-a02d28](w-a02d28.md) stays `blocked` on that same fact, and
`recognize speech` -> `wreck a nice beach` remains green. A green `--lib` run is
not milestone progress, and neither is a fix that is measured red-to-red.

Next action for a later fresh pass, in priority order:

1. `antonina agent status --id a3f19c`, `--id b47d02`, `--id c81e55`,
   `--id d3b7c2`, then their logs. For each, confirm durability **before**
   treating anything in it as evidence: `git ls-remote origin <branch>` with
   `git show <branch>:REPORT-<id>.md`. A progress line and a green suite are not
   completion signals.
2. When `a3f19c` reports the integration clean, with its guards and pool deltas
   as replicate ranges, **merge `madgab-int-79309a1` into
   `post-milestone-acceptance` and push that branch**, keeping
   `REPORT-a3f19c.md` out of the accumulation branch if a docs-only footprint is
   preferred. Re-run the fence scan on the merged result, and re-measure both
   guards *after* the merge rather than only before it. Never push or merge it
   to `main`.
3. `3b14482` stays closed for integration, as recorded above.
4. Open the depth-4 implementation front **only** from `b47d02`'s depth-4 row and
   `c81e55`'s recommendation, and only once both are durable. Do not manufacture
   that territory in advance.
5. `d3b7c2`'s localisation decides whether criteria 3 to 6 of
   [w-6b91d3](w-6b91d3.md) can be judged at all. If the fix lands in
   `coverage_tuples`, treat that as a collision with this item's live fronts and
   sequence it, do not merge both blindly.
6. Only once `approximate_finds_classic_madgab_resegmentation` passes on
   `post-milestone-acceptance` on its own merits, with no phrase-specific case in
   `src/`, close this item, [w-4b1e07](w-4b1e07.md) and [w-a02d28](w-a02d28.md)
   together and re-check every milestone condition in the itinerary.

Host notes unchanged: `git -c commit.gpgsign=false commit`; `/tmp` is `noexec`,
so build under `/workspace` or `/tmp/opencode`; `git archive <sha> | tar -x
-C <dir>` for a second arm; verify durability with `git ls-remote origin`, not a
local remote-tracking ref; `antonina agent prompt` on a running agent needs
`--steer` and blocks for minutes, so background it as
`( nohup antonina agent prompt --id <id> --steer "$(cat file)" > log 2>&1 < /dev/null & )`.
`grep`, `sed` and `python3` are **not** on this host's `PATH`; the usable ones
are `/gnu/store/2daxpqdg74pan6bp78cp6jzknb435w9d-coreutils-9.1/bin` and
`/gnu/store/05vy146ycavwjm8va1d8qym188s14vzv-grep-3.11/bin`, and `node` works for
scanning.

## Pass `coord-b7e3`, 2026-09-26T18:41Z: all four fronts are live, and `a3f19c` is steered to converge instead of re-measuring

Fresh reconciliation pass. No repository code was changed here and no work item
state was moved. The accumulation head is `ed29848`, equal to
`origin/post-milestone-acceptance` and clean; `main` is untouched. Nothing was
merged to `main` and nothing was merged to the accumulation branch.

**All four open fronts are still `running` and were left running** for the next
fresh pass to inspect afresh: `a3f19c` (`/workspace/madgab-int-a3f19c`, 3 prompts),
`b47d02` (`/workspace/madgab-depth4-b47d02`), `c81e55`
(`/workspace/madgab-depthcap-c81e55`) and `d3b7c2` (`/workspace/madgab-pooldet`).
Durability was checked the objective way before anything in a log was treated as
evidence: only `madgab-int-79309a1` (`e5b1c2e`) and `madgab-pooldet-d3b7c2`
(`8d4cab1`) have remote refs, and **no front has yet pushed a `REPORT-*.md`**.
The other two front branches are local-only, so their numbers are still
first-hand-agent claims and not durable state.

**The one substantive fact this pass learned**, from `a3f19c`'s own live log: its
`git archive` base arm reports `approximate_finds_classic_madgab_resegmentation`
as the single failure with the **identical top-12 wordings** the branch arm
produces. So `79309a1` is output-neutral on the visible list for the canonical
target. That is the refutation the milestone item already expected, now measured
on both arms by the integrating front itself rather than asserted.

`a3f19c` was therefore burning its remaining budget re-deriving things three other
fronts have already settled, so this pass steered it in the background (host rule:
`--steer` blocks for minutes) to stop measuring and close out: push
`REPORT-a3f19c.md` on `madgab-int-79309a1` as the blocking deliverable, give an
explicit yes/no merge recommendation with its evidence, state whether the visible
top-50 changed on *any* target, and name the residual as the depth cap rather
than cost accounting. It was told not to merge the accumulation branch itself and
never to touch `main`. The single result that would change the recommendation is a
guard that passes on the base and fails on the branch arm.

**Milestone status: unchanged, not met.** The requested wording is still absent
from the pool for the canonical target, `approximate_finds_classic_madgab_resegmentation`
is still red, [w-a02d28](w-a02d28.md) stays `blocked` on that fact, and
`recognize speech` -> `wreck a nice beach` stays green. A green `--lib` run is not
milestone progress, and an output-neutral fix is not a milestone fix.

Next action for a later fresh pass, in priority order:

1. `antonina agent status --id a3f19c --id b47d02 --id c81e55 --id d3b7c2`, then
   their logs. Durability gate before belief:
   `git ls-remote origin <branch>` and `git show <branch>:REPORT-<id>.md`.
2. If and only if `a3f19c`'s pushed report says merge, **merge
   `madgab-int-79309a1` into `post-milestone-acceptance` and push that branch**,
   re-running the fence scan and both guards *after* the merge. Keep
   `REPORT-a3f19c.md` out of the accumulation branch if a docs-only footprint is
   preferred. Never to `main`.
3. Open the depth-4 implementation front only from `b47d02`'s durable depth-4 row
   and `c81e55`'s durable recommendation. Do not manufacture that territory in
   advance, and do not open a second depth-cap front while `c81e55` is live.
4. `d3b7c2`'s localisation decides whether criteria 3 to 6 of
   [w-6b91d3](w-6b91d3.md) are judgeable. If its fix lands in `coverage_tuples`,
   sequence it against this item's live fronts rather than merging both blindly.
5. `w-4b1e07` is still `working` under `coord-9f2c` but has no front of its own
   left; its pointer text already defers to this item, so a later pass should
   either fold it into this item's closing or record why it stays separate.
6. Close this item, [w-4b1e07](w-4b1e07.md) and [w-a02d28](w-a02d28.md) together,
   and only once `approximate_finds_classic_madgab_resegmentation` passes on
   `post-milestone-acceptance` on its own merits with no phrase-specific case in
   `src/`.

### Pass `coord-b8f4`, 2026-09-26T18:43Z: durability repaired for `b47d02`, `a3f19c` steered to close out, three of four fronts now on the remote

Fresh pass, one minute after `coord-b7e3`. No front had produced a new commit in
the interval, so nothing was integrated and no milestone claim changed: the
milestone is still unmet, `approximate_finds_classic_madgab_resegmentation` is
still red, [w-a02d28](w-a02d28.md) stays `blocked`, `recognize speech` ->
`wreck a nice beach` is still green, and `main` is untouched.

Three durable actions:

- **Durability gap closed for `b47d02`.** `madgab-depth4-b47d02` was local-only,
  and it is the branch that carries the `e1a3f7` pool measurement the hold on
  `79309a1` was released on. It now has a remote ref at `74e3d8b`, so that
  measurement and `REPORT-e1a3f7.md` survive outside this machine. Three of the
  four open fronts (`madgab-int-79309a1` -> `e5b1c2e`,
  `madgab-pooldet-d3b7c2` -> `8d4cab1`, `madgab-depth4-b47d02` -> `74e3d8b`) are
  durable; `madgab-depthcap-c81e55` is still local-only and its tip is still the
  base commit `2bacbcb`, i.e. `c81e55` has produced no commit yet.
- **`a3f19c` steered a second time, in the background, to stop measuring.** Its
  live log shows it had ignored the `coord-b7e3` close-out and was building a
  12-target top-50 diff and interleaved runtimes on top of measurements it had
  already reported. It was told that pool-membership noise is tracked in
  [w-6b91d3](w-6b91d3.md) rather than being a merge blocker, and that the one
  thing that matters is a pushed `REPORT-a3f19c.md` with a one-line
  `MERGE RECOMMENDATION: MERGE|HOLD`, the residual named as the depth cap, and no
  self-merge of the accumulation branch. This was the last steer; if its branch
  tip is still `e5b1c2e` with no report at the next pass, that front is treated as
  failed and its fix is not merged on a recommendation that was never made durable.
- **`w-4b1e07` fold decision recorded** (see its own pass note): it is frontless
  and folds into this item's closing.

`d3b7c2` is confirmed genuinely live rather than merely `running`: its log shows
it reading `src/lib.rs` returns at lines 534/540 and rewriting them in its own
worktree, which is the localisation [w-6b91d3](w-6b91d3.md) criteria 1 and 2
need. `b47d02` and `c81e55` are still at one prompt each with no commits.

Next action for a later fresh pass, in priority order:

1. `antonina agent status --id a3f19c --id b47d02 --id c81e55 --id d3b7c2`, then
   logs, then the durability gate before belief:
   `git ls-remote origin madgab-int-79309a1 madgab-depth4-b47d02` and
   `git show madgab-int-79309a1:REPORT-a3f19c.md`.
2. Merge `madgab-int-79309a1` into `post-milestone-acceptance` **only** if that
   pushed report says MERGE, then re-run the fence scan and both guards after the
   merge. Never to `main`.
3. Open the depth-4 implementation front from `b47d02`'s durable depth-4 row and
   `c81e55`'s durable recommendation; `madgab-depthcap-c81e55` is still
  commit-free, so that front is not openable yet. Do not open a second depth-cap
   front.
4. Judge [w-6b91d3](w-6b91d3.md) criteria 3 to 6 once `d3b7c2` has pushed
   `REPORT-d3b7c2.md` with `file:line` localisation; sequence any `coverage_tuples`
   overlap against this item's live fronts rather than merging both blindly.

### Coordinator pass `coord-3f10`, 2026-09-26T18:55Z: `79309a1` is merged, and `c81e55` has refuted the search-side framing of this item

Two durable facts changed under this item, in opposite directions.

**1. `madgab-int-79309a1` is merged into `post-milestone-acceptance` as `27f9776`**,
on `a3f19c`'s own durable `MERGE RECOMMENDATION: MERGE`
(`git show madgab-int-79309a1:REPORT-a3f19c.md` §0, verified on the remote before
merging). Review before integrating, re-run here on the merge commit rather than
inherited:

- `git diff HEAD~1 HEAD --stat -- src tests` -> **`src/lib.rs` only**, +189 -15. No
  test file was touched, so neither acceptance test was edited, relaxed,
  re-baselined or skipped.
- Fence scan over the added `src/` lines for `wreck|beach|recognize|justice|stupid|dupe|hid|came`
  and `ZZ_|MADGAB_|zz_|env::var`: **0 hits**.
- `EMIT_PROFILE_MAX_DEEP` is still **3** at `src/lib.rs:212` on the merge commit; the
  fix only *reads* it.
- Post-merge suites, release: `--lib` **53/53**, `--test approx_determinism` **2/2**,
  `--test no_phrase_hard_coding` **6/6**, `--test exact_determinism` **1/1**,
  `--test corpus_integration` **10 passed / 1 failed** — the single failure is
  `approximate_finds_classic_madgab_resegmentation`, red on the base arm too, with the
  **identical** twelve wordings. `approximate_finds_recognize_speech_resegmentation`
  and `approximate_output_is_locked` are green.
- `main` untouched: remote `main` is still `c0ecd7c`. Only `post-milestone-acceptance`
  and its focused child branches are written.

**2. `c81e55` succeeded and its report is durable on the remote**
(`madgab-depthcap-c81e55` at `b8391f6`, `git show madgab-depthcap-c81e55:REPORT-c81e55.md`;
the worktree is still local-only, the branch is not). Its headline is a *production*
number: with the requested index tuples pushed through the ordinary `build`, the
ordinary scorer and the ordinary `select_diverse`, `hits justice dupe hid came` lands at
**raw rank 9867 of 17 908, score 0.819901291, against a rank-49 cutoff of 0.915691888**
— a gap of **0.0958**, about **11x the width of the visible top-50 band**.

Consequences this item must absorb, because they change what its next front should be:

- The **depth cap is inert** for the shapes that carry the wording. The two admitting
  segmentations are **5-slot**, where a depth-4 class needs **30 draws of the 16**
  available, so `EMIT_PROFILE_MAX_DEEP = 4` would offer depth 4 only in the 41 four-slot
  segmentations — at most 41 tuples of 3 689, none of them the milestone's shape.
  `b47d02`'s cap-at-4 arm should *confirm* 0 in the five- and six-slot strata rather
  than contradict this; that is the one thing still worth its runtime.
- **`4e8a52`'s pool-membership metric is nearly uncorrelated with the milestone** for
  this target. Membership is not what `approximate_finds_classic_madgab_resegmentation`
  measures, so this item's inherited admissibility rule is the wrong gate here and must
  not be used to declare the milestone met.
- No reweighting of the six existing axes reaches the cutoff short of moving 21.3 % of
  the scoring mass, which works only because the requested clue maxes RHYTHM.

**This item's next action therefore changes.** The depth-4 *implementation* front
previously listed as next action 3 is **not openable and should not be opened**: its
measured effect on the acceptance test is provably zero. The scoring front is open
instead, as [w-5c8e1f](w-5c8e1f.md), agent `9a41d3` in
`/workspace/madgab-score-9a41d3` on `madgab-score-reseg-9a41d3`. This item stays
`working` and now owns integration of that front; `b47d02` and `d3b7c2` are untouched
and still running.

Next action for a later fresh pass, in priority order:

1. `antonina agent status --id b47d02 --id d3b7c2 --id 9a41d3`, then logs.
2. Durability gate before belief, always:
   `git ls-remote origin madgab-depth4-b47d02 madgab-pooldet-d3b7c2 madgab-score-reseg-9a41d3`
   and `git show madgab-score-reseg-9a41d3:REPORT-9a41d3.md` when it exists.
3. Merge `madgab-score-reseg-9a41d3` **only** on a pushed `MERGE RECOMMENDATION: MERGE`,
   then re-run the fence scan and all five suites, and re-check the green guard's
   0.00127 margin. Never to `main`.
4. Judge [w-6b91d3](w-6b91d3.md) criteria 3 to 6 when `d3b7c2` pushes
   `REPORT-d3b7c2.md`; sequence any `coverage_tuples` overlap rather than merging both.
5. If `9a41d3` lands M7a and the canonical test is still red — the predicted outcome —
   do not open a second search-side front. The remaining general degrees of freedom are
   new axes with an independent argument, as a new work item.

### `b47d02` succeeded and confirms `c81e55` by measurement: the depth-4 row is empty

`b47d02` finished (`state: succeeded`) and its report is durable on the remote:
`madgab-depth4-b47d02` at `b8aa6d8`, `git show madgab-depth4-b47d02:REPORT-b47d02.md`.
That branch is **measurement-only and must never be merged** (it keeps `e1a3f7`'s
`cost_probe` instrument on purpose).

Its measured result, twelve targets, four replicates per arm, release, default path:

| arm | offered | accepted | **depth-4 offers** |
|---|---|---|---|
| cap 3 | 45 387 | 36 662-36 674 | **0** |
| cap 4 (main) | 45 387 | 36 647-36 664 | **0** |

`EMIT_PROFILE_MAX_DEEP = 4` offers a depth-4 tuple **zero times in 45 387 offers, on four
independent runs, across twelve targets**, and the per-depth split is unchanged
(d1 16 357, d2 26 266, d3 2 764, d4 0 on both arms). The per-slot and per-pool deltas
between the arms are all inside each arm's own run-to-run band. **On the default path
the cap change is a no-op**, which is the prediction `c81e55` §2.1 made from the class-walk
arithmetic, now measured rather than inferred.

The auxiliary arm prices the reason: at `EMIT_PROFILE_RESERVE = 96` the walk *does*
construct four-slot classes (2 773 depth-4 offers), and **79 %** of four-deep tuples are
then refused — by the additive total budget, not by a per-word gate. But even there, 1 085
offers at the right slot set produce the requested word **0 times**: the 16-tuple reserve
samples roughly 0.001 % of the four-deep rectangle. The green guard
`wreck a nice beach` survives 8 of 8 runs on both arms.

Two consequences, and they are the closure of this item's search-side question:

1. **No depth-cap implementation front is to be opened**, on either front's evidence.
   `c81e55` measured the production score of the requested wording at 9867 of 17 908;
   `b47d02` measured that constructing its shape at all costs 6x the emission reserve and
   still yields 0 occurrences. The gate is the objective, not the enumeration.
2. `b47d02` §1.1 independently reproduces the run-to-run pool nondeterminism filed as
   [w-6b91d3](w-6b91d3.md) (TSVs differ from line 5 633; ±11 accepted tuples), and
   quantifies a **±25 accepted tuple / ±0.06 pp** measurability floor for that
   instrument. Any future claim smaller than that band is noise, not a result.

This item therefore has no live search-side front of its own. Its remaining job is
integration of [w-5c8e1f](w-5c8e1f.md) (agent `9a41d3`, running) and of
[w-6b91d3](w-6b91d3.md) (agent `d3b7c2`, running) into `post-milestone-acceptance`.
`c81e55`'s and `b47d02`'s worktrees may now be removed by a later pass once their reports
are read from the remote branches, which is where the durable copies live.

### coord-1a4c pass, 2026-09-26T18:58Z: durability repair, no new front

Two things a fresh pass must know, neither of which changes the conclusions above.

1. **[w-5c8e1f](w-5c8e1f.md) was missing from the shared branch.** `coord-3f10`
   announced the front in commit `e123dbb`'s message but never staged the file, so
   `9a41d3` had been running for six minutes against a work item that existed only as
   an untracked file on this host. It is now committed and pushed at `181172b`, so the
   claim, its collision boundary and its completion criteria are durable and any later
   pass can discover it. Nothing in the front's scope changes.
2. **All four of this item's agents are terminal** (`e1a3f7`, `a3f19c`, `b47d02`,
   `c81e55` — all `succeeded`), and every one of their reports is reachable from
   `origin` at the refs listed in this item's metadata. The stale `agents:` line above
   claimed three were still running; it did not, and no depth-cap or sweep front is to
   be reopened on this item.

Live fronts elsewhere, verified by `antonina agent list` at 18:56Z: `9a41d3` (6 min,
`/workspace/madgab-score-9a41d3`, the `w-5c8e1f` similarity-axis front) and `d3b7c2`
(23 min, `/workspace/madgab-pooldet`, the `w-6b91d3` pool-determinism front). `9a41d3`'s
log shows the phrase-free resegmentation guard already landed **red-first** on the
canonical target, 0 of 50 visible proposals a full resegmentation, with the other six
table targets green by census (29-50) — which is exactly the per-target red/green
record `w-5c8e1f` asks for.

This item keeps `state: working` because its completion criteria (the canonical
wording generated) are still unmet; but it has no front of its own and its only
remaining action is integrating `w-5c8e1f` and `w-6b91d3`. Do not re-derive the
search side: `c81e55` §4/§5 and `b47d02` have closed it by measurement, and per the
`w-5c8e1f` handoff the next general degree of freedom is a *new* axis with an
independent argument, as a **new** work item, not as a revival of this one.

### coord-7c31 pass, 2026-09-26T19:04Z: both live fronts steered to close out, and the next front opened

Verified by `antonina agent list` at 19:03Z: three agents `running` — `d3b7c2` (30 min,
[w-6b91d3](w-6b91d3.md)), `9a41d3` (13 min, [w-5c8e1f](w-5c8e1f.md)), and `558697`
(new, 15 s, [w-558697](w-558697.md)). No search-side front is opened on this item, and
none should be: `c81e55` §4/§5 and `b47d02` have closed that question by measurement, and
this pass added nothing to contradict them.

The one objective change this pass found is in `9a41d3`'s log, not in this item's territory:
its M7a head fails **two** acceptance guards, `approximate_finds_recognize_speech_resegmentation`
and `approximate_output_is_locked`, alongside the expected red. Details and the steer are in
[w-5c8e1f](w-5c8e1f.md). This item's own completion criteria are unchanged and still unmet;
its remaining action is still integration of `w-5c8e1f` and `w-6b91d3` on their pushed
`MERGE` lines.

Opened [w-558697](w-558697.md) this pass, on the reasoning `w-5c8e1f`'s handoff names as the
honest next move once M7a lands: the search side is refuted, weights are closed by
[w-3f9c02](w-3f9c02.md), so the remaining general degree of freedom is a **new axis with an
independent argument**. It is investigation only — one pushed report, no `src/` change — which
is what makes it safe to run alongside a front in `src/lib.rs` and another in the hashing
paths. It is explicitly forbidden from recommending a reweighting (`c81e55` §5) and from
proposing any axis whose reason for existing is that it lifts the requested clue.
