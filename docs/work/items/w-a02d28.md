---
work_item: true
id: w-a02d28
state: open
priority: high
owner: null
updated: 2026-09-26T11:50:00Z
branch: null
worktree: null
---

# Two approximate-mode acceptance tests fail at the milestone base

## Goal

`cargo test --release` is green on the accumulation branch, or the red tests
state a behaviour decision that is recorded somewhere durable.

## Context

Found while landing the behaviour-preserving speedups in
[w-7fa26c](w-7fa26c.md). At `d46d154` (the content of
`post-milestone-acceptance`) these two tests in
`tests/corpus_integration.rs` fail, and they fail identically on an unmodified
checkout of that commit:

- `approximate_finds_classic_madgab_resegmentation` — expects
  `hits justice dupe hid came` in the top 50 for `It's just a stupid game`;
  the top proposals are now dominated by `it justice ...` spellings.
- `approximate_finds_recognize_speech_resegmentation` — expects
  `wreck a nice beach` in the top 50 for `recognize speech`; the top proposals
  are now `yet a guys each`, `yeah can i.'s it's`, ... and
  `wreck a nice pitch` at rank 8 of the printed head.

Both words are still *reachable* in the fuzzy lattice
(`acceptance_sequences_are_reachable_in_fuzzy_lattice` passes), and both
resegmentations still appear in the proposal list at a much lower rank. The
milestone commit that changed the scoring and the diversity selection
(rhythm axis, boundary-keyed diversity, larger completed pool) moved them out
of the top 50 without any of these tests being updated.

This blocks the "`cargo test --release` is green" completion criterion in
[w-7fa26c](w-7fa26c.md), and it cannot be fixed by a behaviour-preserving
change: the two options are to change the search (which is a clue-quality
decision) or to change what the tests assert (which is a product decision about
how strongly approximate mode must surface the canonical Mad Gab wording).

## Completion criteria

- A decision is recorded: either the search is changed so both canonical
  resegmentations are back in the top 50 (a clue-quality change, probably
  [w-4b1e07](w-4b1e07.md) territory), or the tests are rewritten to assert
  something that is actually true today — e.g. that the canonical wording is
  present in the full proposal set, with an explicit note that rank is not
  guaranteed.
- `cargo test --release` is green.
- If the tests are relaxed, the reason and the accepted rank/position are
  written down here, not just in the test body.

## Handoff / notes

Not started. Do not fix this as part of a pure performance change: it will
show up as an unrelated failure and tempt a behaviour edit. Reproduce with
`cargo test --release --test corpus_integration` on any checkout of `d46d154`
or later.

## Pass 2026-09-26T11:50Z (coordinator coord-11b7): one of the two is resolved, the other is now a scorer question

Update on the two tests this item is about, measured on
`post-milestone-acceptance` at 65ce182:

- `approximate_finds_recognize_speech_resegmentation` now **passes**. The
  ordered selection rule ([w-3c8e10](w-3c8e10.md), integrated as 98c7d3b) put
  `wreck a nice beach` back in the visible list, and this item-s reading of it
  as a clue-quality regression was wrong. No test was relaxed and no
  behaviour was restored by special-casing.
- `approximate_finds_classic_madgab_resegmentation` still fails, and this
  item-s framing of it is also superseded: the alignment is not being
  crowded out of the top 50 by the other scoring work, it scores 0.819901
  against a 0.929514 cutoff ([w-6d1f45](w-6d1f45.md)). Relaxing the test
  would therefore record a defect as intended behaviour.

So the remaining decision is not "search or relax the test": the search is
sound and the objective is what under-rates the alignment. That decision now
belongs to [w-04f83f](w-04f83f.md). This item stays open only to close it
once the second acceptance test passes on its own merits, or to record an
explicit human decision to relax it; do not relax it as a side effect of
unrelated work.

## Pass 2026-09-26T12:05Z (coordinator coord-3f2a): unchanged, and the second test is now the whole of the milestone

Re-measured on `post-milestone-acceptance` at 9ae1f5a, which adds the
integrated reachability front ([w-6d1f45](w-6d1f45.md)):

- `approximate_finds_recognize_speech_resegmentation` **passes**.
- `approximate_finds_classic_madgab_resegmentation` **still fails**, for
  the reason this item already concluded: the alignment is reachable and
  enumerated-adjacent, and scores 0.819901 against a top-10 cutoff of
  0.929514. The pool grew to 15710 candidates and the cutoff is unchanged
  at 0.915691888 for `--top 50`.

`cargo test --release --test corpus_integration` on this base: 8 passed,
1 failed. No test has been relaxed, and none should be: the decision this
item was opened for — search or relax the assertion — has been made in
favour of the search, and it is being carried out by
[w-04f83f](w-04f83f.md), whose agent is running.

This item closes the moment that test passes on its own merits. It should
not be closed by relaxing the assertion, by tuning a retention constant,
or as a side effect of unrelated work.
