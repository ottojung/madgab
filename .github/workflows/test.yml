name: Tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    name: cargo test + clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
      - name: unit tests
        run: cargo test --lib --bins --no-fail-fast
      - name: real-corpus integration tests
        env:
          MADGAB_TRACE_PHRASES: "hits justice dupe hid came|wreck a nice beach"
        run: cargo test --release --test corpus_integration --no-fail-fast
      - name: cargo clippy
        run: cargo clippy --all-targets -- -D warnings
      - name: smoke test (CLI binary builds and runs)
        run: |
          cargo build --release
          ./target/release/madgab --help
          ./target/release/madgab --transcribe "cat dog"
